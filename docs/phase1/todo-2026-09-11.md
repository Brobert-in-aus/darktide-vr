# Next session: 11 September 2026

This is a manual next-session queue, not scheduled work. The user clarified that
"schedule" meant update the todo and handover. The accidentally created automation
was deleted; the earlier continuous-development heartbeat remains paused.

## Second optimisation batch: match Virtual Desktop resolution

- [ ] Use **2496x2688 per eye**, the current VDXR recommendation, for the next
  synthetic batch. This is about 38% more pixels than the earlier 2112x2304
  workload; use the exact sizes for comparisons rather than treating the runs
  as equivalent.
- [ ] Prefer `-VdxrReadinessPath <fresh-ready.json>` after the physical session
  is closed. If the headset is unavailable, use explicit `-EyeWidth 2496
  -EyeHeight 2688`, labelled as the saved higher-resolution control. Refresh
  the receipt after VD resolution changes. Never launch a competing XR session.
- [ ] Establish repeated DLSS Quality baselines with FG On/Off, matched scene,
  native/viewer versions, refresh, cap and precise-polling settings. Record
  original, generated, distinct and repeated frames separately; exclude loading
  and background/alt-tab intervals. Keep 120 FPS (60 native plus FG) the target.
- [ ] Match the accepted stereo HUD panel in both physical and synthetic tests.
  Physical launches require `-EnableHudPanel`; its omission caused the flat HUD
  in the evening comparison and was corrected live. Add a reversible equivalent
  to the synthetic runner before claiming an accepted-configuration baseline.
- [ ] Optimise stereo rendering, DLSS/FG and engine costs using the new baseline.
  Keep each candidate isolated and compare it against the same-resolution control.
- [ ] Separate hub versus mission scene cost from resolution and runtime overhead.
  The live hub showed approximately 40 original + 40 generated FPS; attribution
  is still open. Use meaningful performance workloads, not basic gameplay rechecks.
- [ ] Keep 144 Hz secondary. Preserve accepted files and settings, validate
  changes, and record rollback and any required worn acceptance.

HUD-surrounding blur, pickup asymmetry and other unresolved visual work remain
open. Physical melee remains paused. See the [session handover](../handoffs/2026-09-11.md).
