# Next session: 11 September 2026

**Stopped at the user's wrap-up request.** Heartbeat paused; current mirror
A/B/A finished and installation restored. Resume only on a new user instruction.
See [fresh handover](../handoffs/2026-09-11-end-of-day.md).

The user resumed continuous development on 11 September and restarted the
existing 20-minute heartbeat. The separate accidentally scheduled optimisation
job remains deleted. Current results: [frame production](../FRAME-PRODUCTION-2026-09-11.md).

## Next priorities (user, 11 September): HUD blur and cylindrical billboards

Full steps and current evidence are in the
[end-of-day handover](../handoffs/2026-09-11-end-of-day.md#next-priorities-set-by-the-user-on-11-september).
The evening's worn sessions follow the [home test checklist](test-checklist-2026-09-11.md).

- [ ] **DLSS frame-generation blur around HUD objects.** Establish a worn
  baseline with `dlss` and `dlss_g` set individually (the master toggle
  couples them): HUD visible/hidden at FG off/on with SR fixed, then SR off/on
  with FG fixed, same scene and extent. Ask for three regions and still/moving
  reports. Attribute the panel's path into the FG or SR inputs before any
  mask, history or tagging change; candidate fixes are a separate quad layer
  or UI tagging, each needing a worn A/B.
  See [DLSS-UI-DETAIL.md](../DLSS-UI-DETAIL.md#blur-around-hud-items).
- [ ] **Cylindrical billboarding for atlas particle displays.** Attribute the
  visible smoke/haze draws, then run the prepared transactional home trial
  (stock control, restore, cylindrical) on the same scene and accept only on
  the user's per-eye orientation report. The raw-particle correction is
  already installed; the three atlas families are staged only. Keep `c403...`
  excluded until attributed. See
  [ATLAS-BILLBOARD-HOME-TRIAL.md](../ATLAS-BILLBOARD-HOME-TRIAL.md).

- [ ] **Physical versus simulator gap (user priority, 11 September evening).**
  Attributed: the game's own GPU frame is about 50% longer on the headset
  (25.6 to 28.6 ms per stereo frame versus 17.3 ms in the simulator hub,
  same scene, resolution and binaries); bitrate, codec, viewer waits, CPU
  and FG evaluation are excluded. Next: hardware GPU scheduling on/off (one
  reboot per arm), then reduce per-vsync viewer submissions, keep DLSS
  Performance as the headset default. See the
  [handover](../handoffs/2026-09-11-end-of-day.md#next-priorities-set-by-the-user-on-11-september).
- [x] **FG completion failure offline.** Stacked native `0E4718A3` with the
  fixed combined viewer and direct copy evaluates FG in the simulator but
  never completes or publishes from launch (`complete=2 published=0`). That
  focused build lacks the DLSS-G completion-API chain; this branch's native
  (bundle `CAF9E0DF`) completes and publishes FG offline in the hub and in a
  SoloPlay mission, so the stacked build is retired rather than bisected.

Performance items below continue only behind these two.

## Second optimisation batch: match Virtual Desktop resolution

- [x] Fix live DLSS quality-change recovery (offline): the engine registers
  new Streamline viewport handles and re-creates depth/motion at the new
  internal resolution; the gate and ring now follow both (viewport migration,
  ring reallocation). Both switch directions recover within one present in
  the simulator, WARP test added. See
  [FG-QUALITY-SWITCH-RECOVERY-2026-09-11.md](../FG-QUALITY-SWITCH-RECOVERY-2026-09-11.md).
- [x] Verify the quality-change recovery worn: passed on the second attempt
  (20:05, native `F5799F85`, window kept foreground): both switch directions
  recovered and the user confirms FG kept working. The first attempt (19:54)
  was inconclusive because the game window lost OS foreground before the
  switch. See [physical checks](../FG-QUALITY-SWITCH-RECOVERY-2026-09-11.md#physical-check-2-11-september-about-2005-to-2015-passed).
- [x] **FG ring in a remote-server mission** (fixed 11 September evening: the
  ring now holds paused while the engine's DLSS-G options report mode 0,
  verified offline with the in-game FG toggle, deployed as native `5B7D5B9B`,
  and confirmed worn at 21:38: FG held through a dedicated-server mission;
  see [the bundle document](../PERFORMANCE-BUNDLE-2026-09-11.md#generation-off-hold-second-deployment-about-2120)).
  Original record: in `mission_km_station` (dedicated
  mission server, same session) the ring migrated to the mission's new viewport
  handles and resumed, then rejected every present (`binding_rejection`,
  `bindings_match=0`, pose matching) while `context_misses`/`unmapped` climbed
  and no generated pairs reached the viewer. Inspect the options requirement of
  `streamline_present_binding_matches` (`mode==1`, `options_present ==
  constants_present`) against what the engine sets on mission viewports.
  Evidence: `home-fg-quality-switch-20260911/ring-phases.log` from present 8888.
  Evening: the rejection now logs both eyes' bindings (`phase=binding_detail`).
  Offline reproduction is blocked: hub-to-range (`sim-hub-to-range-fg-20260911.ps1`)
  crashed twice on a stock husk-locomotion Lua error leaving the online hub, and
  SoloPlay (`sim-solo-mission-fg-20260911.ps1`) enters the mission with no
  live-ring transition (FG healthy there). Worn diagnostic run (21:14,
  `home-mission-fg-diag-20260911/`): every rejected binding after the mission
  load carried `options mode=0`, so the engine itself had DLSS-G off during
  the mission intro; the ring thrashed (capture, reject, pause, resume each
  present) only while that lasted, then FG published normally in the mission
  (the user saw a solid 120). Fix: hold the ring paused (`generation_off`)
  while the viewport's DLSS-G options say mode 0; verification wrapper
  `sim-fg-toggle-20260911.ps1` (in-game FG off/on via the test flag action).
- [x] **Performance bundle as baseline** (user request, evening): this branch
  built into `build/windows-vs2022` and deployed (transaction
  `deployment-f6350854edec4ce299d69aa1002cd721`); launcher defaults for viewer
  timing and the native ring. Offline controls 89.5 native / 114 distinct FG.
  See [PERFORMANCE-BUNDLE-2026-09-11.md](../PERFORMANCE-BUNDLE-2026-09-11.md).
- [ ] Worn acceptance of the deployed bundle with the normal launch; on
  rejection restore `deployment-f6350854edec4ce299d69aa1002cd721`.
- The user observed no tracked hands, weapons or controller input in that
  remote mission: this is the documented remote-mission gate
  (`presentation_blocked reason=not_first_person_body_mode`), not a regression;
  enabling remote missions is the existing item 2 in
  [REMAINING-DEVELOPMENT.md](../REMAINING-DEVELOPMENT.md).
- [x] **Remote-server mission admission (12 September):** remote dedicated-server
  missions now admit presentation, stock input and the stock-input hand-aim
  route (the same one SoloPlay missions use), with a `remote_mission_input`
  setting; local-authority overrides stay local. Offline: policy tests, the
  repaired stock-source fixtures, full suite 234/234. Deployed as
  `deployment-830a70dd1ee24082b2ae33ed40b44621`. See
  [REMOTE-MISSION-ADMISSION-2026-09-12.md](../REMOTE-MISSION-ADMISSION-2026-09-12.md).
- [x] **Worn check of remote-server missions** (12 September, 22:54):
  hands and controls worked in a dedicated-server mission; logs show
  `authority=remote`, hand-aim input frames, 166 gameplay deliveries, no
  blocked presentation, no fallback (`home-remote-mission-20260912/`). The
  game was closed with a force-quit key, hence the abnormal exit code.
- [x] **Standard-mod installation and release package (12 September):** the
  game starts the viewer itself, the play configuration is the built-in
  default, `Darktide VR Mode.bat` switches VR/flat (executable patch, d3d12
  proxy, load order, per-mode settings copy), the archive is game-folder
  shaped with LICENSE (MIT), notices and USER-GUIDE.md;
  `darktidevr-0.1.0-alpha.1-2bc4d1d8579e.zip` built with a component build
  record. Plain Steam launches confirmed on the development install and on
  a clean package install. See the 12 September handover, second half.
- [ ] **Worn pass on the 12 September additions:** two-pose calibration and
  first-run prompt, focus warning, `hub_third_person` (weapon hiding, no
  camera swing, stick pitch direction/rate), quick-wield return, the shorter
  switch badge, the settings-profile round trip, and a fresh character
  through the prologue.
- [x] **Desktop-mirror independence (12 September, late):** in-game menus
  (modes 5/6) are blitted from the engine's private eye canvas into the
  shared menu texture at the presentation extent on every Present; the
  viewer opens that shared menu for those modes and maps the pointer through
  the presentation source; the client is no longer forced to 1920x1080.
  Character select and title still use the window capture. Offline: 234/234;
  worn check pending (menus, HUD editor, pointer at a small window).
- [x] **Prologue opening cinematic** (was black with stacked subtitles):
  in-engine cinematics now play in stereo with the cinematic camera leading
  translation and yaw and subtitles on the HUD panel; letterbox overlay and
  stock subtitle widgets hidden. Worn: "cutscene works now" (01:16 to 01:19,
  12 September). Option `stereo_cinematics`.
- [ ] **Onboarding hub missions after the prologue** (prologue_hub): now
  reported as the hub with the third-person presentation (a51dd9f);
  awaiting the first worn pass.
- [x] **Psykhanium onboarding gun placement (12 September, midday):** the
  stock-input rules admitted only the shooting range, so gun placement
  (and two-hand support, online reticle) never ran in the training
  grounds. Training grounds added to the rules' range list; offline
  234/234; worn pending.
- [x] **Worn pass on the late-night additions** (12 September, on the
  published archive): ADS focus, block pose on melee, controller badges
  in tutorial and HUD prompts, end-of-training popup in the headset,
  damage indicators on the HUD panel, quick wield with its badge, the
  third-person hub option both ways including the onboarding hub, the
  focus warning appearing and clearing, a Solo mission and a
  remote-server mission start to finish, the grenade-section and gun
  placement fixes. Still unchecked: per-eye marker convergence at the
  far edge, and the flat/VR round trip through the batch file on the
  installed package.
- [x] **Alpha.2: world-surface markers** (worn 13 September: popup placement good, markers fine in a mission).
  `darktidevr_marker_world.lua` draws each unclamped world marker (which
  includes every nameplate), the interaction popup and the tag prompt
  on a plane through its world anchor, facing the shared head centre,
  sized so one primary-projection pixel keeps its angle at that
  distance: the stock widget draws unchanged, and while it draws, the
  renderer's 2D entry points (bitmap, UV bitmap, text, rect, slug icon)
  are converted into the renderer's own 3D calls with the plane's
  transform on a mod-owned world GUI in the level world (the HUD panel's
  proven path: same renderer, its own materials, only the GUI and a
  transform differ). The engine renders that surface for both eyes, so
  the right-eye replay skips those markers and text converges too.
  Clamped markers, markers with rotated or retained passes, and anything
  that fails geometry keep the per-eye 2D route with the width scale.
  Option "World-surface markers" (default on); `/dtvr_marker_plane
  on|off|flip|status` (flip reverses the facing convention if glyphs or
  bitmaps come out back-face culled or mirrored; status reports routed
  and fallback counts with reasons and routing errors). Log
  `DARKTIDEVR_MARKER_PLANE first_frame`. Unknowns for the worn check:
  facing (flip), depth ordering of layers on a world GUI, occlusion by
  world geometry (markers should show through walls as the stock ones
  do; if the world GUI depth-tests, that needs a material or a
  depth-off flag), and the pixel size feeling right at the centre.
  Offline: `marker_world` test (geometry, admission, routing, restore).
  13 September, worn evidence on the direct 3D route: rects and slug text
  on the marker world GUI are depth-tested by the scene whatever the layer
  or flags; the popup's frame and drop shadows (`interaction_background`,
  `dropshadow_medium`) and a probe quad with the HUD panel's material did
  not show at all (the plane's forward pointed away from the viewer, and
  textured world-GUI quads cull their back face, per the HUD panel's own
  note), so the visible "box" was only the title row's rect. New default
  surface `atlas` (`darktidevr_marker_atlas.lua`): the routed stock 2D
  calls are re-issued on an offscreen renderer set up as the HUD panel's
  (own UI world, overlay viewport, completed copy), shifted into one of
  eight 1024x512 cells; per-pass material handles get their own instance on
  that GUI with every recorded Material setter value replayed; one world
  quad per marker through its anchor, faced toward the viewer like the
  HUD panel's quad, shows its cell with the panel's material at layer 1000
  from the camera update. Worn checks: drawn above the scene; popup box
  1:1 with the stock one; stereo. Known limits: content wider than
  +-512 px or taller than +-256 px from the anchor spills into the
  neighbouring cell; more than eight routed markers fall back to the
  per-eye route (`atlas_full`); cells show one frame late. `/dtvr_marker_plane
  surface world|screen` keeps the earlier routes. Offline: `marker_atlas`.
- [x] **Alpha.2 (worn fixed, 13 September): hub camera moved after a menu opened
  while moving.** Reported 13 September: opening a menu while moving or
  just after resets the third-person camera; standing still for a while
  it does not. Cause: `CameraHandler._update_hub_camera_variables` eases
  a movement pull-back (`hub_speed_zoom`) out and an idle close-in
  (`hub_idle_offset`, after a 1 s idle delay) in; a menu stops the
  character, so the close-in ran on behind the menu. The orbit yaw and
  pitch were already restored exactly (`modal_restore` logs). Fix: the
  function gets no elapsed time while presentation mode is 5 or 6.
- [ ] **Alpha.2: wide markers and the interaction popup still diverge at
  the far edge** (worn, 12 September, published archive). What ships:
  each eye's draw scales the widget's width by the ratio of the
  projected widths of the anchor and a point 0.25 m to its right
  (`marker_eye_horizontal_scale`, clamped to 0.8..1.25), the popup
  scaling its scenegraph boxes about the marker pivot and world markers
  their non-text passes. No log evidence from the session (the metrics
  need `/dtvr_marker_metrics` in a mission with a wide marker near the
  edge of view; `DARKTIDEVR_MARKER_METRICS` lines then compare the two
  eyes' draws). Candidates: the clamp is too tight for the eccentricity
  where it shows; text passes are not scaled, so labels still diverge
  even if the boxes converge; the width ratio corrects only the span,
  not where each pixel column lands, which a finite-depth plane per
  marker would (the geometry-only `darktidevr_marker_plane.lua` exists
  for that and has no runtime hook yet). First step: run the metrics
  and read which primitives diverge and by how much before choosing.
- [x] **Alpha.2 (worn fixed, 13 September): the Skitarii player's servo-skull companion is
  invisible to its owner** (user report, 12 September, published
  archive; the log shows it spawning: `adamant_companion_spawn`). The
  owner's skull is driven by `FlyingCompanionMovementExtension`, which
  places it from the stock first-person unit's position and the
  first-person extension's extrapolated rotation with the
  `first_person` offsets of the servo-skull movement settings; the mod
  does not touch companion units. The user adds that it does not appear
  during minigames either, when the skull flies to the terminal a few
  metres from the player, so this is not the skull sitting outside the
  VR eye's view: the unit is not being rendered for its owner (other
  players' skulls are husks and would show). Candidates: the mod's
  stereo path or billboard substitution dropping its draws; the first
  person system's `is_in_first_person_mode` answer under the mod
  driving a visibility state the stock game only uses in third person;
  something in the mod's unit visibility handling (the body proxy sets
  `Unit.set_unit_visibility` on the player's units and attachments).
  First checks: is it visible with "Third-person body in the hub" on;
  does it show on the desktop mirror while invisible in the headset
  (then it is the stereo path). On the branch: chat command
  `/dtvr_companion` logs each companion unit's alive state, distance
  from the player, mesh count and visible meshes, the player visibility
  extension's answer and the first-person mode (`DARKTIDEVR_COMPANION`
  lines, also echoed to chat). Run it in the hub and in a mission with
  the skull missing and report the lines. First run (hub, 12
  September): alive, 2.1 m away, one mesh, player visible, first person
  false; the mesh-visibility probe was not an engine API and said
  nothing. The user reports the skull missing in the hub in both first
  and third person. Added: `/dtvr_companion show` forces the unit and
  its "main" group visible and fires `lua_visible` (if it then appears,
  something hid it), and `/dtvr_companion trace` wraps the engine's
  unit visibility calls and logs every call on your companions with a
  traceback (`DARKTIDEVR_COMPANION trace`), so arm it before the skull
  spawns (before entering the hub or a mission) to catch the hider.
  **Cause found (12 September, from the user's candle observation: the
  cosmetic's candle flames floated where the skull should be, other
  players' skulls fine):** the companion's cosmetic body and skin
  colour are items in the *owner's* equipment
  (`slot_companion_gear_full`, `slot_companion_body_skin_color`,
  equipped by `CompanionVisualLoadoutExtension` through the owner's
  visual loadout and attached to the companion unit). The mod's body
  pass walks the local player's equipment and hides every 3P slot unit
  that is not the arms or the wielded weapon, so it hid the skull's
  body; the flames are particle effects, and remote owners' skulls never
  pass through the local equipment. Fixed on the branch: those two slots
  are left visible (`presentation.companion_gear_slots`). Worn check:
  the skull in the hub and a mission, with and without a cosmetic.
- [ ] **Alpha.2: cutscenes cannot be skipped in VR** (user report, 12
  September). Done on the branch: `CutsceneView.update` and
  `VideoView.update` are hooked; the right trigger's rising edge arms
  the stock press (`on_skip_pressed`, or the video view's flag and its
  legend) and while it stays down the input service answers
  `skip_cinematic_hold`, so the stock half-second hold and skip run
  unchanged; the legend badge reads "Hold RT" (prompt aliases, test).
  Log `DARKTIDEVR_CINEMATIC skip_pressed`. Worn check pending on the
  mission intro and the prologue video.
- [ ] **Alpha.2: the player appears twice in cutscenes** (user report,
  12 September): the cutscene's own character and the real body. The
  stock cutscene system hides the real players through the player
  visibility extension (snapshot, hide, restore), and the mod's body
  visibility pass forced the 3P body visible every 60 frames regardless.
  Done on the branch: while that extension reports the player hidden,
  the pass keeps the body hidden and changes nothing else; the stock
  restore at the cutscene's end and the next pass bring it back (log
  `DARKTIDEVR_BODY stock_hidden=`). Worn check pending on a mission
  intro.
- [x] **Alpha.2: the left menu button no longer acts as back in menus**
  (user request, 12 September: Virtual Desktop needs a double tap of it
  to switch between the VR and desktop views, and the first tap was
  closing the menu). Back is the right secondary button (B by default)
  only; the viewer's two back paths and the "B / Menu" prompt label
  changed. The menu button still opens and closes the game menu.
- [ ] **Alpha.2: slightly too tall as a new Skitarii character** (user
  report, 12 September). The user adjusted the height slider while
  creating the character and wonders whether that changed the
  calibrated height, or whether the class is taller by default. What
  the code does: calibration writes the official `character_height`
  only when a calibration is saved (`apply_official_character_height`,
  barber flow); a character created afterwards keeps its slider value.
  For humans the mod places the eyes at the calibrated floor eye height
  (`calibrated_character_scale` returns world scale 1 and the visual
  body scale from the calibration), so the profile height should not
  move the eyes; the stock first-person height and mover do follow the
  profile. Candidates: the Skitarii breed has its own
  `breed.heights.default`, so the visual scale (calibrated eye height
  over authored height) and the stock first-person height disagree
  differently from the other humans; or the eye anchor is taken
  relative to the stock first-person position, which the taller profile
  raises. First checks: the `DARKTIDEVR_CALIBRATION` lines for that
  character (breed, authored height, profile scale, requested scale)
  and a rerun of the calibration on the new character (if that fixes
  it, apply the calibrated height automatically when a character is
  created or first selected, and say so in the guide). The Skitarii
  (adamant) archetype uses the human breed, so the class is not taller
  by default; the slider is the difference. Done on the branch: when
  the character-select screen selects a profile
  (`MainMenuView._event_selected_profile_changed`), its official height
  is brought to the calibrated one through the same profile service
  call the calibration uses, when it differs (log
  `DARKTIDEVR_CALIBRATION profile_height reconcile`). Worn check: select
  the Skitarii, read the log line, play and compare.
- [x] **Alpha.2: the chat window (and the other constant elements) never
  reach the HUD panel** (user report, 12 September: chat commands typed
  blind). The HUD panel renders the HUD's fixed elements into its own
  target; constant elements (chat, notification feed, mission buffs,
  voice chat, onboarding tutorial popups, group finder and havoc status)
  draw through the UI manager's own overlay renderer, which the headset
  never shows. **Tried and reverted:** drawing those elements a second
  time with the panel renderer crashed the engine on the renderer
  thread (`E_INVALIDARG, assert: shader`) the moment matchmaking's
  notification appeared: stock widget materials and retained records
  belong to the renderer that created them. The route has to keep the
  elements on their own renderer and bring the *image* across (capture
  the constant-element pass into a target and show it on the panel, as
  the HUD's fixed elements are), or re-create the widgets for the panel
  renderer. Not done; chat stays desktop-only for now.
  13 September, on the branch (worn pending): mirrored instead. While the
  listed elements draw (chat, notification feed, mission buffs, voice
  chat, group finder and havoc status), the marker router's `mirror`
  runs each stock draw call as normal and then again on the panel's
  resource renderer, with per-pass material instances re-created on the
  panel GUI and their recorded values replayed (the marker atlas route,
  which has not crashed). Retained draws are not repeated. Each element
  moves by its root node's alignment from the UI lookup (2112x2304 in
  stereo) to the panel target (2112x1188), so bottom-left chat lands on
  the target's bottom edge. Log: `DARKTIDEVR_HUD
  constant_elements_mirrored`, `mirror_draw_failed`.
  Same day: the Custom HUD editor listed chat but drew no box (it places
  boxes at the node's live world position, below the 1188-px target).
  The mirrored elements' screen-anchored nodes are now placed in panel
  coordinates (left/top) like the status nodes, recorded in
  `layout_nodes` and restored with the panel; nodes with a Custom HUD
  override are left alone. Log: `DARKTIDEVR_HUD constant_node_placed`.
  Worn, 13 September: the user is happy with the placement.
- [x] **Alpha.3: audit for menus a VR player cannot leave** (13
  September, after the end screen's unbound continue; static, against the
  1.12 source, not worn). Method: every `input_service:get` in views,
  view elements, constant elements and the UI manager; every input legend
  entry without a press callback; the stock view registry against the
  pointer allow-list in `darktidevr_menu_input.lua`. Result: every
  registered interactive view is on the allow-list (only `blank_view` and
  `scanner_display_view` are not, and neither waits for input); legend
  entries without callbacks are hints or keyboard-only extras (rotate,
  name entry, DLC purchase, debrief video); progress actions checked
  gamepad-path `confirm_pressed` with a clickable button beside it, or
  close on B (marks vendor result, `back`). Flat screens without the
  pointer: splash advances and closes on its own timer, mission intro and
  loading take no input, title (either trigger), video and cutscene (hold
  RT), end screen (hold RT, fixed 646bbbb). Remaining, none of them a dead
  end: the survival-mode buff choice overlay
  (`ConstantElementMissionBuffs`, drawn on the non-interactive HUD panel)
  cannot be answered and picks at random when its 20-30 s timer ends;
  HUD votes (kick, mission flow) read Unbound and time out; the character
  name needs a keyboard, Randomize works.
- [ ] **Alpha.3 (worn pending): the survival-mode buff choice in VR.**
  While `ConstantElementMissionBuffs` holds a live choice (`_using_input`,
  `is_choice`, no `buff_chosen`), presentation classifies it like the
  confirmation popups (mode 5, `mission_buffs:choice`): the interactive
  panel with the pointer, whose cursor path holds left (RT) on a card.
- [ ] **Alpha.3 (worn: RT continues past the score screen; Y vote not yet checked): menu hotkeys on controller buttons**
  (user direction, 13 September: special menus get fixed buttons for their
  keyboard defaults, overlap with gameplay binds does not matter there).
  `MenuInput.menu_buttons` keyed by key alias: hotkey_menu_special_1 (E) Y,
  hotkey_menu_special_2 (Q) X, continue_end_view RT (a plain press, per the
  user: the half-second hold did not work), social_show_list (Space) A; the
  menu proxy answers each action's pressed, held or released type from the
  button's edges (release required after a menu takes over; buttons alone
  make the proxy). Prompts label the same buttons. Replaces the end screen's
  held-RT and point-to-vote handling from 646bbbb.
- [ ] **Alpha.3 (worn pending): hold RT did not skip cutscenes or videos**
  (worn, 13 September): the skip read `controller_observation.right_trigger`,
  which only stereo gameplay refreshes; flat screens never saw the trigger.
  It now reads the controller state directly, as the title screen does.
- [x] **Alpha.3 (worn fixed, 13 September: scanner comes out, cycle good): the scanner could not be brought out on a
  shared button** (worn, 13 September, 07:49-07:51 log): with Y selecting
  both the device and the carried-item cycle, every press delivered
  `wield_5,wield_3_gamepad` together and the game took the cycle. User
  direction: the device is the cycle's first item. With a device equipped,
  a press carrying the cycle steps device -> pocketable -> small pocketable
  -> device, skipping empty slots, and from outside the cycle brings out the
  device; the press keeps only the selector that reaches that slot.
  Generalised after a clash sweep (the game applies only one of several wield
  inputs in one press, not a chosen one): any press selecting several wield
  targets (quick wield, pocketable, stim, device, item cycle) is a cycle over
  exactly those targets, device, pocketable, small pocketable, then the
  weapon when quick wield is on the button; empty slots skipped; one direct
  selector delivered (`Bindings.wield_press`). Single selectors and the
  device-less item cycle keep stock behaviour. Sweep notes: defaults clash
  only on Y; right stick down (interact/reload) and A (jump/dodge) are
  intentional; any wield input while carrying an object drops it (stock).
  The user's saved profile had the item cycle, pocketable and stim unbound
  (unbound to get the scanner out), leaving carried items unreachable; they
  rebind the cycle on the next launch. The same report's
  "scanner does nothing": the log shows plain RT (`action_push`, the
  scanner's shove); the scan is LT held (`scan_start`) then RT
  (`scan_confirm`), and LT was never pressed. Minigames on the scanner take
  RT, A or interact to press and the left stick for the knob; LT cancels.
- [x] **Alpha.3 (worn fixed, 13 September: no crash at mission end): crash leaving a survival mission**
  (13 September, 06:17:06 log: engine crash in `package_manager` unload,
  "Trying to unload resource ... used elsewhere by the engine, refcount 2",
  resource type `eac0b497876adedf`, during the outro video and hub load).
  Leading cause: material instances the marker atlas (and the HUD panel's
  mirror) create from widgets, some from the mission's own packages, were
  released only after the unload (the atlas at the next world's stereo
  setup). Both are now released at the first mode heartbeat in mode 2
  (loading, video, cutscene; 0.5 s in, world still alive).
  Crashed again at the end of a standard mission (dm_stockpile, 07:01:16,
  texture `70d971d9631ae9f2`, 0.4 s after GameplayStateRun -> StateGameScore):
  the end-of-mission cutscene is stereo, so mode 2 never came and nothing
  was released. Resource names are Stingray murmur64 hashes (verified:
  `texture` cd4238c6a0c69e32, `material` eac0b497876adedf); the 06:17
  material is `content/ui/materials/dividers/faded_line_left_01`, used
  only by the smart tagging and emote wheels, whose tag prompt draws
  through the atlas (mission-only HUD package). The texture is not named
  in any script (140k candidates). Now the atlas releases itself after 30
  camera updates without a marker frame (markers stop before every
  unload), and entering StateGameScore or StateLoading releases the atlas
  and mirror materials as a backstop. The published alpha.2 carries the
  atlas and very likely this crash at mission end. (The 05:59 session's
  06:00:21 access violation followed "Window signals application exit".)
- [ ] **Alpha.3: scan hologram floating away from the scanner** (worn, 13
  September: the spheres shown while LT is held are in world space and float
  in the air instead of above the device). `AuspexScanningEffects.
  update_unit_position` places holo units (`auspex_scanner_holo_drop_01`,
  `auspex_scanner_holo_ball_01`) from `active_unit` node 2, the 1P item when
  `first_person_extension:is_in_first_person_mode()` and camera-follow, else
  the 3P item. A hook forcing the 3P branch (dded6e1) was a no-op and is
  removed: the mod's `_update_first_person_mode` hook already returns
  `show_1p_equipment=false` in VR body modes and `is_in_first_person_mode()`
  returns that flag, so the effect already used the 3P scanner. Leading
  hypothesis, unverified: the effect samples the 3P scanner's animation pose
  during the visual loadout update, before the body proxy moves arms and held
  item to the tracked hand at pre-render. Holograms only appear in a mission
  scanning zone. Next: trace the 3P item node-2 position at effect time
  against the tracked grip, then re-anchor the holo units after the hand
  pose if confirmed.
  Static confirmation (13 September): `PlayerUnitLocomotionExtension.
  post_update` calls visual loadout `update_unit_position` (hologram placed
  from the animated source attach node), then returns into the mod's
  `post_update` hook, where `apply_body_ik` syncs the source hand/attach nodes
  to the tracked hand. Nothing moves the item later. Every frame, then, not
  only while the scanner is brought out. Fix (`darktidevr_scanner_holo.lua`):
  after IK, call the wielded `AuspexScanningEffects:update_unit_position`
  again with dt 0 and the same t; its smoothing, sound, light, outline and
  highlight state are unchanged at dt 0, so only hologram poses move. The
  `DARKTIDEVR_SCANNER_HOLO scan=start` line logs how far the stock placement
  missed (`moved_m`) and the new distance to scanner node 2 (expect about
  0.084 m). Psykhanium check `/dtvr_scan_test`: brings out the auspex and
  answers `any_active_scanning_zone` true (the scan action otherwise puts the
  scanner away at once with `no_mission_zone`); shows the player marker only,
  since target spheres need real scannables. Cleared on loading. Not yet worn.
  First worn try (13 September 10:02): the check switched on but the scanner
  never came out; the next LT delivered action_block on slot_primary. A wield
  written directly from the command did not take. The check now only hands
  out the auspex and the zone; the player brings it out with their own
  device button.
  Second try (10:15): Y delivered `wield_5` but nothing came out. The item
  was `content/items/devices/auspex_scanner` (from the F7 display check),
  whose template `auspex_scanner` sets `not_player_wieldable = true`, so
  `PlayerUnitWeaponExtension.can_wield` refused it. The scan device uses
  template `scanner_equip`; the check now picks the catalogue item with that
  template (logged as `test_item=`) and replaces any other device.
  Same pattern may apply to other `update_unit_position` slot scripts
  (flamer gas, chain lightning hand/link, aim projectile arc, wind slash);
  not re-run, since they are not checked for dt-0 idempotence.
- [x] **Alpha.3: weapon effect script audit** (13 September, static, 69
  scripts plus `utilities/material_fx.lua`). The audit listed ~40 scripts that
  place particles, spawn units or change visibility from the 1P unit or 1P
  mode, largely via `PlayerUnitVisualLoadoutExtension._register_fx_sources`
  and `PlayerUnitFxExtension._spawn_unit_particles`. Its premise that VR
  reports first person does not hold: in VR body modes equipment mode is
  third person (above), so sources register on 3P units, 3P/husk variants are
  chosen and flow events reach 3P units. Residual class, same as the
  hologram: effects sampled once at a 3P node position rather than linked to
  it (e.g. `weapon_shout_effects` one-shot burst, `missile_launcher_effects`
  discarded prop, `riot_shield_effects` activation burst, flamer stream start)
  would use the animation pose, not the tracked hand. Linked particles follow
  the node at render. Not observed worn yet.
- [x] **Alpha.3: Skitarii blitz crosshair shift** (worn, 13 September:
  "the skull and melee have the same crosshair, the gun is offset").
  Trace (DARKTIDEVR_RETICLE_TRACE, removed afterwards): switching maul to
  galvanic rifle with the controller still dropped the published aim pitch by
  9.3-9.9 degrees and back. Cause: the "Gun pitch angle" option
  (`vr_gun_pitch`, default -10) tilts gun aim, gun and crosshair relative to
  the controller for guns only (`gun_aim.base_aim` via `weapon_aim_target`);
  melee and the skull order aim along the controller. Fix (user decision:
  one pitch across the board, -10): `base_aim` pitches every wielded item and
  `weapon_aim_target` applies it to the dominant hand on either side (the
  left-handed single-hand path had no pitch before). Option renamed "Aim pitch
  angle"; the setting id is unchanged so saved values carry over. Gun model
  tilt is still guns only. Not yet checked worn.
- [ ] **Alpha.3: Skitarii flamethrower skull order has no ground preview**
  (worn, 13 September). Stock path: the order aim (`action_aim`, kind
  `servo_skull_order_target_finder`, `MultiTargetingActionModule`) casts a
  ballistic ray from `first_person_component` and writes
  `action_module_position_finder.position` (+2 m) and `position_valid`; on
  the server it starts effect template
  `companion_servo_skull_aim_on_ground_effect`, which creates the particle
  `cryptic_servoskull_flamer_cone_decal` / `_circle` there with
  `World.create_particles` while `position_valid` and `can_aim_on_ground`
  (grenade action `action_aim`, enough charges, skull following) hold. No mod
  code touches this path. Diagnostic `DARKTIDEVR_SKULL_PREVIEW`
  (`darktidevr_skull_preview_trace.lua`, bounded, logs on change) separates
  never started / position invalid / particle created but not visible.
  Worn trace (10:14): template started, `position_valid=true`,
  `particle=true` for both cone and circle, 8-9 m ahead. Created, not seen.
  The user's `user_settings.config` had `decals_enabled = false` (from the
  early dual-render performance baseline); both previews are decal
  particles. Enabled decals at the user's request (backup
  `user_settings.config.before-decals.bak`). Awaiting worn confirmation;
  remove the trace afterwards. If confirmed, the user guide should note the
  skull preview needs Decals on.
- [ ] **Game crashes on shutdown** (every close on 13 September, 04:58 to
  10:04): `shutdown = true`, access violation reading 0x0 at
  00007FF6620E13E6 after GameplayStateRun exit; also logs leftover
  `event_darktidevr_calibration_sample` callbacks. Pre-dates alpha.3 work.
- [ ] **Post-alpha.2: chat on the HUD panel is very small and still has
  no box in the Custom HUD editor** (worn, 13 September, after
  1099d1f). Shipped as a known limit in alpha.2. Leads: the mirrored
  draw keeps the constant elements' own scale while the panel shows a
  1920x1080-unit HUD at its own size and distance; and the editor builds
  its node boxes when it opens, so the check is whether the placed node's
  world position has been recomputed by then and whether chat's nodes
  pass the editor's allowed-node filter.
  13 September, alpha.3 branch (worn pending). Logs of the worn sessions:
  in stereo the UI lookup is 2112x2304 at scale 1.1 and the panel target
  2112x1188, while the panel draws fixed HUD elements at scale x
  `object_scale` (2.08). The mirrored constant elements drew at their own
  scale (half the HUD's size), and their nodes were placed in lookup units
  at that scale, while the editor draws boxes at world position x scale x
  object scale: chat's box landed below the target (only voice chat ever
  logged a placement). Now the mirror copy draws `object_scale` larger
  (pixel calls scale position, size and font size; logical calls through
  the target's scale; a recorded `ui_scale` is scaled), and the nodes are
  placed in the panel canvas at that mapping (same margin from the same
  edge, clamped inside). Stock chat (500x250 units, 490 above the bottom)
  lands top-left. Log: `constant_node_placed ... canvas=` (first eight).
- [x] **Alpha.2: HUD panel toggle** (user request, 12 September): mod
  option "Show the HUD panel" in the HUD group; off hides the panel's
  world quad only, the HUD keeps rendering so turning it back on is
  immediate. World markers and the crosshair are unaffected.
- [ ] **Post-alpha: proper two-hand support.** Today's
  `darktidevr_two_hand_support.lua` is a session-only prototype behind
  `/dtvr_two_hand_calibrate`, `/dtvr_two_hand_on` and `/dtvr_two_hand_off`:
  no shipped socket profiles, nothing saved, no menu option. Needed: saved
  per-weapon profiles (or defaults by weapon family) with an in-game
  capture flow, a mod option with acquire/release radii, the stock anchor
  tuned, ADS hold policy, left-dominant coverage, and a worn pass in the
  Psykhanium and a mission.
- [ ] **Post-alpha: broad haptics pass.** The viewer creates a right-hand
  haptic action but nothing pulses it. Design and add feedback for firing,
  melee contact, block, ADS entry, damage taken, interaction and menu
  pointer hover, with per-event strength options.
- [x] **Rebuild the release package** from the final commit
  (record-component-provenance, then build-runtime-package
  -ReleaseVersion 0.1.0-alpha.1) and run the flat/VR round trip through
  the batch file on the extracted package. Done from `bda5d37` (12
  September): `darktidevr-0.1.0-alpha.1-bda5d379fa9c.zip`, verifier pass,
  batch flat/VR/status round trip pass. Rebuilt again from `06b662c`
  after the pointer swapchain change:
  `darktidevr-0.1.0-alpha.1-06b662c2b662.zip`, verifier pass, installed.
  Rebuilt from the rename commit and again from the switch-fix commit;
  the handover carries the current archive identity. Repeat if the worn
  checks above change code.
- [x] **End-of-training popup pointer (12 September afternoon):** no
  laser or cursor at all. The pointer quads sampled a corner texel of the
  menu texture that the canvas blit left transparent under a popup. The
  pointer now has its own static swapchain in a fixed dark green. Worn
  pending; rebuild the package (viewer changed).
- [ ] **Worn: medicrate prompt badge and the tag-while-aiming-at-nothing
  location marker** through the tutorial's marker step (both deployed
  after the 12:46 start).
- [x] **Mod id renamed to `darktidevr`** (12 September, late afternoon)
  with the user's settings migrated on disk; worn check of the renamed
  install pending (settings, bindings and options must carry over).
- [x] **Mode switch refused an earlier build of its own proxy** (found
  installing the renamed package): identification by marker now, with
  `outdated` replaced or removed; `mode_switch` test added.
- [x] **Two Lua crashes guarded (12 September evening):** nil player
  viewports during a scene transition in the render hook (second entry
  into the Psykhanium), and the onboarding handler's destroy-time sync
  after the UI manager dropped its view handler. Worn on the 4efe666
  package: second Psykhanium entry and a normal exit, no crash; neither
  guard fired in that run.
- [ ] **Worn on the full-mission fixes (12 September evening):** device
  screen (F7 in the Psykhanium, then a real scan and generator), view
  height after a rescue, third-person spectating, prompts in the voting
  notifications and end-of-round tabs, return to the hub after a mission
  without the eye-target crash. Check the log for
  `DARKTIDEVR_SCANNER` and `DARKTIDEVR_PROMPTS view_alias=` lines.
- [x] F7 equips the auspex in the Psykhanium and its screen shows the
  idle display (worn, 12 September). A real scan or generator in a
  mission remains the definitive device-screen check.
- [x] Half-length laser (worn, 12 September). Loading boards at their
  true shape (worn, 12 September evening).
- [x] **Alpha.2 (worn fixed, 13 September): the pointer sprite
  has its own upload buffer**, so the window-capture memcpy can no
  longer overwrite it before the copy; the target should now be the
  cyan ring and cross and the laser dark green. See the item below for
  the cause.
- [x] **Fixed in alpha.2: the pointer's hit marker rendered as a dark square
  and the laser can look black** (worn on 2993ea3, 12 September
  evening; shipped as a known limit). Cause, from the code: the pointer
  sprite and colour block are staged in the corner of the shared flat
  upload buffer and copied into the static pointer swapchain by the
  frame's command list, but later in the same frame the window-capture
  branch memcpys the whole newest capture into that upload buffer on the
  CPU (`consumed_capture.reset()` forces it), before the command list
  executes. The GPU copy therefore takes the capture's bottom-right
  pixels (dark title-screen texels, opaque) instead of the sprite. The
  16x16 green block only worked while no capture had arrived by the
  first frame. Fix: give the pointer sprite its own small upload
  resource (64x64 BGRA, own footprint) and copy from that, or record
  the pointer copy after the capture memcpy; then the target is the
  cyan ring and cross from `paint_pointer_target` and the laser its dark
  green. Worn check afterwards on a popup, an in-game menu and the
  title screen.
- [ ] **Worn: the `confirm` and tab prompts reading Point + RT.**
- [ ] Stock keyboard and mouse did not answer a popup while the mod
  presented a mode-2 (loading or cutscene) panel, even with the window
  focused. Popups now take the interactive panel; find why mode 2
  blocked stock input, since loading boards still rely on it.
- [ ] Votes (kick, mission flow) cannot be answered in VR: the
  notifications read Unbound. Route them (a hold on a spare control, or
  the pointer) after the alpha.
- [ ] Cosmetic: the executable patch step prints a repository-relative
  `restore.command`; in the package the batch's flat choice is the restore.
  Reword when the package is next rebuilt.
- [ ] Remote-mission follow-ups: per-action-family coverage (throws,
  interactions, downing, rescue, spectating, extraction, reconnect), the
  traversal-aim policy, and rubber-banding observation over longer play; see
  [MISSION-READINESS.md](../MISSION-READINESS.md). The build from this branch (`BF8ED0A1`) is
  compile/WARP-verified only because this lineage still fails FG offline.
  The earlier failed physical control stays excluded from GPU/VD comparisons;
  see [physical failure evidence](../PHYSICAL-STREAMING-GAP-2026-09-11.md).

- [ ] Test gameplay desktop mirror suppression separately from viewer window
  capture, then vary desktop resolution and test window lifetime dependencies.
  The user confirmed the game mirror was running even with a black simulator
  preview. See [controls and source audit](../DESKTOP-MIRROR-PERFORMANCE-CONTRACT.md).
  Native mirror-copy A/B/A is complete: 89.09/89.52/89.06 FPS (about 0.5%).
  Counters confirm suppression; FG and resize/window lifetime comparisons
  remain open. Keep default mirror behavior.
  The [viewer capture comparison](../SIMULATOR-WINDOW-CAPTURE-2026-09-11.md)
  is complete in the simulator: same-binary always/on-demand/off delivered
  89.33/91.65/91.57 FPS, and it exposed a capture-worker shutdown hang that
  is now fixed with a test. Physical comparison under VD streaming is still open.

- [x] Use **2496x2688 per eye**, the current VDXR recommendation, for the next
  synthetic batch. This is about 38% more pixels than the earlier 2112x2304
  workload; use the exact sizes for comparisons rather than treating the runs
  as equivalent.
- [ ] Prefer `-VdxrReadinessPath <fresh-ready.json>` after the physical session
  is closed. If the headset is unavailable, use explicit `-EyeWidth 2496
  -EyeHeight 2688`, labelled as the saved higher-resolution control. Refresh
  the receipt after VD resolution changes. Never launch a competing XR session.
- [x] Establish repeated DLSS Quality baselines with FG On/Off, matched scene,
  native/viewer versions, refresh, cap and precise-polling settings. Record
  original, generated, distinct and repeated frames separately; exclude loading
  and background/alt-tab intervals. Keep 120 FPS (60 native plus FG) the target.
  The 11 September mission controls recovered approximately 116 distinct FPS
  with FG and 77 native FPS once VD desktop encoding was idle. Record the
  [background GPU activity](../BENCHMARK-GPU-ENGINE-ACTIVITY.md) in future
  comparisons; this recovery is benchmark hygiene, not a mod speedup.
- [x] Match the accepted stereo HUD panel in both physical and synthetic tests.
  Physical launches require `-EnableHudPanel`; its omission caused the flat HUD
  in the evening comparison and was corrected live. The simulator now enables
  it with restoration; the user confirmed both-eye preview and world-space HUD.
  See [launch parity](../LAUNCH-PARITY-2026-09-10.md). Keep debug-layer and preview
  settings matched, and obtain uninterrupted controls before assigning the gap.
- [ ] Optimise stereo rendering, DLSS/FG and engine costs using the new baseline.
  Keep each candidate isolated and compare it against the same-resolution control.
  The [descriptor and native-ring candidate](../NATIVE-ORIGINAL-RING.md) now
  delivers 89.57/89.34 native FPS versus a fresh 76.47 control. Follow up with
  physical/worn checks before promotion. DLSS Performance delivered 95.80 FPS,
  about 7% above Quality. A post-ring four-worker control delivered 88.14 FPS
  versus about 89.5 with the existing configuration; retain the existing setting.
  The [combined small-change trial](../STACKED-PERFORMANCE-2026-09-11.md) supports
  about 1% locally across two combined and two nearby baseline runs. Retain its
  focused candidate; do not claim a large additive gain or physical acceptance.
- [ ] Establish controlled full-streaming baseline/candidate performance,
  including VDXR and VR encoding. Idle-simulator results omit that cost. VD
  desktop encoding activated during one stacked trial while the user was away;
  investigate readiness/runtime automation as a possible trigger. Exclude that
  confounded run rather than assigning its FPS change to the optimisation stack.
- [ ] Separate hub versus mission scene cost from resolution and runtime overhead.
  The live hub showed approximately 40 original + 40 generated FPS; attribution
  is still open. Use meaningful performance workloads, not basic gameplay rechecks.
  Updated [hub controls](../HUB-PERFORMANCE-2026-09-11.md) deliver 92.58 native
  and 117.35 distinct FG FPS at the saved resolution with idle desktop streaming.
  The physical ~40/~80 result is not reproduced; retain the physical comparison.
- [ ] Keep 144 Hz secondary. Preserve accepted files and settings, validate
  changes, and record rollback and any required worn acceptance.
  Matched 120/144 Hz simulator FG controls both deliver about 120 distinct FPS;
  144 Hz adds about 23 repeats/sec. No 144-distinct or physical-headset claim.

HUD-surrounding blur, pickup asymmetry and other unresolved visual work remain
open. Physical melee remains paused. See the [session handover](../handoffs/2026-09-11.md).
