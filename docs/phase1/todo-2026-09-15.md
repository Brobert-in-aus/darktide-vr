# Next session: 15 September 2026

Branch `codex/alpha-4-2026-09-14` (not yet merged or published). Today's work
and evidence: [session handover](../handoffs/2026-09-14-unattended-session.md).
The backlog sections of [todo-2026-09-14.md](todo-2026-09-14.md) still apply;
this file holds the new items from the 14 September evening worn test, first.

## State at the end of 14 September

- Installed game: branch head Lua, viewer and capture library. The two-hand
  reload and bash fix (below) is committed and tested but was not deployed or
  worn.
- VR mode, vr settings profile. The PC hard-crashed at 20:43; the settings
  file was lost and restored from `%LOCALAPPDATA%\DarktideVR\user_settings.vr.config`
  (07:23 profile). The reset file is kept beside it as
  `user_settings.after-crash-reset-20260914.config`.
- Worn screenshots: `artifacts/worn/2026-09-14-evening/` (ignored by Git).

## From the worn test (14 September evening, in order)

- [ ] **PC hard crash loading the hub** (20:43, first launch of the evening).
  Evidence:
  - Windows bugcheck `0x154 UNEXPECTED_STORE_EXCEPTION`, and the dump write
    failed.
  - No NVIDIA driver reset was logged at the time.
  - Game log stops partway through the hub load; the viewer was healthy at
    116 fps on the loading board until 20:43:32.
  - The same build loaded the hub and the Psykhanium cleanly on the next
    launch.
  - The PC also had unexplained resets on 2, 5, 11 and 17 August, and NVIDIA
    driver resets from late August to 5 September.
  - Lean: system instability (memory at 6000 MT/s EXPO, or storage) under the
    VR plus hub load, not a mod fault; not proven.

  Next:
  - If it recurs, the user sets the page file to system-managed so a dump
    survives.
  - Optional control: flat mode to the hub.
  - No system changes without the user.
- [x] **Settings safeguard.** (`cd132ea`: backup at each launch, restore-settings
  option 4; `run26` saved a backup; evening item 1.) The crash destroyed `user_settings.config`
  mid-write, so the launcher ran as a first run and the mod's calibration
  opened. Add a copy of the last good settings file:
  - taken at each successful VR launch (mode switch or native module
    start-up), in `%LOCALAPPDATA%\DarktideVR`;
  - with a documented restore step, and possibly a restore offer when the
    game starts from defaults while a backup exists.
- [x] **Ammo count at the hand: clip and reserve overlap** (`7efa0ef`..`0da004b`:
  clip, dash bar, reserve stacked; calibrated in eye renders run1-run5; evening
  item 3.), and the user wants
  a `—` between them.
  - Screenshot `shot-205314.jpg` shows the clip `6` descending into the
    reserve `49`. The glyph metrics in `darktidevr_ammo_readout.lua`
    (primary at `size * 0.727`, reserve at 0) are too tight.
  - Plan: stack the clip, an em dash (a short bar, in case the font lacks the
    glyph) and the reserve as three blocks with explicit gaps.
  - Verify with the readout test flag (`front`) in an eye readback before
    deploying. The reload ring was accepted as it is.
- [x] **Crosshair slightly misaligned with the iron sights** (`96b13ed`, evening
  item 6: the sight line was 11.8 cm above the aim ray; the drawn gun is now
  zeroed on the reticle, presentation only; see the handover) (galvanic
  rifle; the reticle sits just off the rear sight notch in both screenshots).
  - Compare the reticle ray with the muzzle and sight line after gun aim,
    and check whether the offset is a fixed sight height (sight-to-muzzle
    parallax) or a rotation.
  - The aim evidence log prints `aim_angle_deg=0.0000`, so the muzzle already
    matches the aim; the sight line may differ from the muzzle line.
- [x] **Stray bullet sticking out of the player's right hand.** (`abc400b`,
  evening item 5.) It is the receiver's parked round column (meshes 27-58
  and 63-66 of `receiver_01`), normally inside the stock wrist. It is now
  hidden outside reloads while the arms are hidden. Found by elimination in
  eye renders; see the handover.
  - A cartridge shows below the right glove in both screenshots.
  - It disappears during the reload animation (user), so it is probably a
    reload prop or ammunition attachment on a hand or weapon node, placed
    for the stock pose and left where the tracked glove no longer covers it.
  - Find the unit or attachment: the galvanic rifle's item attachments and
    the first- and third-person hand nodes. Hide or re-place it outside the
    reload.
- [x] **Two-handing: reload and bash no longer break the grip** (deployed 15
  September; `run26` holds with a reload ended `released`; evening item 2).
  - The allow-list gained reload, shotgun reload, charge ammo, load special,
    toggle special, vent, overload charge, charge, flamer gas, sweep, push
    and windup.
  - Inspect, draw and holster still end the grip.
  - Worn check: reload and bash while holding the foregrip.
  - The evening log showed every hold ending `cancelled` (reloads), with
    steering up to 92 degrees.
- [ ] **Two-handing: moving the right hand feels off** (the left hand feels
  good). Research industry best practice for two-handed weapons and virtual
  stocks before changing anything, then write the design into a dated
  document.
  - **Research and design done (15 September):**
    [two-hand-aim-design-2026-09-15.md](two-hand-aim-design-2026-09-15.md).
  - **Cause found offline:** the support correction is smoothed in
    controller-local space. A 10° wrist turn swings the barrel 8.5° off the
    left hand, then it drifts back over about 0.2 s.
  - **Done 15 September:** the "hands line" filter option (`9b1e549`),
    the shared body frame, and the virtual stock option (`1222cc6`), all
    default off.
  - **Next:** the worn A/B and stock tuning (evening items 4 and 7).
  - **Correction:** the note below is wrong. The dominant rotation is not
    ignored; the aim is a minimal swing on top of it.
  - Games: Pavlov, Onward, H3VR, Contractors, Boneworks, Half-Life: Alyx.
    Also developer write-ups, such as the VR aiming talks and
    stock-emulation articles.
  - Questions: which hand is the pivot; whether the primary hand's rotation
    or the line between the hands wins; smoothing on each hand; virtual stock
    anchoring (shoulder or cheek position, radius, strength); snapping and
    offset handling; and how the aim is kept stable when the dominant hand
    moves.
  - Current behaviour: the aim follows the line from the dominant grip to
    the support socket, filtered, with the primary hand's position fixed. A
    right-hand translation therefore rotates the gun about the left hand's
    socket, while the right hand's rotation is ignored.
- [ ] **Full-body IK: research and a design for implementing it** (user, 14
  September evening). Investigate the same way as two-handing (industry
  practice first), then write a dated design document for implementation.
  - **Research and design done (15 September):**
    [full-body-ik-design-2026-09-15.md](full-body-ik-design-2026-09-15.md).
  - **Design choices:**
    - a full-profile proxy posed by the mod;
    - legs copied from the stock avatar;
    - a shared body frame owning the shoulder for both the stock and the
      arms;
    - head slots and near-eye meshes hidden in first person.
  - **Progress (15 September afternoon):**
    - milestone 1 done: body frame module, and the rig scan of the human
      and Ogryn rigs;
    - milestone 2 checked with the dev-flag body mirror: the full profile
      spawns with its cosmetics and copies every joint;
    - the overlay on the player with a two-bone arm solve puts the hands on
      the weapon hands.

    See the design's milestone sections.
    - looking down (overlay14): readable arms, gloves and gun, with one
      pair of hands. Hooded torsos need the neck placed behind the eye.
  - **Next:**
    - a worn look at `overlay` and `overlayscale` (flag file
      `darktidevr_body_mirror.flag`), once the user wants to;
    - milestone 3: body frame shoulders, clavicles, spine and neck on the
      overlay;
    - then move it from the dev flag into the experimental full-body mode.

  The design must cover how the existing body works today, so the stock
  third-person model can be enabled with the mod's own IK and players see
  their character and cosmetics:
  - tracked rigid hands (the default);
  - the experimental upper-body proxy (`darktidevr_body_proxy.lua`,
    `full_body_experimental_enabled`, `inherit_authoritative_pose`), and the
    hub's third-person body;
  - how the first-person and third-person rigs, the gameplay avatar and the
    equipment attachments relate (the equipment sync that overwrites the
    third-person hands, and the untouched first-person rig);
  - what other players see.

  Build on the existing
  [whole-body IK design](whole-body-ik-design-2026-09-14.md) and the
  30 Hz pose trace recorder. Research: body IK in shipped VR games and
  middleware (Boneworks and Bonelab, Half-Life: Alyx hands without body,
  Blade & Sorcery, Contractors, Pavlov; FinalIK VRIK, Unreal's IK Retargeter
  and Control Rig, and IK papers on head and hands to full body), covering:
  - pelvis and torso estimation from head and hands;
  - elbow and knee poles;
  - leg locomotion and foot planting;
  - calibration and arm span;
  - comfort, meaning never moving the camera from the body and how
    first-person occlusion is handled;
  - where tracking is ambiguous.

  Overlap with two-handing: a virtual stock needs a virtual shoulder (the
  stock's anchor), and the body solve would estimate that same shoulder.
  Design one shared shoulder and chest estimate used by both the stock and
  the arm IK, so the stock, the drawn arm and the aim agree. Decide which
  system owns the shoulder, how it follows head yaw and lean, and how the
  stock behaves before full-body IK exists. Sequence the two-handing and
  virtual stock design (item 7) with this one.

## Carried from 14 September (still open, after the items above)

- Evening checklist answers not yet given:
  - should a toggled grip let go past an angle;
  - should Immersive include the Informative-only notices;
  - suggested defaults for the bindings while gripping;
  - a flat chat-hitch test with frame generation off.
- Haptics: menu kinds done 15 September (`6e0d1a6`, evening item 8; the poll move
  proved unnecessary where shared eyes are attached). Originally: (need the viewer's pulse poll moved out of the
  shared-eyes path); worn feel of every kind (haptics were reported working).
- The rest of [todo-2026-09-14.md](todo-2026-09-14.md): SoloPlay buff and vote,
  reticle charge meters, whole-body IK, diegetic HUD items, waiting-on-user
  decisions.
