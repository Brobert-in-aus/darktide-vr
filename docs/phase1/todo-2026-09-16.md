# Next session: 16 September 2026

Branch `codex/alpha-4-2026-09-14`. The open items in
[todo-2026-09-15.md](todo-2026-09-15.md) still apply; this file holds new
items, first.

## First

- [ ] **Nothing presentation-side driven by 1p or 3p locomotion or
  animation** (user, 15 September evening). Audit and make absolutely sure,
  in matches (SoloPlay) and in the Psykhanium. Out of scope: whatever the
  server drives.

  Worn evidence: the forearm holster miniatures turned and flickered while
  strafing, following the hidden character as it turned to run at an angle.
  They also moved when switching between the gun and any other item. The
  displays now anchor to the controller grip and the placed gun (see the
  15 September checklist, item 22), but the source is still there.

  Worn again 15 September 19:50: anchoring the forearm items to the controller
  grip did not stop the wobble while strafing, so the controller targets
  themselves move. Lead: `controller_grip_target` hangs from the body anchor,
  which `refresh_body_anchor_from_avatar` and the camera update both take from
  `body_camera_anchor`. That is the fixed-step `first_person_component.position`
  plus a model-eye offset from `body_stable_eye_anchor`, possibly in the
  character's root frame, which turns to run at an angle when strafing. Check
  whether the offset rotates with the root, whether the two writers disagree
  between frames, and whether the drawn view or just post-update placements
  see it. The gun and glove likely wobble with it too.

  Known leads to check first:
  - `BodyProxy.align_gun_hand` (`darktidevr_body_proxy.lua`) sets the gun
    hand's wrist from the gameplay unit's animated `j_righthand`, relative to
    the weapon attach node. The drawn glove and `BodyProxy.hand_pose` follow
    the 3p animation, including strafe turns, and the pose differs for every
    weapon.
  - `BodyProxy.follow_gameplay_hands` copies the first-person rig's animated
    wrists.
  - `presentation.visible_grip_target` (`darktidevr.lua`) is built on
    `hand_pose`, so anything still using it inherits the above.
  - The body holster frame in `darktidevr_holsters.lua` (`body_frame`) reads
    the first-person unit's position and `body_visual_yaw`.
  - The two-hand support hand, the body mirror and overlay arms, the
    equipment hand sync (`sync_equipment_hand_to_proxy`) and the scanner
    hologram.

  Method:
  - List every read of animated nodes (`j_*hand`, `j_head`, spine,
    first-person unit poses) and every visual root that is placed from them.
  - For each, record whether the value reaches anything drawn. Replace it
    with a tracked or controller-derived pose, or record why it must stay.
  - Verify in eye renders while strafing and while switching items, in
    SoloPlay and in the Psykhanium.
