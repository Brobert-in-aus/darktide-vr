# Next session: 14 September 2026 (alpha.4 queue)

Branch `codex/alpha-4-2026-09-14`, from `5d1e7ca` (`main`, the published
0.1.0-alpha.3 with its dated changelog). Tomorrow's unattended run follows
[the brief](../handoffs/2026-09-14-unattended-brief.md). History and evidence
for every item below are in [todo-2026-09-11.md](todo-2026-09-11.md).

State at the start: 0.1.0-alpha.3 published on Nexus and as GitHub release
`v0.1.0-alpha.3` (archive `darktidevr-0.1.0-alpha.3-8bc59403fcd8.zip`,
SHA-256 `F809E292...9EC73`). The installed game folder holds that package
(every file matched it on 13 September); VR mode, vr settings profile,
`decals_enabled = true`.

## Unattended queue (14 September, in order)

- [ ] **Game crashes on shutdown.** Every close on 13 September (04:58 to
  10:04 UTC logs): `shutdown = true`, access violation reading 0x0 at
  `00007FF6620E13E6` about 0.3 s after `GameplayStateRun` exit, with
  `EventManager [destroy] Still have registered callbacks for event:
  (event_darktidevr_calibration_sample)` just before. That callback is
  registered in `darktidevr_calibration.lua` (`Managers.event:register(runtime,
  "event_darktidevr_calibration_sample", "sample")`) and never unregistered;
  `mod.on_unload`/`on_disabled` in `darktidevr.lua` do not release it, the
  native viewer or the d3d12 proxy hooks either. First establish whether it
  is the mod's: flat-mode control (batch choice 2, DMF only) with the same
  exit path, then VR mode. Then bisect inside the mod (unregister the event;
  stop the viewer and native hooks before engine teardown). Accept on three
  consecutive clean exits in VR mode with no `<<Crash>>`; record the exit
  path used (in-game Quit versus window close), since the user's path is not
  recorded.
- [ ] **Microstutter on each open-chat press** (worn 13 September; persists
  after the chat pin fix). Reproduce unattended in the Psykhanium: open and
  close chat repeatedly (drive the chat element's activation from a test-only
  command or flag rather than faked keys) and time the frame around it; the
  console's `[Profiler] ... Stall N ms` lines and a timed Lua log around
  `ConstantElementChat._handle_active_chat_input` are the first measures.
  Candidates: first text-input/material creation on the HUD panel mirror
  (`mirror_target.material` in `darktidevr_hud_panel.lua` creates GUI
  materials lazily), a scenegraph rebuild or Custom HUD re-pin when the chat
  window changes state, presentation/input-proxy reclassification when the
  chat input service takes focus. Fix only with a before/after measurement;
  the feel is a worn check.
- [ ] **Held-item effects placed before the hand pose.** The scan hologram
  fix (`darktidevr_scanner_holo.lua`, worn fixed) re-runs
  `AuspexScanningEffects:update_unit_position` after IK. Other wieldable slot
  scripts with `update_unit_position` read item or hand nodes at the same
  pre-IK moment: `flamer_gas_effects`, `chain_lightning_hand_effects`,
  `chain_lightning_link_effects`, `chain_lightning_ability_hand_effects`,
  `aim_projectile_effects`, `force_weapon_wind_slash_activation_effects`.
  Also sampled once: `weapon_shout_effects` burst, `missile_launcher_effects`
  discarded tube, `riot_shield_effects` activation burst. For each: static
  read (what it samples, whether it is linked or re-placed each frame,
  whether a dt 0 re-run is state-neutral), then a Psykhanium numeric trace of
  effect position against the tracked node after IK. Fix only those that are
  both displaced and safe; list the rest with the reason. Worn visual
  acceptance stays with the user.
- [ ] **Optional, local SoloPlay only:** the survival buff choice
  (`mission_buffs:choice`, mode 5) and the end-screen Y vote. Only if an
  offline SoloPlay route reaches them without matchmaking; logs as evidence,
  worn check remains.
- [ ] **Stretch, default off:** weapon charge meters around the XR reticle
  (rebuild the eight `forcesword_bar` material-value passes of the
  `cooldown_charges` counter on a world GUI, as `darktidevr_crosshair_feedback`
  does for the crosshair's charge passes). Ship behind an option only after a
  worn A/B.

## Waiting on the user

- [ ] Worn: survival-mode buff choice on the menu panel; Y vote on the end
  screen; moving chat with the Custom HUD editor.
- [ ] Decision: restore `particles_capacity_multiplier` to 1 (halved since
  before the VR work; can drop particle effects in busy fights). Do not
  change graphics settings until the user answers.
- [ ] Worn, carried from alpha.2: the player no longer appears twice in a
  mission-intro cutscene; Skitarii height on selection.

## Carried backlog (unchanged priority, see the 11 September file)

- DLSS frame-generation blur around HUD objects; cylindrical billboarding for
  atlas particle displays; physical versus simulator GPU gap.
- Votes (kick, mission flow) answerable in VR; pointer text entry.
- Proper two-hand support; broad haptics pass; left-hand dominant
  presentation.
- Remote-mission follow-ups; mode-2 panel blocking stock keyboard input on
  popups; the `restore.command` wording in the executable patch step.
