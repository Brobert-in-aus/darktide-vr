# Active development, 10 September 2026

The user resumed continuous development and the 20-minute heartbeat, until
explicitly told to stop. This supersedes the previous day's home/stop handoff.
Basic gameplay verification remains out of scope. Prioritize stereo efficiency,
DLSS implementation and deeper engine optimisation.

- [x] Establish the core 120 FPS target in the simulator: first mission-start
  trial delivered 119.41 distinct FPS with 60 native plus FG; native-only
  delivered 72.85 FPS. Continue CPU/stereo investigation and combat workloads.
- [ ] After the current 120 Hz tests and fixes, run a **144 Hz stretch test**.
  Compare 72 native plus FG where supported and native-only throughput.
  Keep 120 FPS the core target; simulator support does not establish physical
  Quest 144 Hz availability or worn acceptance.

- [x] Run the existing synthetic dual-view benchmark when physical tracking
  fails; record profiling limitations and the CPU recorder permission failure.
- [x] Verify a drop-in D3D12 OpenXR simulator with the existing XR consumer,
  including clean shutdown with the documented local runtime patch.
- [x] Make frame generation an explicit test variable; verify actual sustained
  generated stereo submissions, not just an enabled setting. The completion
  API lookup fix remains a focused candidate, with the accepted install restored.
- [x] Finish matched, repeated On/Off frame-generation comparisons, including
  cap policy and original/generated/repeated-frame accounting. The user prefers
  DLSS frame generation to Virtual Desktop SSW, but allows dropping it if stereo
  generation costs too much. Simulator results do not measure SSW or headset
  latency. See [the benchmark](../SYNTHETIC-FRAMEGEN-BENCHMARK.md).
- [x] Investigate generated-frame selection losses: the four-run uncapped
  comparison delivered 81.7/83.1 distinct pairs per second Off versus 67.7/67.2
  On. Counters identified exact-deadline reservation losses; a nearest-slot
  boundary delivered 89.96/90.00 distinct pairs per second in two full repeats.
  Retain frame generation for development; physical-runtime benefit and visual
  acceptance remain pending. Accepted installed files were restored.
- [ ] Continue engine performance work from measured repeated costs, with
  resource lifetime and per-eye correctness preserved. The decoded shader work
  and shadow-reuse contract provide starting points; neither proves a safe
  shadow-pass skip or unconditional linked-list clear removal.
- [ ] Investigate DLSS blur **around HUD items**, retaining it as unresolved
  until the specific artifact is fixed and accepted. See
  [surrounding-HUD investigation](../DLSS-UI-DETAIL.md#blur-around-hud-items).
- [ ] Obtain specific worn feedback for pending visual candidates when the user
  is available. Synthetic counters do not establish visual acceptance.

Keep the accepted mixed native/Lua/viewer installation and saved settings.
Use focused, reversible simulator trials and record exact restoration. Run the
physical Ready preflight before returning to a physical-headset session; the
simulator is an explicitly separate test environment.
