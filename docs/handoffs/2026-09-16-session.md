# Session: 16 September 2026

Branch `codex/alpha-4-2026-09-14`, starting at `dca0843` after the
0.2.0-alpha.1 release (tag `v0.2.0-alpha.1` on `d6d0e90`). Queue:
[todo-2026-09-16.md](../phase1/todo-2026-09-16.md), then the open items of
[todo-2026-09-15.md](../phase1/todo-2026-09-15.md). Boundaries as in the
[14 September brief](2026-09-14-unattended-brief.md); worn checks in
[test-checklist-2026-09-16.md](../phase1/test-checklist-2026-09-16.md).

## Start of session

- Heartbeat restarted (cron `7,27,47 * * * *`, session-only).
- Quest 3 `2G0YC1ZFB00CHH`: connected over USB and Wi-Fi, awake, 87 %, on AC
  power, proximity not positive (not worn). The proximity override state
  cannot be queried; reapply Disable before any unattended launch.
- `git status` clean at `dca0843`; Darktide not running.
- Installed mod, `bin` and `binaries\d3d12.dll`: all 114 package files match
  the 0.2.0-alpha.1 package tree.
- Unattended game launches are kept to the ones that need the game: three PC
  bugchecks (0x154) during level loads on 15 September.

## Work so far

- **Servo skull throw** (`c50bc04`, option default off): rest position
  brought forward, off-hand grab through the holster grip request (blitz
  selector), throw animation free for 2/5 of the estimated flight then
  blended into the real skull by moving a child node. Unit tests
  (`skull_throw`); worn check item 1. Assumption: "same height, just brought
  forward" means keeping the stock side and height offsets and replacing
  only the forward offset (-0.15 to +0.30 m).
- **Animation audit** (`docs/phase1/animation-audit-2026-09-16.md`, static,
  from a subagent sweep checked against the code):
  - Strafe wobble cause: displays placed in the locomotion post-update read
    last frame's tracked eye, a fixed step of travel behind while moving.
    Fixed by storing the eye relative to the body anchor and reading it from
    the current one (`790cb47`, test `tracked_eye_anchor`; item 2).
  - Anchor refreshed on the stock melee and keyboard-and-mouse path
    (`ac8dde8`; item 3).
  - Holster frame rebuilt at draw time, facing from the tracked head when
    there is no body yaw (`c75778a`; body holsters stay withheld).
  - Gun-hand glove held at a per-weapon grip captured over 30 steady frames
    (`efe5138`; item 4). Trade-off: the gun hand no longer moves during
    reloads or inspects.
  - Melee charge count placed from the controller grip;
    `visible_grip_target` removed (`be05618`; item 5).
- Suite 263/263 after each change (game closed); each deployment diffed
  against the previous commit's installed files first.

## Waiting on the user

- **Animated hands during stock melee swings and keyboard-and-mouse play**
  (audit item G): the gloves copy the first-person animation's wrists there
  by design. Options: keep that (swings look like the stock animation), or
  place the hands from the controllers during melee too (true to the
  requirement, but a button swing then shows no arm motion). Assumed: keep
  until decided.
- The body holster facing before they return (todo 2026-09-16).

## Full-body milestone 3 start

- Clavicle swing toward the body frame shoulders in the `overlay` dev mode
  (`ea302f8`). A/B unattended runs (`clav1`, `stock1`, Psykhanium, synthetic
  path, proximity override disabled for the runs and re-enabled after): right
  arm out of reach on 70 frames against 3,043 of 5,400; left 1,344 against
  1,411, with the 30 degree cap binding on the left. Details in the full-body
  design, "Milestone 3 start". Both runs ended cleanly (no script errors,
  no leftover processes or flags).
- Root yaw from the body frame (`dbb0af3`), run `yaw1`: no effect (the
  avatar root already faces the body frame yaw in this mode, delta 0.0), so
  the left/right clavicle gap difference is not a yaw mismatch. Four
  unattended launches today, all clean; further milestone 3 steps (spine,
  neck, clavicle cap) need eye renders or a worn look, so live runs stop
  here for now. Proximity automation restored after each run.
- Spine bend toward the body frame's neck (`6d04199`, `overlayspine`), run
  `spine1`: neck gap 0.08 m to under 0.01 m, clavicle gaps left about 0.06 m,
  right about 0.025 m, root stays over the feet. Eye readbacks were not
  served, so no render yet. Five unattended launches today, all clean.
  The render poller (`artifacts/unattended/body-mirror-20260916/poll-readbacks.ps1`)
  matched its log line only as the session ended, so its first request went
  unserved; the stale `%TEMP%\darktidevr-shared-eye-readback.request` was
  deleted afterwards. Next render attempt: request by time after the scene is
  reached rather than by log line.
