## Narrow native command-state copies

[Two consumers now copy only their needed state](../NARROW-COMMAND-SNAPSHOT-PERFORMANCE.md): cluster-light CBVs 0/2 (16 bytes) and menu original target (8 bytes), preserving existing lock scope and admission. A fair local-copy benchmark with identical return boundaries measures 100,000 reads at median 2.6584 ms full versus 1.1712 ms narrow. Native Release builds; command-snapshot/native-hook checks pass 2/2 in 0.72 seconds. No deployment or game FPS claim; outside staged `8b3697a`.
## Hand-pose performance review: existing implementation retained

[Scalar rotation experiment](../TWO-HAND-ROTATION-PERFORMANCE-REVIEW.md) matches 10,000 geometry cases and reduces interpreter allocation, but LuaJIT already eliminates the candidate tables. JIT heap growth is unchanged and timing ranges overlap. Production pose code was restored unchanged; only the explicit benchmark and evidence are saved. No gameplay tests or live work were added. Continue with costs that remain in the compiled path.
## Expanded native performance comparison

The [native hook benchmark](../NATIVE-HOOK-PERFORMANCE.md) now records/checks adapter and driver identity and accepts 3-31 odd trials. Fifteen fresh processes per version compare accepted native with staged `8b3697a`: exhausted log median 11.0243 to 5.1497 ms; short-name path 11.0891 to 7.0136 ms; control 5.1804 to 5.1031 ms with overlapping ranges. Thirty diagnostic logs match exactly after pointer normalization. Actual selected adapter is RTX 4090, driver 32.0.16.1088. No queue submission, game or OpenXR session. Both installed DLL hashes remain accepted. Receipt: `artifacts/unattended/native-hook-comparison-15-trials-20260909/comparison.json`. Continue useful development until home/stop.
## Projection calculation performance

[Projection loop optimisation](../PROJECTION-LOOP-PERFORMANCE.md) shares constant corner signs and computes eye tangents once. Exact scale/width/center results match across 2,000 constructed frusta. Ten temporary arrays per pair are removed; representative tangent calls fall 34 to 10. JIT mathematical-double benchmark median falls 2.4318 to 1.5641 ms; interpreter ranges overlap, so no broad timing claim. Initial numeric-loop rewrite regressed JIT and was discarded. All 69 Lua chunks compile and the existing projection check passes. Undeployed and outside the staged three-file Lua performance package.
## Focused Lua performance package

Implementation `008e1b0`, documentation `bd3c7ce`, PR #197, worktree `D:/Projects/games-xr/Darktide VR Lua performance`, base accepted Lua `3341afb`. Only controller prompts, marker GUI and HUD panel change. All 49 package chunks compile; its three affected existing checks and actual-module performance comparisons pass. Mainline HUD-follow optimisation is `3d270fb`, PR #196.

Staged plan: `artifacts/focused-trials/lua-performance-008e1b0/trial-plan.json`. Three replacements pin actual installed byte hashes; two source files differ from installation only by line endings, with normalized content equality checked. Copied-file apply/rollback passes: `artifacts/unattended/lua-performance-rehearsal-77c32c9a735f4c8786a950494c1fafb9/rehearsal.json`. Candidate/restored hashes match; sentinel and real installation remain unchanged. No deployment. Communication candidate overlaps controller prompts; do not blindly stack. Native performance, native UI, marker, communication and Lua performance plans remain staged independently.
## HUD follow allocation optimisation (offline, undeployed)

Removed temporary component arrays and spring closure; shared damping is calculated once. Actual baseline/candidate modules match every field across 10,000 moving poses and preserve reset/reuse identities. Temporary allocation volume falls 37%; median 10,000-call time falls 5.3158 to 2.1476 ms with JIT and 9.8451 to 5.4570 ms without it. These are isolated CPU results, not FPS or worn acceptance. All 69 Lua chunks compile; existing HUD panel check passes. See [measurement details](../HUD-FOLLOW-PERFORMANCE.md). Accepted installation remains unchanged; continue performance work until the user says home/stop.
# 9 September development: current handoff and evidence

## Current state

Latest candidate: [HUD draw wrapper allocation removal](../HUD-DRAW-ALLOCATION-PERFORMANCE.md)
for marker GUI, stock crosshair and fixed-HUD scale scopes. All 69 Lua chunks
compile; 2/2 existing affected checks pass in 0.04 seconds. Marker/crosshair
benchmarks remove roughly 16-17 MiB of result tables per 100,000 constructed calls;
interpreter medians are 18.1297 to 7.5582 ms and 15.2958 to 5.8341 ms respectively.
No layout/projection change, game FPS claim or deployment. Prompt optimisation
is saved as `89c274c`, PR #194. Continue performance work until home/stop.

Latest candidate: [HUD prompt allocation removal](../HUD-PROMPT-ALLOCATION-PERFORMANCE.md).
Protected scope and update hooks forward multiple returns without per-call result
tables. Existing prompt check passes (0.03 seconds), all 69 Lua chunks compile,
and benchmark arity/error checks pass with JIT on/off. Roughly 16-17 MiB of temporary
tables per 100,000 constructed calls disappear; interpreter scope median is
14.1989 to 4.0985 ms. No game FPS claim or deployment. Hook-comparison harness is
saved as `c95bf9f`, PR #193.

14:58 Brisbane Inventory remains asleep, one physical Quest/two resolved transports,
one Streamer, zero game/launcher. No proximity override was applied; readiness
remains false. Installed Lua/native/bootstrap hashes remain accepted. Receipt:
`artifacts/unattended/performance-inventory-20260909.json` and matching log.
The active 20-minute heartbeat still includes the user's performance priority.

Latest performance evidence: [actual native hook comparison](../NATIVE-HOOK-PERFORMANCE.md)
compares accepted DLL with staged `8b3697a` in ten isolated recording-only processes.
Per 40,000 barriers: menu-log median 11.3766 to 5.6263 ms; name-matching median
10.9198 to 6.9455 ms; control 5.0514 to 5.3798 ms with documented run variability.
All ten admitted 4,096-entry logs match after normalising process-local addresses.
No GPU submissions, game, OpenXR session or deployment. Later buffer-cache and
pose-math candidates are outside this focused comparison. Pose candidate is
`5a2b9eb`, PR #192. Continue development until home/stop, not basic gameplay checks.

Latest candidate: [pose normalisation CPU cost](../POSE-MATH-PERFORMANCE.md).
Composition normalisations fall from six to three, inversion from two to one.
Native and viewer builds plus 6/6 focused math/caller checks pass in 0.66 seconds.
Median per million compositions: 42.5970 to 24.0944 ms; 100,000 varied inputs
show maximum component difference 8.58307e-6. This is a floating-point change,
not bit-identical output or live acceptance, and is not in the staged native trial.
Buffer-cache final source is `aa7dcac`, PR #191, also undeployed.

Buffer-cache follow-up keeps newest-allocation checks before hashing and compares
four workloads through identical full-metadata lookup boundaries. Final 2/2 native
checks pass in 1.54 seconds. Median per 100,000 queries: repeated early addresses
43.3195 to 1.7254 ms; newest 1.3266 to 1.2705; cyclic all 22.6544 to 22.2357;
unique misses 45.8822 to 46.3814. Cold/newest differences are within substantial
run variation; no general speedup is claimed. See the final receipt in
[buffer lookup performance](../BUFFER-LOOKUP-PERFORMANCE.md). PR #191 initial
implementation is `bbfbe8b`; this remains separate from the staged native trial.

Newest offline candidate: [exact-address buffer lookup cache](../BUFFER-LOOKUP-PERFORMANCE.md).
Native Release build and 2/2 focused native checks pass in 0.82 seconds. It keeps
fresh mapping metadata and invalidates on registration/refresh/destruction.
Constructed cache-friendly median: 38.0049 to 1.8084 ms per 100,000 lookups.
This is not live FPS evidence and is not included in the staged three-change trial.
Mainline trial handoff is saved as `b54c820`, PR #190; focused native PR #189 has
a documentation follow-up `4ccaee7`, with reviewed payload implementation `8b3697a`.

Focused native performance trial is staged, not deployed: source `8b3697a`,
PR #189, based directly on accepted native `23345e5`. Worktree:
`D:/Projects/games-xr/Darktide VR native performance`.
Only the three CPU optimisations from PRs #186-188 are ported; shared helpers/tests
match mainline and all three focused CTests pass in 0.77 seconds after a native
Release build. Mainline snapshot implementation is `c7aea64`, PR #188.

Plan: `artifacts/focused-trials/native-performance-8b3697a/trial-plan.json`.
Candidate DLL SHA-256 `63592118307969E087F7EBC849BB011291C4C75708591C5092966F001A74235B`.
Both copied native locations installed/restored by hash, sentinel preserved,
and both actual baseline hashes remained accepted. Receipt:
`artifacts/unattended/native-performance-rehearsal-c10ba603607d421ba2b0f131a6a6f5e8/rehearsal.json`.
Ready is still required before real deployment. The native UI and native performance
payloads replace the same DLL and must not be blindly combined.

Performance follow-up: [fixed command snapshots](../COMMAND-SNAPSHOT-PERFORMANCE.md)
avoid diagnostic map cloning in six helpers; native build and 4/4 focused checks
pass in 0.66 seconds. Constructed 128-entry map benchmark median falls from
39.6368 to 0.2621 ms per 10,000 copies; empty map 0.8794 to 0.2669 ms.
These are copy-cost measurements, not in-game FPS. Resource-name optimisation is
saved as `311d1cb`, PR #187. All performance changes remain undeployed.

14:17 Brisbane performance follow-up: [resource-name matching](../RESOURCE-NAME-PERFORMANCE.md)
now avoids short-name allocations and size queries while observing renames.
Native Release build and 2/2 focused CTests pass. Real WARP resource metadata
benchmark median: 17.3987 to 4.9313 ms per 200,000 wide-name queries, not game FPS.
Previous exhausted-diagnostic change is committed as `3dc0f6e`, PR #186.
Both remain undeployed; continued performance work is the active priority.

Latest user steering: no further basic gameplay verification is needed. Continue
with measurable performance optimisation while visual priorities await their
return. The 20-minute heartbeat now includes this direction. The unfinished
cached mission runner and strict ranged-profile loader edit are parked in the
path-limited stash named `Park unfinished cached gameplay-contract runner after
performance priority change`; do not resume them as the next task.

Latest candidate: [exhausted menu diagnostic CPU cost](../BOUNDED-DIAGNOSTIC-PERFORMANCE.md).
Native build and 2/2 focused checks pass. Median exhausted-admission benchmark:
27.1734 to 0.7815 ms per four million calls/eight threads. This is not an in-game
FPS result. No deployment.

Continue development and the 20-minute heartbeat until the user says they are
home or asks to stop. Do not treat completion of a bounded checkpoint as the
stopping condition. Work independently offline while the worn billboard
observation remains unanswered; physical melee remains paused.

- **Installed acceptance is unchanged:** Lua `3341afb`, native `23345e5`,
  viewer `6688841`. Do not deploy the accumulated main branch as a unit.
- **Full offline validation:** the undeployed SR observation candidate passes
  **183/183 in 60.93 seconds**, headset tests OFF, including the pinned LuaJIT
  source gate (69 chunks) and optional hash-pinned NGX ABI reference. Receipt:
  `artifacts/unattended/ngx-sr-offline-suite-20260909.log`. The final additional
  CPU-result/first-call log fields then rebuild and pass 9/9 NGX checks in
  1.33 seconds (`ngx-sr-build-final-20260909.log`, `ngx-sr-tests-final-20260909.log`).
  Cached-stock optional contracts retain their separate recorded runs.
- **Live work is unavailable:** 13:44 Brisbane Inventory finds the Quest asleep,
  one physical device/two resolved transports, one VD Streamer, no game or
  launcher. It did not change proximity or certify Ready. The existing
  20-minute heartbeat remains active.
  Receipt: `artifacts/unattended/sr-wield-inventory-20260909.json` and matching log;
  installed Lua/native/bootstrap hashes remain accepted.
- **Priority remains billboarding, then pickup sizing.** Worn attribution and
  complete popup raster bounds remain open; no sizing/blur fix is visually
  accepted. DLSS blur around HUD items remains on the active todo.
- **Focused trials remain staged, undeployed:** native UI `a4ec84c`, marker
  metrics `8178c5f`, communication `848b78d`. Their rollback rehearsals pass;
  marker and communication both replace main Lua and cannot be blindly stacked.
- **Recent offline work:** ranged/grenade sequences through the stock parser, popup
  pass restoration against stock renderer code, between-eye invalidation and
  animation admission, diagnostic report input/output preservation, and a
  default-off bounded SR resource observation (not in the focused native trial).
  Detailed validation limits and receipts are retained below and in feature docs.

## Checkpoint history (newest first)

- **14:00 Brisbane: syringe use lifecycle.** New optional
  `tests/tooling/test-stim-stock-contract.lua <cached-source-root>` passes with
  pinned LuaJIT. It runs source validators/settings and actual syringe lifecycle
  methods, trigger timing and fixed-frame clamp: 40 variant/target/authority/speed
  cases, 25 interrupted/invalid-target/replay cases, two corruption-health guards,
  post-use replay suppression and targetless minimum-time exit. Effects, stats,
  superclass, unit state and inventory operations remain doubles; no live buffs,
  charge persistence, input-to-action integration or worn acceptance. Receipt:
  `artifacts/unattended/stim-stock-lifecycle-20260909.log`. No production changes.

- **Stock stim input sequences:** all five cached syringe consumer modules and
  their complete generator now run through the optional real formatter/parser/
  hierarchy/queue fixture. Four ordinary stims add 25 steps each for self/ally
  use, gift aim/release and tracking-loss rearm; Broker adds two automatic-input
  cases and generates no gift actions. Total **279 observed steps**, passing
  both main/focused `848b78d` with default/saved bindings. Receipts:
  `artifacts/unattended/stim-sequences-{main,focused}-{default,saved}-final-20260909.log`.
  Targeting/resources and inherited action bodies are placeholders; no buff,
  inventory transfer, charge or live acceptance. Gift release on tracking loss
  is not a cancellation guarantee. The supplied-profile loader also now rejects
  false/nil/non-table values; false previously produced a default-profile pass.
  Reproduction/rejection receipts are `stim-profile-*20260909.log`.

- **Mission-device saved controls:** the optional actual-stock device consumer
  fixture accepts argument 3 with reviewed binding settings and resolves buttons
  or stick sectors for primary/alternate/interact/jump-dodge. Main and focused
  `848b78d` pass with default and saved layouts; saved interaction uses RS-down.
  Unbound interact and a non-table profile fail explicitly. Device hold/release,
  dodge, cancel/escape policy, weapon block, axes, completion and wield loss
  retain stock decisions. All three device-action controls release on input
  loss, suppress held reconnect and require neutral before reactivation.
  The separate timer/alias section still uses its
  constructed X/right-grip layout; engine/device behavior is mocked. Receipts:
  `artifacts/unattended/mission-device-{main,focused}-{default,saved}-rearm-20260909.log`
  and `{unbound,invalid}` negatives. No production/settings/deployment changes.

- **13:43 Brisbane: stock wield-slot eligibility.** The optional wield-overlap
  fixture now runs the real slot resolver, unwield condition and weapon/visual/
  ability `can_wield` methods. Both main and focused `848b78d` pass 32 valid
  layout/device/order combinations, actual ownership/template/depletion guards,
  and three independent dispatch vetoes. Rejected chosen-slot eligibility does
  not fall back to the other X selector inside this condition. Full action queue,
  inventory mutation and live mission behavior remain untested; inventory and
  ability state are constructed. The fixture does not rewrite user settings.
  Receipts `artifacts/unattended/wield-slots-{main,focused}-20260909.log`.

- **SR metadata reader:** `tools/stereo/read-ngx-sr-probe.py` validates bounded
  schema-1 observations without writing files. It distinguishes complete
  metadata, incomplete records, failed queries, successful null inputs and CPU
  evaluation failures; no result claims HUD pixels or GPU completion. It groups
  interleaved calls and rejects contradictory identities, descriptors, windows,
  duplicate indexes/results, unsupported fields and budget/range violations.
  The native formatter is shared with an offline fixture, and its actual output
  roundtrips through the reader. Eight Python cases and the existing native/ABI
  checks pass as 10/10 focused NGX CTests in 1.53 seconds; the isolated native
  DLL rebuild passes. Receipts `artifacts/unattended/ngx-sr-reader-{build,tests}-20260909.log`.
  The full 183-test result above predates this reader; configured total is now
  184 including the optional NGX ABI reference. No deployment or live capture.

- **13:29 Brisbane: SR resource observation, undeployed.** Main candidate
  `codex/ngx-sr-input-observation-2026-09-09` adds a default-off, separate
  `darktidevr-ngx-sr-<pid>.log`. It queries seven official resource names for
  verified kind-1 lifetimes, within the existing window, at most 64 evaluations
  per process independent of FG. Per-query results/descriptors and original
  CPU evaluation results are recorded; no pixel copies, parameter writes,
  resource references or state changes. Missing/failed queries and incomplete
  calls cannot establish missing HUD inputs. Jitter/history and HUD pixel
  ownership remain unobserved. See [DLSS detail](../DLSS-UI-DETAIL.md).
  Configure the isolated build with `DARKTIDEVR_NGX_REFERENCE_INCLUDE_DIR`
  pointing at the hash-pinned local `artifacts/diagnostics/dlss-output-boundary-20260906`
  headers; build Release targets `darktidevr_native_capture`,
  `darktidevr-ngx-sr-observation-tests`, `darktidevr-ngx-abi-reference`.
  CTest full/final focused results are recorded above. Both installed DLLs
  still hash to `FCCDD0DE699F9D50D2BD316792829D4EE5700843D925D194BE4DF1F3EEB02369`.
  No flag, focused trial payload, saved setting or live session changed.

Earlier uses of “latest” and test counts below refer to their own checkpoint;
they do not supersede the current state above.

- The accepted installed mixed build and saved settings are unchanged.
- Unloaded popup capture now rejects text box/debug-box passes at non-unit or
  unspecified scale. Stock text and rectangle paths apply scaling twice to the
  optional box, so it cannot share the text's crop translation. The regression
  failed before the guard; eight actual-stock transform cases, eight widget
  CTests (0.11 seconds) and the 69-chunk Lua gate pass. No live popup diagnosis
  or deployment; receipt `widget-scaled-text-box-stock-20260909.log`.
- The delayed grenade contract now executes stock Ammo/finish helpers in eight
  client/server, one/two-unit and released/pre-release cases. Consumption occurs
  once after the deadline; last-charge finish requests item removal, while the
  ammo route leaves ability charges unchanged. Main/focused `848b78d` pass.
  Eligibility, superclass finish, physics and actual inventory teardown remain
  substituted or untested. No production change. Receipts:
  `grenade-ammo-{main,focused}-20260909.log` under `artifacts/unattended`.
- The input sequence fixture now observes 177 steps after loading quick flash's
  automatic inputs and the four expedition grenade hierarchy overrides. Main
  and focused `848b78d` pass default/saved profiles. The source audit records
  17 generator consumers; it does not establish owned or available equipment.
  Safe-zone conditions, ammo/removal, ability-switch branches and actual
  projectiles remain outside this test. No production change. Latest receipts:
  `grenade-sequences-*-overrides-20260909.log` and
  `grenade-generator-consumers-20260909.json` under `artifacts/unattended`.
- Both grenade generator base tables now join the stock input sequence fixture:
  89 total steps pass for main/focused `848b78d`, default/saved profiles. This
  includes overhand/underhand/cancel and tracking-loss rearming. Loss produces
  `aim_released`, not a proven throw cancellation. The separate stock delayed
  release/preview contract still passes with physics substituted. No production
  change. Cancellation also replaces a deliberately unconsumed aim request.
  Latest receipts: `grenade-sequences-*-pending-20260909.log` and
  `grenade-delayed-stock-contract-20260909.log` under `artifacts/unattended`.
- Alpha and generated-frame report output guards now cover all their PNG/JSON
  destinations and input images/logs. Fourteen alias cases preserve bytes and
  separate reports retain existing semantics; ten alpha and twelve generated
  tests pass, with all four DLSS analysis CTests passing in 3.69 seconds.
  Both regressions failed before the change. No runtime or actual capture edits.
- The stereo UI input inspector validates both eyes before writing any output,
  preserving existing reports after missing/malformed/mismatched second-eye
  input. Output aliases to source bitmaps are rejected. Regressions failed
  before the guards; real BMP fixtures and all four DLSS analysis CTests pass
  in 3.64 seconds. The added test brings registration to 181; no new full-suite
  result is claimed. Actual captures and installed runtime remain unchanged.
- Marker and DLSS detail report writers now prevent report paths from replacing
  capture inputs, including hard links. Regressions failed before the guards;
  eight marker and seven detail tests pass, with three focused CTests passing
  in 0.74 seconds. Separate report generation still works. Real captures and
  focused native/Lua payloads are unchanged.
- Popup capture now has an optional actual-stock renderer pass contract:
  normal and failing begin/draw/end paths restore the dedicated renderer's
  saved graph, queue, settings and base pass. No production correction was
  needed. Pinned LuaJIT stock/isolated variants and eight widget/plane CTests
  pass (0.11 seconds); GPU allocation remains mocked. See
  WIDGET-SURFACE-LIFETIME and `widget-stock-renderer-pass-20260909.log`.
- A separate ranged sequence fixture now passes 47 steps through the actual
  cached stock formatter/parser/hierarchy/queue for plasma, shotgun and staff.
  Both main and focused `848b78d` pass default and saved profiles, including
  tracking-loss release and neutral rearming. It consumes inputs immediately
  and excludes wield arbitration, buffer aging, action execution and live
  acceptance. No production change. See RANGED-WEAPON-AUDIT and the four
  `ranged-sequences-*-20260909.log` receipts under `artifacts/unattended`.
- Latest read-only Inventory, **12:50 Brisbane**, still reports one physical
  Quest via two resolved transports, one VD Streamer, Quest asleep and no game
  or launcher process. No proximity change or Ready certification. Installed
  Lua/native hashes remain accepted. Receipt:
  `artifacts/unattended/report-tools-inventory-20260909.json` (with matching `.log`).
  The existing 20-minute heartbeat remains ACTIVE; no duplicate was created.
- Ranged stock input coverage now includes supplied saved bindings and stick
  sectors. All 62 templates/530 combat elements pass hold/toggle ADS with 256
  real transitions for both main and focused `848b78d`, in default and saved
  profiles. Saved right-stick-down reload is observed; an unbound reload fixture
  is rejected. All 43 reviewed binding keys and the saved-settings hash are
  unchanged. All 14 duration elements also pass interval-start admission and
  deadline completion, closing the deadline-only fixture gap. This is
  input-element coverage, not full sequence or live firing/mission acceptance.
- Popup capture rejects active widget animations until their results can be
  frozen before bounds admission. Empty animation tables remain eligible and
  first-eye rejection stays latched. Both regressions failed before the guard;
  eight widget/plane CTests pass in 0.11 seconds, actual cached UIWidget animation
  dispatch confirms the ordering, and all 69 Lua chunks compile. Still unloaded.
- Popup capture invalidation between eyes now hides image/submission state while
  preserving the first eye's handled/rejected decision. Both regressions failed
  before the fix; eight widget/plane CTests pass in 0.19 seconds and 69 Lua chunks
  compile. Next-frame warming and destruction cleanup remain intact. These
  modules are still unloaded; no popup sizing or worn acceptance claim.
- Prepared atlas smoke trials now pin reviewed shader hashes and absent
  destinations through the transaction. Both profiles pass current-installation
  ValidateOnly and separate fake-root commit/restore rehearsals: two added
  shaders removed, copied production shader and unrelated sentinel preserved.
  Receipt: `artifacts/unattended/atlas-trial-rehearsal-fcdade7e25bd44c987e99eca03a0a27d/rehearsal.json`.
  No real trial or Ready run; worn comparison remains pending.
- Latest full Windows x64 offline Release at `8e723b1`: **180/180 pass in
  58.15 seconds**, headset tests OFF; all 69 Lua chunks compile. This includes
  the later menu guards and analysis fixes below. Receipt:
  `artifacts/unattended/timing-marker-analysis-offline-suite-20260909.log`.
- Frame-stage/VDXR report writers reject source-file output paths and hardlink
  aliases. Both temporary-file CLI regressions failed before the fix; twelve
  Python cases pass across two analysis CTests in 0.69 seconds. Saved captures
  are unchanged.
- Marker log analysis now accepts the actual observer's unmatched-eye record,
  including its required input-only marker. The real Lua logger roundtrip failed
  before the fix; eight Python cases and three marker CTests pass in 0.39 seconds.
  Orphaned eye scopes remain unmatched evidence, with no Lua payload change.
- Saved timing-log analysis now preserves restart separation after a malformed
  row. The regression failed before the fix; six Python cases and both timing
  CTests pass in 0.17 seconds. This is analysis-only, with no live performance claim.
- Direct menu View retrieval and character-select readiness now protect retiring
  null-reference lookup/query. Failed eligibility preserves the stock returned
  service, retires XR state and reads no pointer. Readiness treats query failure
  as blocked. The regression failed before the fix; five related checks pass in
  0.14 seconds and 69 chunks compile. Focused port `848b78d` (PR #161) passes
  64/64 Lua checks in 0.833 seconds and compiles 56 chunks. Its refreshed payload
  passes copied-install rollback with 57/50 compiled chunks, 51 originals restored
  by hash and seven additions removed. Actual installation preconditions are unchanged.
- Menu null-service recovery now retires the old XR gesture. The first recovered
  sample drains queued click/back/scroll and requires release, without reading
  blocked input. The delayed-click regression failed before the fix; both service
  routes, held/same-frame recovery and fresh rearm pass. Five related checks pass
  in 0.14 seconds and 69 chunks compile. Focused port `29b30bd` (PR #158) passes
  64/64 Lua checks in 0.895 seconds and compiles 56 chunks. Its refreshed payload
  passes copied-install rollback: 51 originals restored by hash, seven additions
  removed, 57/50 chunks compile with their own descriptors. No real deployment.
- Earlier full Windows x64 offline Release at `549b385` (PR #156): **180/180
  pass in 57.09 seconds**, headset tests OFF. The first run exposed the early
  launch-failure fixture's missing Lua package; it now supplies compile-only
  chunks so its intended cache failure remains covered. Receipt:
  `artifacts/unattended/installed-lua-gates-offline-suite-final-20260909.log`.
  This full run precedes the menu null-service recovery follow-up above.
- Launcher now compiles installed Lua after optional sync (also when skipped),
  before worker tuning or launch preparation. Ready checks installed Lua before
  live work; Inventory remains observational. Real script-prefix fixtures
  reproduced the old acceptance of a broken installed descriptor and now pass
  skip-sync/post-sync/Ready/Inventory cases. Six related CTests pass in 5.32
  seconds. CTest count is now 180; the full run above includes this check.
  No actual launcher or Ready session ran in this validation.
- The Lua source gate now compiles the selected package's own descriptor rather
  than borrowing main's when `-SourcePath` points elsewhere. Broken-descriptor
  acceptance reproduced before the fix. The expanded Windows PowerShell source
  fixture passes in 0.55 seconds; deployment/package checks pass. Main/focused/
  installed gates compile 69/56/50 chunks with their respective descriptors.
  These gates compile without executing Lua or writing installed files.
- Inventory/tag/tactical null-service probes now catch retiring lookup/query
  failures, letting stock consumers run without VR injection. Tag rejection
  remains paired and inventory edges expire. The regression failed before the
  guard; three related checks pass in 0.12 seconds, 69 chunks compile, and cached
  stock tactical update passes. Focused port `96292b4` (PR #153) passes 64/64
  Lua checks in 1.203 seconds and compiles 56 chunks. Its refreshed payload
  changes one of fifteen files; copied-install rollback restores all 51 fixture
  hashes and removes seven additions, with 57/50 chunks compiling before/after
  recovery. Actual installation preconditions remain unchanged. Latest inventory
  at 11:20 Brisbane still finds the Quest asleep and Darktide closed, without
  changing proximity or claiming Ready.
- Actual cached tutorial/text functions now pass the optional controller-prompt
  contract: VR remaps, loss/recovery, stock device aliases and held/released
  wording survive both prompt hooks, with no global input-mode change. Three
  ordinary prompt/UI checks pass in 0.06 seconds. This is text-route evidence,
  not worn layout/tutorial completion; no production fix was required.
- Opaque UI placement comparison now retains zero in every search and prefers
  minimum displacement on exact score ties. Missing/flat UI flags ambiguity
  instead of inventing a shift; the tie count covers sampled offsets only.
  The regression failed before the change; three DLSS analysis CTests pass in
  1.15 seconds. This remains measurement only, not surrounding-blur acceptance.
- Latest full Windows x64 offline Release at `556f4c6` (PR #148): **179/179
  pass in 56.27 seconds**, headset tests OFF. This includes both retiring-input
  fixes and dynamic widget callback admission. Receipt:
  `artifacts/unattended/dynamic-widget-offline-suite-20260909.log` (ignored).
- DLSS source/settings audit confirms the master menu toggle changes SR and FG
  together. Saved settings currently select Quality SR plus one generated frame;
  this is not verified live state. Existing native bitmap evidence observes FG
  kind 11, not SR kind 1. See the independent-controls section in
  [UI detail](../DLSS-UI-DETAIL.md). Settings and runtime remain unchanged.
- Popup capture now rejects dynamic pass change/visibility callbacks before
  drawing: cached stock UIWidget demonstrates either callback introducing an
  unsupported viewport transform after admission. Rejection is latched across
  both eyes. The regression failed before the guard; eight widget/plane checks
  pass in 0.14 seconds, 69 chunks compile, and actual stock geometry checks pass.
  Stock interaction visibility callbacks still need owned frozen results before
  complete popup capture can be integrated. This candidate remains unloaded.
- PTT eligibility probes now protect retiring input lookup/query failures,
  skipping controller injection so stock voice-mode/mute updates still run.
  A regression failed before the guard. Three related checks pass in 0.06 seconds,
  69 chunks compile, and cached ChatManager mode/cleanup checks pass with microphone
  operations mocked. Focused communication includes both retiring-input fixes at
  `2dded96`, PR #146: 64/64 Lua checks pass in 1.048 seconds and 56 chunks compile.
  Its fifteen-file payload passed copied-install deployment and rollback: 57
  chunks compile installed, 50 after rollback; all original fixture hashes return
  and seven added modules are removed. The actual installed files are unchanged.
- Deferred wheel release treats throwing input-service lookup/query as lost
  ownership and cancels. The regression reproduced a stuck queued release before
  the fix. Six related tests pass in 0.11 seconds; 69 chunks compile; actual cached
  wheel callbacks pass with effects mocked. Integrated fix is `2bae6b7`, PR #143.
  Focused communication first included it at `90970d5`, PR #144, with 56 chunks and
  expanded coordinator/stock/input checks passing.
- Full Windows x64 offline Release at `100959d` (marker summary PR #142):
  **179/179 pass in 56.19 seconds**, headset tests OFF. Receipt:
  `artifacts/unattended/marker-trials-offline-suite-20260909.log` (ignored).
  This full run precedes both retiring-input follow-ups above.
- Read-only marker log analysis now separates runs, kinds, empty/incomplete
  pairs and matching observed inputs; normal stereo anchor deltas remain separate
  from mismatch counts. Invalid evidence rejects before output, and input bytes
  are hashed. Eight fixtures pass, including actual Lua observer log emission;
  three related CTests pass in 0.36 seconds. This is not raster/worn acceptance.
  Recovery preconditions are `0b99afd`, PR #141. Current CTest count is 179.
- Recovery now carries original-backup and installed-state hash preconditions
  through staging, closing the gap after its initial validation. Concurrent
  changed/removed/appeared destinations or damaged backups reject before writes;
  explicit changed-file recovery still protects later concurrent replacements.
  The first regression failed before the fix; four related checks pass in 5.20
  seconds and the fifth explicit-override race passes alone. Trial staging is
  `ab8f6b6`, PR #140. No real files were deployed or restored.
- Native UI and marker measurement payloads are separately staged with exact
  candidate/baseline preconditions. Both install/rollback rehearsals pass in
  temporary copies, including both native destinations and removal of the new
  Lua module. Actual installed preconditions and viewer hash remain unchanged.
  See [focused trial staging](../FOCUSED-DIAGNOSTIC-TRIALS-2026-09-09.md).
  Hash preconditions are `b515e72`, PR #139; no live deployment occurred.
- Selective deployment entries can now pin candidate and installed SHA256s,
  including explicit expected absence for new modules. A later mismatch rejects
  the whole plan before installed writes; manifests retain the preconditions.
  The regression failed before the fix. Four temporary-install deployment/sync/
  recovery/package checks pass in 4.95 seconds. No real installation changed.
- Marker observer scopes now retire on stop/restart, including reentrant draw
  callbacks; old success/failure cannot corrupt a new run's primary record.
  Disable/unload stop measurement. A regression failed before the fix; expanded
  fixtures and five related checks pass; 69 chunks compile. The focused port
  `8178c5f`, PR #138 (extending PR #136), is separate from communication Lua and
  now includes this follow-up; 50 chunks and its expanded observer/stock/main
  renderer checks pass. Integrated lifecycle fix is `8e5bdd8`, PR #137.
  Popup transform admission is `ac7bc9a`, PR #135.
- Latest read-only inventory: one physical Quest, one VD Streamer, Quest asleep,
  Darktide/launcher closed. It did not change proximity or certify readiness.
  Receipt: `artifacts/unattended/popup-inventory-20260909.json` (ignored).
- Popup capture now rejects absolute widget scaling and viewport-relative pass
  styles before drawing/copying. A first-eye rejection is retained for the pair.
  The regression failed before the guard; cached actual UIWidget.draw confirms
  the translated-offset mismatch and viewport sizing. Seven widget tests pass
  in 0.09 seconds; all 69 chunks compile. Complete raster bounds and engine
  integration remain open. Preceding checkpoint is `d266be3`, PR #134.
- Full combined Windows x64 offline Release suite at `de5e2d9`: **178/178 pass
  in 56.73 seconds**, headset tests OFF, including the LuaJIT gate (69 chunks).
  Receipt: `artifacts/unattended/owned-ui-full-offline-suite-20260909.log`.
  Accepted viewer SHA256 is unchanged. The 20-minute heartbeat remains active.
- Focused native UI diagnostic candidate is `a4ec84c`, PR #133, based directly
  on accepted native `23345e5`, in `D:/Projects/games-xr/Darktide VR UI diagnostics`.
  It changes five producer source/header files and related tests/docs only.
  The native DLL builds; five recovery/readback tests pass in 2.70 seconds,
  native loading passes in 0.50 seconds, and the integrated Python native-export
  checksum roundtrip passes. No deployment. Trial/rollback details and candidate
  hash are in that worktree's `docs/UI-READBACK-FOCUSED-2026-09-09.md`.
- The native diagnostic candidate now allocates/copies owned UI independently
  of tag submission (`ui_capture=1`, `ui_alpha=0`). Only complete same-pose pairs
  reach readback after capture-fence ordering; missing optional UI after replay
  stops does not stop generation. Seven related CTests pass in 3.31 seconds and
  the isolated native DLL/recovery builds pass. WARP rejects presentation; no
  live NVIDIA generation or blur acceptance is claimed. Integrated change is
  `de5e2d9`, PR #132; focused port is above. Request handling is `a307148`, PR #131.
- UI readback now keeps an explicitly requested UI capture pending until a
  matching overlay pair exists, and ends diagnostic replay once an attempt is
  consumed, including pre-staging failure. A regression failed before the fix.
  Five readback CTests pass in 2.80 seconds; the native DLL builds only in
  `build/xr-frame-stage-timing`. Accepted viewer hash is unchanged. Source audit
  confirms UI-input-disabled observed exports use `owned_ui=0` and do not arm
  NGX matching; the subsequent candidate above adds independent owned snapshots.
  This is instrumentation work, not a blur fix. Main focused handoff is
  `9700b19`, PR #130.
- Focused communication candidate: `77b1ef5`, PR #129, in
  `D:/Projects/games-xr/Darktide VR tactical overlay`, branch
  `codex/focused-communication-input-2026-09-09`. It extends `d1092af` on accepted
  Lua with unassigned wheel/PTT controls and required stick handling. Seven
  modules match main exactly. No popup/native/two-hand/physical-melee port.
  All 64 registered Lua fixtures pass; 56 chunks compile. A pre-existing ranged
  fixture lacked mock unit identity; the four-call fixture fix was ported without
  changing aim code. Cached stock navigation/wheel/chat checks pass with effects
  mocked. No deployment occurred. Focused handoff:
  `docs/COMMUNICATION-INPUT-FOCUSED-2026-09-09.md` in that worktree.
- An unassigned physical push-to-talk action now reaches stock ChatManager
  through the mapped gameplay hold. Observed route loss requires release before
  reuse, including brief UI/ImGui and cached-service loss between samples.
  A regression caught old-hold revival. Four focused checks pass in 0.13 seconds;
  69 chunks compile and the cached stock update passes with microphone mocked.
  Source only: no microphone, voice mode, installed settings or game changed.
  The preceding complete checkpoint is `7645871`, PR #127 (176/176 tests).
- Earlier combined Windows x64 offline Release suite: **176/176 pass in 53.22
  seconds**, headset tests OFF, at `332a17c`. Receipt:
  `artifacts/unattended/communication-popup-offline-suite-20260909.log`.
  All 68 Lua chunks compile. Accepted viewer SHA-256 remains
  `36D60D68E3FA70CDAF136920DAE6D044268B7A4A60851C7D2FEBF3788FA41815`;
  no game/viewer process was running at the checkpoint. The heartbeat is active.
- Popup active invalidation now discards pending publication and latches the
  hidden/handled decision through the eye pair. Copy-time invalidation starts no
  draw; draw-time invalidation cannot repeat animation for the other eye. A
  regression failed before the fix. Seven widget CTests pass in 0.09 seconds;
  68 chunks compile. Reentrant cleanup is `8367eed`, PR #125, based on PR #124.
- Popup capture now defers session destruction requested during a draw until
  pass/cache restoration completes. Copy-time retirement prevents another draw;
  standalone active destruction rejects before retirement so cleanup can retry.
  A regression failed before the fix. Seven widget CTests pass in 0.09 seconds,
  the added copy-time case passes, and all 68 Lua chunks compile. Still unloaded.
- Wheel ImGui cancellation is `d0a47ef`, PR #124, based on integrated PR #123.
- The integrated wheel candidate is `1da75b5`, PR #123, based on coordinator
  PR #122. A follow-up regression caught ImGui taking input between sample and
  deferred release; its independent ownership is now checked. Six focused
  communication/production-input CTests pass in 0.10 seconds; 68 chunks compile.
- Main now loads the communication-wheel input/coordinator candidate with an
  unassigned physical-button action. It prepares the binding profile before
  claiming the stick and forwards that claim to turning and action mapping.
  Remaps, owner/context/generation changes, tracking and null input invalidate
  deferred release. No deployment or live communication occurred. Worn sector
  selection, cancellation, pending taps and cursor behavior remain open.
  The full offline run passed 175/176 in 55.51 seconds; the old gameplay fixture
  lacked the new module loader. Its update now executes actual module loading
  and asserts production claim order, attack suppression and neutral rearm.
  All five affected input checks then pass in 0.14 seconds; 68 chunks compile.
  Receipt: `artifacts/unattended/communication-input-offline-suite-20260909.log`.
  The prior coordinator is `a905d08`, PR #122, based on handoff PR #121.
- The unloaded communication-wheel coordinator now owns stock HUD lifecycle,
  deferred exactly-once release, pending taps and neutral stick rearm. A blocked
  input between scheduling and callback execution cancels rather than sending.
  Five focused CTests pass in 0.08 seconds, 67 Lua chunks compile, and actual
  cached stock handling/close/release methods pass with communication mocked.
  Main loading and the physical binding remain open; see the wheel context doc.
- Focused spectator port: `d1092af`, PR #120, extends `45d065f` with only the
  spectator module, regression fixture and focused handoff. Pre-port blobs
  matched exactly. The focused fixture passes with its own bindings/context;
  49 Lua chunks compile. It remains an undeployed accepted-Lua-based candidate.
- A subsequent resume attempt brought the existing Virtual Desktop Quest
  activity forward and preserved its process. The fresh Ready smoke then saw
  an HMD/session, but submitted 0/600 frames (600 not rendered), so it still
  failed. No Darktide launch followed. Normal proximity automation was restored
  and Status queried in failure cleanup. Reports:
  `artifacts/unattended/performance-resume-ready-20260909.json` and local
  `performance-resume-{activity,proximity}-20260909.log`. Do not treat the
  available HMD or 117 Hz loop rate as renderable readiness. Continue offline
  until the streaming state changes; no app/runtime restart was attempted.
- Spectator next-player proxies now expire with their camera-update scope and
  input sample, recheck local owner/mode/routing and forward stock service
  members. A retained-proxy regression failed before the fix. Two input CTests
  pass in 0.06 seconds; 66 Lua chunks compile. Nested remote/local updates and
  errors cannot revive the old edge. The stock-only native fallback is retained.
- Marker metrics now include the third 3D position-offset component separately
  from draw layers. A regression failed before the fix; two CTests pass in 0.04
  seconds and 66 Lua chunks compile. Actual stock script forwarding is verified;
  native depth/occlusion effects and live popup sizing are not inferred.
- Capture sessions can now own their display through `create_display`, enforcing
  display -> pass-cache -> backend cleanup and preserving combined failures.
  Seven widget CTests pass in 0.10 seconds; 66 Lua chunks compile. HUD/world
  lifecycle hooks remain open; this candidate is still unloaded.
- Unloaded popup display owner now binds only the completed capture copy and
  submits the shared-plane quad through the accepted immediate GUI convention.
  It latches the first eye's geometry/hidden decision, rejects changed targets
  and owns material/GUI cleanup. Eight focused CTests pass in 0.12 seconds;
  66 Lua chunks compile. Complete bounds, lifecycle hooks, explicit accepted
  layer/occlusion behavior and native/worn rendering remain open.
- Latest live readiness attempt, 08:43 Brisbane: one physical Quest and VDXR
  were detected, but the fresh Ready smoke reported `hmd-unavailable`, skipped
  the session and exited 1. No Darktide launch or performance capture followed.
  The wrapper restored normal proximity automation and queried Status in its
  failure cleanup. Do not reuse the earlier successful Ready result. Reports:
  `artifacts/unattended/performance-{inventory,ready}-20260909.json`; proximity
  receipt is local `performance-proximity-20260909.log`. No VD restart occurred.
- VDXR trace analysis now excludes duplicate-start activity identities instead
  of shortening the span to the second start. A regression failed before the
  fix; two analyzer CTests pass in 0.23 seconds. Saved onset/later trace reports
  remain identical because neither has duplicate starts. No new trace ran.
- Unloaded push-to-talk input candidate scopes a hold to the stock ChatManager
  update and cached service. It preserves keyboard input and stock voice modes,
  expires retained/nested proxies and restores state on errors. Three focused
  CTests pass in 0.06 seconds; 65 Lua chunks compile. Actual cached stock update
  runs with microphone operations mocked. No binding or microphone was enabled;
  see `docs/PUSH-TO-TALK-INPUT.md` for remaining integration.
- Unloaded wheel context ownership now isolates pending tap state and cancels
  only the claimed context. Real cached stock close/input methods plus the
  gesture token fixture produce no delayed tag or deferred release after
  cancellation. Four focused CTests pass in 0.06 seconds; 64 Lua chunks compile.
  Integration remains open; see `docs/COMMUNICATION-WHEEL-CONTEXT.md`.
- Optional composition `--verify-native` now checks all six scene/final/UI
  images against completed native RGBA checksums before producing a report.
  Legacy capture analysis stays explicitly unverified. Nine Python fixtures and
  the real native-export roundtrip pass with the other focused DLSS tests
  (four CTests, 1.49 seconds). This establishes file/export consistency only.
- DLSS composition diagnostics now separate opaque/translucent UI and nearby/
  distant transparent pixels. Seven Python fixtures and four focused CTests
  pass (1.40 seconds). The older pose-8589 sample has no above-tolerance
  residuals in any region, including 44,438/43,289 nearby pixels; this is not
  temporal blur or worn acceptance. The checker explicitly leaves capture
  identity unverified unless the subsequent strict mode succeeds.
  See `docs/DLSS-UI-DETAIL.md`.
- Binding/turning consumers now expose optional exclusive-stick gates. Claims
  cancel directional actions without synthetic release edges, preserve physical
  aliases and block turning through neutral rearm. Seven focused tests pass in
  0.24 seconds; 63 Lua chunks compile. Existing callers omit the new argument.
  The future wheel route must establish one claim before both consumers run;
  current main still applies turning first. Nothing is wired or deployed yet.
- Unloaded communication navigation adapter supplies private stick/cursor
  vectors within a token-checked callback scope, without changing global gamepad
  mode or emitting buttons/events. Eighty actual cached stock presentation cases
  preserve selection across viewport/scale/input modes. Three focused tests pass
  in 0.10 seconds and 63 Lua chunks compile. Binding, owned HUD cancellation,
  deferred release and gameplay-stick suppression still need integration.
- Packaging audit: `source_revision` describes the packaging checkout; existing
  copied DLL/EXE build sources are not established by it. New manifests explicitly
  label that scope and `binary_source_provenance=not_recorded`. Legacy manifests
  retain the same limitation. No package was built from the experimental stack.
- Billboard candidate builds now exclusively claim a new output directory.
  Existing and partial outputs are preserved, including a competing directory
  creation during input validation. Four mocked workflow tests pass through the
  new `billboard_build_output` CTest (0.25 seconds). No staged shader changed;
  these tests do not execute DXC or establish shader equivalence.
- Focused UI input lifetime port: `45d065f`, PR #103, extends the isolated overlay
  candidate `8eab44e` with only its input module, regression fixture and handoff.
  Five focused fixtures and the actual stock overlay update pass; 49 Lua chunks
  compile. This is the newer focused candidate for a deliberate input trial,
  still undeployed and separate from the main popup/native experiments.
- Tag and tactical-overlay proxies now use unique HUD-handler scopes and input
  samples too. Regression coverage catches retained tag injection and same-HUD
  overlay proxy revival, nested remote HUD scope leakage, handler failures and
  player/sample changes. Five focused input tests pass in 0.08 seconds; the
  actual stock overlay fixture and 62-chunk Lua gate pass. Undeployed.
- Inventory input proxies now expire with their original handler scope and input
  sample, including failures, routing loss and player replacement. A regression
  reproduced the prior stale injection; five focused input tests pass in 0.08
  seconds and all 62 Lua chunks compile. Keyboard input remains independent.
  This source fix is undeployed and is not in the focused overlay PR #92.
- Capture sessions now save their own pivot and exact pixel crop. The unloaded
  widget-quad descriptor derives readable texture mapping and shared world
  geometry from that completed image, avoiding use of the next frame's layout.
  Sixteen scale/anchor cases and an actual capture-session transition verify the
  mapping; seven focused tests pass in 0.42 seconds and 62 Lua chunks compile.
  Native partial-texture drawing, complete bounds and layer/occlusion policy
  remain unverified. The accepted viewer SHA256 is unchanged.
- Complete-widget capture admission now includes immediate vector icons,
  pictures, rotated icons and rotated rectangles. Actual cached stock pass
  functions verify capture-renderer arguments, stock pivot defaults and source
  cache restoration without retained requests. Five capture/lifetime tests pass
  in 0.08 seconds; all 61 Lua chunks compile. Beacon-style vector content is no
  longer rejected solely by pass type, but custom logic, complete bounds and live
  integration remain open. The module is still unloaded and undeployed.
- Pickup/marker diagnostics now compare raw 2D/3D draw layers and renderer
  start-layer offsets separately from shape and spatial position. A regression
  failed before the fix; actual cached stock bitmap functions verify observation
  before 2D position mutation and use of the separate 3D layer scalar. The two
  focused marker tests pass in 0.18 seconds and all 61 Lua chunks compile.
  This diagnostic remains undeployed; it does not establish rendered bounds or
  fix popup sizing. See `docs/MARKER-PRIMITIVE-METRICS.md` for report semantics.
- The user's DLSS blur **around HUD items** is explicit in the active todo.
  Offline investigation confirmed the opaque contrast tool excludes surrounding
  world pixels and translucent boundaries. `docs/DLSS-UI-DETAIL.md` now defines
  separate region observations and controlled SR/HUD comparisons, with frame
  generation held fixed first. No new capture or blur acceptance is claimed.
  This documentation-only follow-up passed `git diff --check`; the existing
  code validation at that checkpoint was 166/166.
- The latest full isolated Windows x64 Release checkpoint passes **174/174 in 56.45
  seconds**, including push-to-talk, popup-display/lifetime and marker-offset
  follow-ups. Main Lua compiles 66 chunks. Receipt:
  `artifacts/unattended/widget-display-offline-suite-20260909.log`.
- All 49 tracked Lua/mod files match accepted `3341afb` after CRLF normalization:
  17 match byte-for-byte, 32 differ only in line endings, zero content mismatches.
  Receipt: `artifacts/unattended/accepted-lua-integrity-20260909.json`. No sync ran;
  the accepted viewer SHA256 remains unchanged.
  This is offline validation; headset
  tests were disabled.
- Duplicate ADB connections are proven aliases of one Quest. Default Ready
  subsequently passed with accepted VDXR/viewer: 600/600 submitted frames, zero
  skipped. Proximity automation was restored and game/viewer are closed. Recheck
  before a new unattended session; this is not worn/mod visual acceptance.
- Popup capture resources, independent passes, owned material state, draw-only
  geometry, lifetime coordination and pixel-aligned crop bounds are offline
  candidates. Complete raster bounds, all primitive families, display/layer policy
  and live integration are still required. No popup fix is installed.
- Tactical-overlay hold has a focused, undeployed port at `8eab44e`, PR #92,
  based on accepted Lua `3341afb`. Its 49 chunks and eight focused tests pass.
  Existing saved-layout routes are preserved; the new action is unassigned.
  Use that focused port for any deliberate trial, not the full integrated branch.
- Communication-wheel gesture ownership is an unloaded foundation. Stock HUD
  cancellation/navigation integration and wheel/voice/chat routes remain open.

## Review map

PRs #75–91 form an ordered source stack. They are not a release or authorization
to replace the installed mixed runtime. PR #92 is independently based on the
accepted focused Lua branch.

| Review | Change | Commit |
| --- | --- | --- |
| #75 | RGB versus alpha-only billboard readback | `75952fd` |
| #76–78 | Capture resources, renderer pass and detached geometry | `c1a9348`, `9d6874c`, `154ed51` |
| #79 | Marker transform depth and rotation pivots | `2d78b85` |
| #80–81 | GUI-owned capture caches and session coordination | `f8c2613`, `d6a0e3c` |
| #82–83 | Saved controls and stock X-overlap precedence | `9b84b20`, `550f61a` |
| #84 | Non-mutating inventory when selection is unavailable | `de4d808` |
| #85–86 | Stock wheel contract and pure gesture ownership | `a66211c`, `b23ffe7` |
| #87–88 | Capture pixel alignment and text layout measurements | `4fd3c19`, `3431fa6` |
| #89 | Ranged fixture identity and full offline validation | `86c9b45` |
| #90 | Verified Quest aliases and actual Ready/restoration | `45de197`, `baed9f9` |
| #91 | Integrated unassigned tactical-overlay hold | `d6d3427` |
| #92 | Focused overlay port from accepted Lua | `8eab44e` |
| #93 | Workday review index | `58e71f0` |
| #94 | Read-only weapon/item binding overlap report | `de83632` |
| #95 | Proximity broadcast and power result checks | `0ee929b` |
| #96 | Preserve combined watcher/restoration failures | `12dfcff` |
| #97 | Explicit surrounding-HUD DLSS blur investigation | `2ac8bc8` |
| #98 | Marker draw-layer input measurements | `6031d09` |
| #99 | Immediate vector/rotated-rectangle capture admission | `ebc2178` |
| #100 | Captured pivot/crop and shared popup quad | `82ab0d3` |
| #101 | Inventory request proxy scope and sample lifetime | `94a85e7` |
| #102 | Tag/overlay request proxy scope and sample lifetime | `fbf9c22` |
| #103 | Focused input lifetime port on overlay baseline | `45d065f` |
| #104 | Exclusive billboard builder output ownership | `f12870a` |
| #105 | Package revision scope and binary provenance limitation | `75b1cb9` |
| #106 | Scoped private wheel navigation vectors | `42f2a0b` |
| #107 | Exclusive stick claims and neutral rearm | `f32cb49` |
| #108 | Composition residual regions around UI coverage | `06fc013` |
| #109 | Optional six-image native composition proof | `f358c25` |
| #110 | Owned wheel context and pending cancellation | `3d99212` |
| #111 | Full 172-test offline checkpoint | `9ed0207` |
| #112 | Scoped candidate push-to-talk hold | `3669d67` |
| #113 | Exclude ambiguous VDXR timing identities | `c167d02` |
| #114 | Latest unavailable-HMD readiness and cleanup | `18eb7a6` |
| #115 | Immediate completed-widget display ownership | `ec6c2b8` |
| #116 | Session-owned display/cache/backend cleanup | `47d46b4` |
| #117 | Separate third 3D position-offset metrics | `e958c42` |
| #118 | Full 174-test checkpoint and accepted-install integrity | `aa40151` |
| #119 | Camera-update-scoped spectator input edges | `da1f7df` |
| #120 | Focused spectator lifetime port onto accepted Lua candidate | `d1092af` |
| #121 | Focused spectator and non-renderable VD resume handoff | `8a3f49f` |
| #122 | Owned wheel HUD lifecycle and deferred release | `a905d08` |
| #123 | Physical wheel binding and gameplay preclaim integration | `1da75b5` |
| #124 | Cancel deferred wheel release on ImGui ownership | `d0a47ef` |
| #125 | Defer popup cleanup until active draw scopes unwind | `8367eed` |
| #126 | Discard captures invalidated during copy/draw | `332a17c` |
| #127 | Full combined 176-test offline checkpoint | `7645871` |
| #128 | Physical PTT binding and persistent cancellation | `796327b` |
| #129 | Accepted-Lua-based wheel/PTT focused candidate | `77b1ef5` |

Review URLs use the repository's Forgejo `/pulls/<number>` route. The detailed
sections below record evidence chronologically; later results supersede earlier
counts, ambiguous-device observations and next-step wording.

## Initial billboard diagnostic checkpoint

The user resumed development with a 20-minute heartbeat in this task. The existing
heartbeat was reactivated and attached here; the older duplicate remains paused.
Follow the [next-session todo](../phase1/todo-2026-09-09.md) and preserve the
mixed accepted deployment in the [night handoff](2026-09-08-night.md).

Branch: `codex/billboard-visible-delta-2026-09-09`.

The readback analyzer previously counted all attachment channels together. An
alpha-only write therefore produced a changed-pixel report even if RGB was
identical. Reports now additionally expose RGB, alpha and alpha-only changed
pixel counts. Existing aggregate metrics retain their meaning; R11 HDR has no
alpha and reports null for alpha metrics. These distinctions help interpret
draw attribution, but neither RGB nor alpha changes alone prove smoke ownership.
Alpha may affect later compositing even when the current RGB is unchanged.

Windows offline validation:

```powershell
python tools/stereo/test-analyze-billboard-readback.py
git diff --check
```

Both pass. Fixtures include alpha-only RGBA and BGRA changes alongside existing
HDR, padding, bounds and truncation checks. No Lua or native binaries changed;
no deployment, game launch or headset experiment was performed.

Next: obtain the requested specific worn observation identifying the scene and
head-turn behavior, then continue bounded draw attribution using the isolated
billboard diagnostics. Ready preflight is required before live work. Popup
capture integration remains second; physical melee stays paused.

## Continued work while the user is away

The user explicitly requested continued work until they get home. The heartbeat
now preserves that stopping condition and continues independent offline items
when a live observation is unavailable.

Reanalysis of the three saved 8 September captures confirms zero RGB changes
for the sampled c403 draw, 179986 for e18a and 74388 for fe640. All three are R11
HDR with no alpha channel, so the new channel metrics do not alter their earlier
attribution. Receipt: ignored `artifacts/unattended/billboard-readback-channels-20260909/analysis.json`.
No fresh scene or orientation acceptance is inferred.

Branch `codex/widget-capture-backend-2026-09-09` adds the offline resource backend
described in [capture lifetime](../WIDGET-SURFACE-LIFETIME.md). It is not loaded or
deployed. Allocation failures (nil and exceptions at every stage), first-frame
hiding, own-world submission, stale submission rejection, distinct copying,
draw/copy errors and cleanup continuation pass focused fixtures.

Validation: rebuilt the pinned validator with `tools/lua/build-luajit.ps1`;
`tools/stereo/test-darktide-lua-source.ps1` compiles 57 chunks. Configured
`build/xr-frame-stage-timing` with headset tests OFF and ran Release CTest
`^(widget_capture|widget_surface|marker_plane|marker_gui|marker_metrics)$`:
5/5 pass in 0.10 seconds. `git diff --check` passes. CMake/CTest were absent
from PATH, so used the executables under Visual Studio 2022's
`Common7/IDE/CommonExtensions/Microsoft/CMake/CMake/bin/`. No native build or
installed file was changed. Next independent work is capture-pass isolation
and complete bounds/scenegraph integration; worn billboard comparison remains
the first live priority.

## Capture pass isolation

Branch `codex/widget-capture-pass-2026-09-09` adds a dedicated renderer-pass
helper. It copies top-level render settings, rejects nesting, restores the
original scenegraph queue and the fields stock begin/end mutate, and retires
failed capture work. A draw error remains visible even if end_pass also fails.
The caller still owns the translated capture scenegraph and nested settings.

Pinned LuaJIT compiles 57 chunks; Release `widget_capture|widget_surface` pass
2/2 in 0.04 seconds. Fixtures exercise partial begin, draw, end, combined errors
and nesting, including original queue identity and nil-valued field restoration.
No live hook or installed file changed. The preceding checkpoints are pushed as
Forgejo PR #75 (readback channels) and #76 (capture resources).

## Detached capture geometry

Pass isolation is pushed as PR #77. Branch
`codex/widget-scenegraph-snapshot-2026-09-09` adds a draw-only scenegraph snapshot:
resolved source sizes and translated, independently owned positions, without
engine cache pointers. This prevents root/fit nodes from resizing to the smaller
capture viewport. It supports the checked UIWidget draw getters, not scenegraph
updates or arbitrary custom callbacks.

LuaJIT compiles 58 chunks. Four Release CTests (`widget_capture`, `widget_surface`,
`widget_scenegraph`, `marker_plane`) pass in 0.12 seconds. The optional fixture
also executes actual cached stock `ui_scenegraph.lua` get_size/world_position
functions for 28 sizing-mode/scale combinations, all passing:

```powershell
build/dependencies/luajit/src/luajit.exe tests/tooling/test-widget-scenegraph.lua mods/darktidevr_stereo_probe/scripts/mods/darktidevr_stereo_probe/darktidevr_widget_scenegraph.lua artifacts/vendor/Darktide-Source-Code/scripts/managers/ui/ui_scenegraph.lua
```

The snapshot is not a complete raster-bounds policy or an installed popup fix.
Full primitive extents, retained/material ownership and final shared-plane
display remain open. The accepted runtime remains preserved.

## Complete transform input measurements

Snapshot review is PR #78. Branch `codex/marker-transform-metrics-2026-09-09`
fixes the measurement scope's omission of depth-axis basis/translation and
rotated rectangle/vector-icon pivots. A new fixture fails against the prior
module with `depth basis change was missed`, then passes with the correction.
It also verifies depth translation and pivot-only differences. Missing transform
components now mark extraction incomplete instead of disappearing from the row.

`marker_metrics` and `marker_gui` Release CTests pass 2/2 in 0.04 seconds; LuaJIT
compiles 58 chunks. No deployment or actual popup measurement took place.

## Capture material ownership

Transform metrics are PR #79. Branch `codex/widget-capture-materials-2026-09-09`
adds separate immediate-pass caches, preserving stock GUI-owned materials and
rejecting unsupported/retained paths before drawing. The capture cache persists
across draws and is retired on removed/retyped passes. Source pass.data bindings
restore after exceptions. Destroy capture caches before their renderer.

Six focused Release CTests pass in 0.14 seconds; LuaJIT compiles 59 chunks. The
optional fixture runs the actual cached stock texture, texture_uv and
rotated_texture pass code, including material value changes and destruction:

```powershell
build/dependencies/luajit/src/luajit.exe tests/tooling/test-widget-capture-state.lua mods/darktidevr_stereo_probe/scripts/mods/darktidevr_stereo_probe/darktidevr_widget_capture_state.lua artifacts/vendor/Darktide-Source-Code/scripts/managers/ui/ui_passes.lua
```

Source and capture ownership assertions pass. Custom logic, retained paths,
complete primitive bounds and installed draw/display hooks remain open. No
visual acceptance or game deployment is claimed.

## Capture session assembly

Material isolation is PR #80. Branch `codex/widget-capture-session-2026-09-09`
connects the actual lifetime, pass-cache and geometry modules to the backend.
It latches rejected first-eye decisions as well as accepted ones, keeps copied
bounds with their submitted image, rejects inconsistent scale/oversized bounds,
and orders material cleanup before renderer destruction. Target replacement,
hide/show, time rollback and draw/copy failures are covered without re-drawing
stock animation callbacks.

Six focused Release CTests pass in 0.09 seconds; LuaJIT compiles 60 chunks;
`git diff --check` passes. This is an offline integration boundary requiring
complete supplied raster bounds, not a live widget hook or sizing correction.
Full bounds/material/primitive coverage and display policy remain necessary
before focused deployment; billboard worn comparison remains first live work.

## Saved mission controls refreshed

Session assembly is PR #81. Branch `codex/current-mission-binding-audit-2026-09-09`
records a read-only refresh in the [control audit](../MISSION-CONTROL-AUDIT-2026-09-08.md).
Both current integrated and focused accepted mappers resolve the saved settings
identically: left grip ability, B Blitz, Y crouch, stick up quick wield and stick
down interact/reload. X selects both device and carried-item cycle; an isolated
focused-mapper press confirms both semantic bits. No settings were changed.
Direct stim/pocketable selection is unassigned, but stock cycle rules can select
both pocketable sizes, so lack of direct binding is not proof of no access.

The actual current layout supersedes the earlier proposed unused-stick mappings.
Gameplay precedence of the X overlap remains unverified. Validation was read-only
Lua execution against the saved normalized action values, with no game input.

## Stock wield overlap precedence

The mapping refresh is PR #82. Branch `codex/mission-wield-overlap-evidence-2026-09-09`
adds an optional cached-source fixture that executes the stock parser with real
VR mapper output. Reversing the two simultaneous selectors in the input array
reverses the winner; independently assigned selectors remain unambiguous. Both
integrated and focused mapper runs pass. The stock common wield list derives
from table iteration, without an explicit device/cycle priority contract.

```powershell
build/dependencies/luajit/src/luajit.exe tests/tooling/test-wield-overlap-stock-contract.lua artifacts/vendor/Darktide-Source-Code mods/darktidevr_stereo_probe/scripts/mods/darktidevr_stereo_probe/darktidevr_controller_bindings.lua
```

No bindings or runtime code changed. The fixture covers raw selection, not
equipment eligibility, full action queue, mission execution or network behavior.

The existing Quest recordings were also sampled offline into ignored contact
sheets under `artifacts/unattended/recording-review-20260909/`. Coarse samples
show mission combat/haze but do not provide paired-eye popup measurements or
a controlled orientation comparison. Their earlier UI presentation does not
reopen the later evening's accepted corrections. No new visual acceptance is
claimed from these single-view recordings.

## Readiness inventory with ambiguous devices

Wield evidence is PR #83. The first read-only inventory attempt stopped at two
authorized ADB devices. Branch `codex/readiness-inventory-ambiguity-2026-09-09`
lets Inventory continue with explicit missing/ambiguous/query-failed status
without selecting a device, querying its power, or applying proximity changes.
Ready and RunXrSmoke retain the single-authorized-Quest gate before mutation.

The corrected real Inventory run reports two devices, ambiguous selection,
one Streamer, Darktide closed, readiness_verified=false and no proximity apply.
Ignored receipts: `workday-inventory-fixed-20260909.{json,log}`. Device identifiers
are not recorded in source. The accepted viewer SHA256 still matches the night
handoff's `36D60D68...18FA41815` value.

The fixture executes the actual preflight selection block with mocked ADB calls:
13 cases cover missing/ambiguous/selected/non-Quest targets, failed device/model
queries, non-mutating Inventory and strict Ready/RunXrSmoke behavior. Windows
PowerShell 5.1 passes; Release `preflight_device_inventory`, `validate-xr-readiness`
and `xr_smoke_timeout` pass 3/3 in 3.74 seconds. No XR session was started.

## Communication-wheel ownership evidence

Inventory correction is PR #84. Branch
`codex/communication-wheel-stock-contract-2026-09-09` executes the stock wheel
handler offline with mocked effects. It verifies delayed/instant opening, cursor
ownership, close delay, duplicate pending-stop scheduling, blocked-input release
and pending single-tap delivery. The test passes with the pinned LuaJIT:

```powershell
build/dependencies/luajit/src/luajit.exe tests/tooling/test-communication-wheel-stock-contract.lua artifacts/vendor/Darktide-Source-Code
```

The [control audit](../MISSION-CONTROL-AUDIT-2026-09-08.md) now records the required
exclusive stick ownership, fresh navigation vector, once-per-frame execution and
explicit cancellation boundary. No input route, saved binding or installed file
changed. The active todo introduction now reflects the user's resumed heartbeat
and until-home authorization instead of the superseded overnight stop.

## Offline communication gesture boundary

Stock contract evidence is PR #85. Branch
`codex/communication-wheel-gesture-2026-09-09` adds an unloaded pure gesture
component. It reserves the stick through deliberate release/closing and neutral
rearm, caches each frame decision, and permits a deferred selection token once.
Tracking/routing loss, owner change, malformed input, backwards frames and
explicit cancellation revoke pending selection. No binding or runtime hook is
added. The stock adapter, cancellation of owned stock context, navigation and
complete gameplay stick consumption remain work before any live deployment.

Validation: Release `communication_gesture`, `gameplay_ui_input` and
`controller_bindings` pass 3/3 in 0.08 seconds. The pinned LuaJIT source gate
compiles 61 chunks. No engine, chat/voice/network or headset calls were made.

## Capture bounds preserve pixel alignment

Gesture ownership is PR #86. Branch
`codex/widget-capture-pixel-bounds-2026-09-09` rounds supplied complete popup bounds
outward to whole capture pixels before checking target capacity or translating
the owned scenegraph. This preserves source pixel-snap phase. A 512-unit width
at scale one starting at x=0.25 requires 513 capture pixels and is now rejected
by a 512-pixel target rather than clipping silently. Metadata carries the same
expanded bounds as the captured image; caller bounds are unchanged.

Tests cover fractional/negative origins, scales 0.5/1/2, edge capacity and
overflow/precision loss. Release `widget_capture_session`, `widget_surface`,
`widget_scenegraph` and `widget_capture` pass 4/4 in 0.06 seconds; LuaJIT compiles
61 chunks. This corrects the unloaded coordinator, not the missing whole-widget
raster measurement or installed pickup appearance.

## Text layout measurement for popup diagnosis

Pixel alignment is PR #87. Branch `codex/marker-text-layout-metrics-2026-09-09`
extends the bounded, opt-in marker observer with stock maximum text extents and
minimum layout origin at the final script-call font/box scale. A glyph-origin
change is now visible even if font and layout-box sizes match. 2D/3D text use
their actual options argument; no text/resource content is logged. Failed layout
queries cannot stop real drawing, and default-off performs no queries.

`marker_metrics` and `marker_gui` pass 2/2 in 0.05 seconds; the optional cached
stock `UIRenderer.text_size` contract passes; LuaJIT compiles 61 chunks. The
candidate is not deployed. Layout API output still does not establish complete
shaded raster bounds, shadow coverage or equal apparent size in the headset.

## Broader offline validation and ranged fixture correction

Text layout is PR #88. Branch `codex/ranged-aim-owner-fixture-2026-09-09` repairs
four stale ranged-aim fixture objects that omitted `_unit`. The production hook
already requires the current human player's identity to protect against local
bot updates. The fixture now supplies human/remote ownership as intended; the
runtime guard is unchanged. Ranged aim, reticle owner and online reticle checks
pass 3/3, including the separate human/bot ordering test.

The first wider run exposed unbuilt native test executables in the isolated
`build/xr-frame-stage-timing` directory and this fixture failure. Test targets
and their native capture/bootstrap dependencies were then built in that isolated
directory, with headset tests OFF. The complete Windows x64 Release suite passes
**165/165 in 54.40 seconds**. Receipts are ignored:
`workday-offline-test-build-20260909.log` and
`workday-offline-suite-built-20260909.log` under `artifacts/unattended/`.
No build output was synced or deployed. The accepted viewer under
`build/windows-vs2022` still has SHA256 `36D60D68...18FA41815`.

Additional read-only ADB model and hardware-identity queries establish that the
two authorized connections are aliases for the same Quest 3. No device identity
is recorded here and no transport was disconnected. The existing preflight
still treats them as ambiguous until explicit duplicate-transport handling is
implemented and validated; this observation itself is not Ready certification.

## One Quest with duplicate authorized connections

The fixture correction is PR #89. Branch
`codex/quest-duplicate-transport-readiness-2026-09-09` adds a shared read-only
resolver for preflight and default proximity selection. Multiple connections
are accepted only when every model and non-placeholder hardware identity agrees.
The direct serial connection is preferred; equivalent network-only aliases use
stable ordering. Different Quest identities, missing/failed queries, non-Quest
targets and conflicting models are rejected without selection or mutation.
Neither connection is disconnected and device identities are not in source.

Twenty-seven Windows PowerShell 5.1 cases pass. The focused readiness/timeout
checks pass 3/3 in 3.77 seconds; package integrity, Lua invariants, watcher cleanup
and selection pass 4/4 in 2.15 seconds. The resolver is included in the runtime
package manifest. Actual Inventory reports two connections, one physical Quest,
duplicate resolution true, Awake and display suspend blocker held. The standalone
proximity Status command also succeeds through default selection. No proximity
change or Ready certification has been performed at this checkpoint.

Ignored receipts: `workday-inventory-deduplicated-20260909.{json,log}` and
`workday-proximity-status-deduplicated-20260909.log` under `artifacts/unattended/`.

Default **Ready** preflight subsequently passes on this source checkpoint using
the unchanged accepted viewer: VirtualDesktopXR, 600/600 submitted frames,
zero not-rendered frames, 116.524 submit Hz. Quest proximity Disable then Status
preceded the preflight; normal automation was restored with Enable then Status
in a finally block, with broadcast result zero. Game and viewer are closed.
The accepted viewer checksum is still unchanged. Receipt:
`artifacts/unattended/workday-ready-deduplicated-20260909.json` (ignored).
This readiness session is complete; recheck before a new unattended session.
It does not establish mod initialization, popup appearance or worn billboard
acceptance, and no candidate runtime was deployed.

## Tactical overlay hold access in source

Duplicate-transport readiness is PR #90. Branch
`codex/tactical-overlay-controller-route-2026-09-09` adds the unassigned
**Hold tactical overlay** action. The gameplay UI adapter scopes hold input to
the current human overlay update through the existing InputManager hook. No
second central hook, keyboard simulation or direct overlay activation is added.
Menu/inventory precedence, null services, owner cancellation, nested scopes and
exception restoration are covered; held input uses the existing neutral rearm.

Five focused input/binding/prompt/invariant tests pass in 0.51 seconds, and
options-layout/gameplay-UI-ownership pass 2/2 in 0.04 seconds. LuaJIT compiles
61 chunks. The optional actual-stock-update fixture passes activation, release,
held-frame deduplication and stock menu blocking. No settings or installed Lua
changed. Full overlay navigation, input-taking subpanels and worn readability
remain live acceptance work; deploy only a focused reviewed port onto the
accepted Lua baseline, not this branch's complete integrated runtime.

## Focused tactical-overlay port

Integrated overlay work is PR #91. The separate
`codex/focused-tactical-overlay-2026-09-09` worktree starts at accepted `3341afb`
and contains only the six Lua-file edits and three related fixture updates plus
its handoff document. Commit `8eab44e` is PR #92. The only change to the main
integration Lua file passes the already-computed held mask to the UI sampler.

All 49 focused Lua chunks compile using the rebuilt pinned validator. Eight
fixtures pass against that worktree: menu input, turning, gameplay UI input,
crosshair feedback, options layout, controller bindings, gameplay UI ownership
and controller prompts. The actual-stock overlay update fixture also passes.
The earlier normalized saved-layout snapshot produces identical routes for all
existing actions; overlay is unassigned in both profiles. No native build or
deployment occurred. Details are in the focused branch's
`docs/TACTICAL-OVERLAY-HOLD-2026-09-09.md`; ignored receipt:
`artifacts/unattended/tactical-overlay-focused-tests-20260909.json`.

## Read-only binding conflict report

The review index is PR #93. Branch `codex/wield-binding-conflicts-2026-09-09`
adds `wield_conflicts()` and optional `/dtvr_binding_conflicts` to the integrated
mapper. The report uses current resolved controls and profile, covering the
stock common wield list's quick switch and four item/device selectors. It
preserves mappings, normal combined actions and input state; turning-owned
horizontal directions are excluded. Returned report tables are independent.

The earlier saved-layout snapshot reports X device/cycle overlap in both combat
and hub. Fixtures pass current-profile overrides, remaps, aliases, quick-switch
overlap and turning ownership. Binding tests pass, three prompt/UI/layout checks
pass, and the pinned gate compiles 61 chunks. This is an undeployed integrated
diagnostic; focused overlay PR #92 is unchanged.

## Proximity command result validation

Binding conflict reporting is PR #94. Branch
`codex/quest-proximity-result-validation-2026-09-09` makes standalone Disable and
Enable require the successful Android broadcast receipt as well as ADB exit zero.
Malformed/missing/nonzero results cannot announce apply/restore success. Status
rejects failed power queries. Preflight's receipt match also rejects a leading
zero on a nonzero result. No real ADB command was needed for this change.

Twelve standalone command cases use a temporary mock client; preflight selection
now covers 29 cases. The watcher fixture emits realistic success receipts and
verifies that exit-zero/result-one restoration is recorded as failed. These
three checks plus package integrity pass 4/4 in 2.20 seconds. `git diff --check`
passes. The earlier actual successful Ready/restoration evidence is unchanged.

## Watcher failures retain both causes

Proximity receipt validation is PR #95. Branch
`codex/quest-watcher-combined-failure-2026-09-09` fixes a reproduced double-failure
case: a watcher loop fault followed by a restoration failure previously replaced
the original error. Both causes now appear in the message and are retained as
inner exceptions. Cleanup still attempts every tracked device once; a lone loop
fault keeps its original error and a lone restore failure keeps its existing
failure report. The mock-only watcher/proximity/invariant checks pass 3/3 in
1.68 seconds. No headset state was changed.

The subsequent complete isolated Windows x64 Release run passes **166/166 in
51.71 seconds**, with headset tests OFF. Receipt:
`artifacts/unattended/workday-offline-suite-followup-20260909.log` (ignored).
All current integrated source changes are committed/pushed; the focused overlay
worktree is clean and undeployed. The pre-existing untracked user image in the
main checkout remains untouched. The 20-minute heartbeat remains active in this
task with the user's return home as its stopping condition.
