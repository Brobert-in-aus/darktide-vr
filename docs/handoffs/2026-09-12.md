# Handover, 12 September 2026: remote-server missions

Continues [11 September end of day](2026-09-11-end-of-day.md). The user's
instruction at the end of that night: remote-server mission readiness, after
confirming full functionality in a SoloPlay (local authority) mission.

## Done today

**Remote-server mission admission** ([REMOTE-MISSION-ADMISSION-2026-09-12.md](../REMOTE-MISSION-ADMISSION-2026-09-12.md)).
The gameplay-context policy now distinguishes an established remote
authority from an unknown one (`Context.authority`: true, false, nil) and
admits mission modes under a remote dedicated server to presentation and
stock input (`body_mode`) while leaving the local-authority aim route
(`aim_mode`, `local_mission`) unchanged. The online rules admit missions under
either established authority, so a remote mission uses the same stock-input
route as Psykhanium's online rules and SoloPlay missions: dominant-hand
yaw/pitch/roll and the movement vector written into the stock fixed-frame
input cache that prediction, resend, replay and the server read, with the
server's own firing origin, actions and damage. Every local-authority
override stays off because `is_controller_aim_mode` is false whenever the
online rules are enabled. New mod setting `remote_mission_input` (default
on, read per query) withdraws it without a relaunch. The admission record
names the authority: `DARKTIDEVR_ONLINE_RULES ... authority=remote`.

**Tests.** `gameplay_context` and `online_rules` cover the new admission,
unestablished sessions, the settings reader, and the unchanged local route.
The optional stock-source fixtures `test-online-input-stock-contract.lua`
(with the adapter) and `test-online-rules-stock-contract.lua` (range and
mission modes) had rotted since the adapter began deriving yaw/pitch/roll
from forward/up vectors; their engine-rotation stubs now supply those axes
and both pass against `_downloads/Darktide-Source-Code`. Two unrelated test
regressions found on the way were fixed: `visual_settings` (its main-hook
stub lacked yesterday's `update_dlss_quality_test`) and
`launcher_cleanup_continuation` (the new ring-flag restore block ran before
its variable existed under strict mode). The deployment tree was built for
every target and the complete offline suite passes: 234 of 234.

**Deployed** by the ordinary sync at about 10:20, transaction
`artifacts/deployment-backups/deployment-830a70dd1ee24082b2ae33ed40b44621`.
Installed: native `5B7D5B9B...` (unchanged from the 11 September bundle),
bootstrap `E7ED3929...`, viewer `1587F172...`, main Lua `4F273B2D...`,
`darktidevr_gameplay_context.lua` `CF1ABFA6...`,
`darktidevr_online_rules.lua` `C6B32F0D...`. Rollback:
`tools/stereo/restore-darktide-vr-deployment.ps1 -Manifest artifacts/deployment-backups/deployment-830a70dd1ee24082b2ae33ed40b44621/manifest.json`,
or turn the `remote_mission_input` setting off in the mod options for a
view-only remote mission without touching files.

## Worn check on a real mission server: passed (22:54 to 22:59)

The user reports hands and controls worked in a dedicated-server mission
(`mission_fm_resurgence`, `coop_complete_objective`). The logs agree:
`DARKTIDEVR_ONLINE_RULES ... authority=remote`, the first authored input
frame with `aim=dominant_hand movement=stock_packed replay=stock_history`,
166 gameplay deliveries, 5 melee wrist-roll records, no
`presentation_blocked`, no `input_fallback`, ring paused/resumed cleanly
around the mission load and the hub return, FG published throughout. The
exit code 1 during the hub reload is the user's force-quit key (Super+F4),
not a crash; no crash record exists. Still
open: per-action-family coverage (throws, interactions, downing, rescue,
spectating, extraction, reconnect), the traversal-aim policy, and any
rubber-banding observation over longer play. Procedure for those runs:
Ready preflight, then

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "artifacts/unattended/home-remote-mission-20260912/launch.ps1"
```

which runs the normal session with what is installed and, after exit, keeps
`console-decisive-lines.log`, `ring-phases.log` and the health log next to
the script and prints the counts of online-rules records, gameplay
deliveries, presentation-blocked lines and remote admissions. In the
mission check: hands and weapon visible and following the controllers;
movement and turning; firing where the dominant hand points; reload, dodge,
sprint, interact; a throw; button melee; the view staying with the headset
while the gun points elsewhere; and whether anything rubber-bands (a server
correction fighting the client). Hands/weapons/input absent with a
`presentation_blocked` line would mean the session's authority was not
established (check the `DARKTIDEVR_ONLINE_RULES` record); absent without
that line points at the setting.

Known limits carried from the online-rules design: ledge discovery follows
the sent hand aim; no independent weapon origins or two authoritative hand
poses; incoming correction convergence, other players' husks and official
server acceptance are only observable worn. The lifecycle checks (downing,
rescue, spectating, death, reconnect, extraction, return to hub) remain in
[MISSION-READINESS.md](../MISSION-READINESS.md).

## Also still open from 11 September

- Worn acceptance of the performance bundle in ordinary hub play (the
  mission and the quality switches are confirmed).
- The unattributed Present-hook CPU spikes in the hub spin
  ([PERFORMANCE-BUNDLE-2026-09-11.md](../PERFORMANCE-BUNDLE-2026-09-11.md)).
- DLSS frame-generation blur around HUD objects and cylindrical billboarding,
  queued behind the mission work in the previous handover.

## Afternoon and evening: standard-mod installation, release package, hub options

Direction from the user, in order: the mod must install like an ordinary
mod (drop the folder in, one batch file for the executable patch, launch
through Steam), the desktop mirror must not affect VR, EAC status to be
confirmed, a licence recommendation, a game-folder backup and a clean
install rehearsal, then a set of gameplay requests that came in during
testing. The in-game "enable VR" toggles discussed at midday were dropped in
favour of a mode switch, on the user's suggestion.

**Findings.** EAC is absent from the installed game: no EAC files or shim,
and the Fatshark launcher starts `Darktide.exe -eac-untrusted --bundle-dir
../bundle --ini settings` (launcher log). The launcher runs whatever is
named `Darktide.exe`, so the patched executable needs no launcher change.
The native module already entered the game as a drop-in `binaries\d3d12.dll`
proxy; the PowerShell chain existed only to write probe flags, pre-set the
window, press Play, start the viewer and check hashes. Custom HUD is optional
and guarded at every call site; DMF is the only hard dependency. Licence:
MIT (all dependencies are Apache-2.0, BSD-2, MIT or LLVM/MS permissive;
Custom HUD is not bundled).

**Built and verified offline (234/234) and by plain Steam launches.**

- The native module starts `darktidevr-xr-harness.exe` beside itself under
  a job object with a stop file (`dtvr_viewer_control/state`,
  `/dtvr_viewer start|stop|restart|status`); Lua requests it on the first
  update when the d3d12 bootstrap installed (`dtvr_bootstrap_state`).
- The accepted play configuration is the built-in default when a flag file
  is absent: generated stereo at stereo submit, the continuous eight-owner
  submission, the Streamline probes, controller aim, gameplay input, the
  fixed HUD panel, the headless first-person body, body follow and IK
  presentation. A present flag still decides; the development launcher
  writes explicit off values for probes it did not request. The viewer
  defaults to the precise pair wait, direct native originals and the
  crosshair scale file beside the mod. Without an NGX runtime the default
  install leaves generated stereo off instead of failing the whole install.
- `mods/darktidevr/Darktide VR Mode.bat` +
  `darktidevr-mode.ps1`: VR mode patches the executable (pristine copy under
  `%LOCALAPPDATA%\DarktideVR`), installs the proxy from `bin\d3d12.dll`, adds
  the load-order line and swaps to a VR copy of `user_settings.config`; flat
  mode reverses all of it. Status reports each item.
- Package: game-folder layout, everything under the mod folder (Lua, bin,
  switch, patch tool, LICENSE, notices, changelog, USER-GUIDE.md, README,
  manifest). `record-component-provenance.ps1` rebuilt the producer and
  viewer from the clean checkout `2bc4d1d` and the archive
  `artifacts/packages/darktidevr-0.1.0-alpha.1-2bc4d1d8579e.zip`
  (SHA-256 `50CE094E...`) carries that record. Ignored artefact; rebuild
  with the two commands in RUNTIME-PACKAGE.md.
- HUD warning while the game window has lost the desktop focus
  (`focus_warning`, default on). Calibration opens once on the
  character-select screen when nothing is saved.
- Hub: first person is the default; `hub_third_person` (default off) keeps
  the stock third-person character with the primary and secondary weapon
  units hidden, the head does not swing the camera, stick turning and the
  stick's vertical axis are deltas onto the stock orientation (the mouse
  moves the same orbit), the headset keeps its tracked pitch. The hub
  overrides list omits actions the hub cannot perform. Quick wield from a
  grenade, pocketable, stim or device returns to the last weapon held; the
  switch badge reads "[RS Down]".

**Plain Steam launches.** 09:47, development install: Steam, Play by hand
(my test used the Play helper; nothing in the mod presses Play), viewer
started by the game, VDXR session, stereo active in gameplay, generated
pairs up to 56/s. 00:02, clean install from the first package after flat
mode, deleting the mod folder and the duplicate `binaries` native copy:
viewer started, session created, the user reached the hub, and found it in
third person because the body flags were absent; fixed as above. The
current install is the rebuilt `2bc4d1d` package extracted over the game
folder with the switch in VR mode. Game-folder backup:
`D:\Backups\Darktide-game-2026-09-12` (96.5 GB, robocopy, no failures).

**Not yet worn-checked** (all in the current install): the two-pose
calibration and its first-run prompt; the focus warning; `hub_third_person`
(weapon hiding, no camera swing, stick pitch direction and rate, and whether
movement should follow the camera there); quick wield return; the shorter
badge; the settings-profile swap across a flat/VR round trip; a fresh
character through the prologue and the two onboarding hub missions (still
not VR-admitted, see USER-GUIDE known limits).

**Next, in order.** 1. Worn pass on the items above. 2. Desktop-mirror
independence: publish stock menus at the virtual extent through the
existing shared-menu path so window size and aspect stop mattering; the
survey's recommendation names `capture_menu_from_resource`, the
`publish_mode` source extent for modes 5/6, and the viewer's
`native_window_extent` as the three couplings to cut; until then the native
still nudges the window to 1920x1080 and menus are captured from the
window. 3. Nexus page text from USER-GUIDE.md and CHANGELOG.md; the
archive is ready to upload once the worn pass is clean. 4. Retire the
launcher scripts from the package spec docs (already gone from the
package) and decide whether `binaries\darktidevr_native_capture.dll` still
needs deploying in development.

## Late evening: third-person hub fixes, menu canvas capture, fresh character

- Third-person hub: the mod's own MissionManager hook answers "no third
  person" for the hub before the extension initialises, so the withdraw path
  now asks the mission again (`stock_force_third_person restored=true`);
  curios, pocketables, device and luggable units are hidden too; hand IK is
  withdrawn; the option is recognised at spawn from the mission name and
  flushed to disk on change (DMF otherwise saves only on state change or a
  normal exit; a force-quit lost it). Reset view looks along the orbit.
  Worn: "3p is good now" (user). Options menu reordered: movement direction
  first, "Hub and missions" group above the controls.
- Desktop-mirror independence: see the todo entry; commit `bd1aca0`. The
  installed mod has the new native module, viewer and main chunk copied in
  by hand (hashes 1C30CF24, 59E797DC, 2B08FF00); not yet worn-checked.
- Fresh character (user, 00:37 to 00:50): calibration prompt, class and
  appearance views, name entry needed the keyboard (documented), prologue
  reached at 00:48 with local authority and first-person body; the opening
  cinematic was black with stacking subtitles (todo entry). The session
  ended before the onboarding hub missions.

## Night: stereo cinematics, onboarding hub, menu canvas confirmed

- Menu canvas capture confirmed worn at a 1280x768 window ("menus are
  good"). Third-person hub: menu exit no longer moves the camera (orbit
  held across menus); reset view looks along the orbit.
- In-engine cinematics in stereo (`stereo_cinematics`, default on): the
  cinematic camera leads translation and yaw through the camera driver's
  yaw-only branch, the body anchor origin stands aside, subtitles are
  mirrored to the HUD panel's bottom edge (stock widgets, their boxes and
  the cutscene overlay letterbox hidden; placeholder gated by the stock
  draw condition). Worn: "cutscene works now, subtitles at the bottom with
  no bars" (prologue, fresh character).
- Onboarding hub missions (prologue_hub) now report as the hub and take the
  third-person presentation (commit a51dd9f); deployed for the next start,
  not yet reached worn.
- Nexus page draft: docs/NEXUS-PAGE.md. Package rebuild from the final
  commit still to do (record-component-provenance then
  build-runtime-package with -ReleaseVersion 0.1.0-alpha.1).

## Late night: onboarding tutorial, aim focus, markers, popups

- Aim-down-sights focus (481727d, option `ads_focus`): the native aim state
  carries the sight flag (v5); the viewer eases a vignette and a tighter
  reticle over 150 ms and the stabilisation filter runs 1.6x stronger. A
  haptic pulse is deferred to the post-alpha haptics pass (user decision).
- Block uses the stock guard pose on every melee weapon; tutorial and HUD
  prompts render controller badges through the INGAME_INPUT macro hook.
- New-user defaults now match the accepted layout (dd2c97c). The user's own
  profile was not touched.
- Psykhanium onboarding softlock after the grenade section: the training
  steps' `ensure_player_healthy` condition asked for first person, which
  the VR presentation answers false; wrapped for the check. The
  end-of-training question is a stock popup that was only drawn on the
  desktop; active popups now select the interactive flat panel (b2138fd).
- Wide markers and the interaction popup are scaled per eye about the
  marker pivot so both eyes span the same world extent (b2138fd);
  damage-direction indicators moved from the per-eye layer to the HUD
  panel (the user reported them flat).
- Gun model off the hand in the onboarding (lasgun, "the same angle as
  before the placement fix"): the tutorial log (02:17:50) has no
  DARKTIDEVR_GUN_AIM line at all, the prologue log (01:15:35) has one, so
  the placement write never ran. Cause: `darktidevr_online_rules.lua`
  admitted only `shooting_range` as a range (since e3647d0, when the
  stock-input route was proven only there), so the training grounds fell
  outside the route that gun placement, two-hand support and the online
  reticle are gated on, while aim itself still worked through the
  gameplay-context range list. The training grounds are now a range for
  the rules too; offline 234/234; deployed to the installed mod, worn
  check pending (also for everything else in this section).
- Release package rebuilt from `bda5d37` (12 September, 12:35):
  `record-component-provenance.ps1` (pass `-OutputPath` explicitly; the
  default fails when the script is started from a non-PowerShell shell)
  then `build-runtime-package.ps1 -ReleaseVersion 0.1.0-alpha.1
  -ComponentProvenancePath ... -RequireComponentProvenance`. Archive
  `artifacts/packages/darktidevr-0.1.0-alpha.1-bda5d379fa9c.zip`, 91 files,
  SHA-256 `6E584EA314848D0D7C5591D0E96FBE481A3F879EAF2586D62A91EECE0EA44322`;
  verifier pass on the extracted tree. Extracted over the game folder and
  driven through `Darktide VR Mode.bat` with its menu choices: flat
  (pristine executable restored, proxy and load-order line removed, flat
  settings profile restored), VR (patched, proxy, load order, vr profile),
  status; all exit 0. Cosmetic: the patch step echoes a repository-relative
  `restore.command` that a package user does not have; the batch's flat
  choice is the restore.
- If the pending worn checks need code changes, rebuild the package from
  the final commit again; the archive name carries the revision.

## Afternoon: worn pass on the package build (game started 12:46)

- Gun placement in the onboarding: confirmed fixed worn.
- End-of-training popup: appears in the headset; the user reports the
  pointer needs enabling there. The log shows the pointer transport
  active with three consumed presses at popup-button positions
  (02:52:41 UTC) and no rejection; the stock handler takes the same
  proxied View service as every view, so what failed (laser visual,
  hover, or press) is not yet known. Asked for specifics.
- "Equip medicrate" prompt read "[Unbound]": the prompt asks for the
  slot's own alias (`wield_3`), which the default layout leaves unbound
  because X cycles carried items. Prompts for pocketable, stim and
  device now fall back to the cycle control's badge when the slot has no
  control of its own (`darktidevr_controller_prompts.lua`).
- Tutorial marker step softlocked: "tag while aiming at nothing" is the
  stock single-tap location marker, produced by the com_wheel path
  because the middle mouse button carries both `smart_tag` and
  `com_wheel`. The VR tag press only injected `smart_tag`. The wheel
  module's unowned path now sees a one-frame `com_wheel` press on the
  same frame as the tag tap (query `gameplay_ui.tag_tap_frame`), so the
  stock stop callback leaves the location marker when nothing was
  tagged; an enemy tag in the same frame still wins there. Offline
  234/234; deployed for the next start, worn pending. The user passed
  the step with the mouse's middle button this time.
- Popup pointer, resolved by the user's report: no laser or cursor was
  visible at all on the end-of-training popup (mouse used; the log's
  consumed presses were the mouse). The laser and cursor quads sampled
  one texel at the bottom-right corner of the flat menu swapchain image.
  The window-capture path painted an opaque yellow swatch there on every
  frame; the engine-canvas blit (last night) copies the canvas straight
  into that image, so the pointer took the canvas corner's own colour
  and alpha: opaque UI under a full-screen menu, transparent world under
  a popup, hence nothing. Fix (user's direction: a fixed dark green, not
  overridable): the pointer owns a 16x16 static swapchain, filled once
  from the upload buffer on the first frame and never written again;
  a quad layer can only sample a swapchain image, so a colour needs one.
  The window capture no longer paints the swatch. Viewer rebuilt, full
  suite 234/234, deployed to the installed mod; worn pending.
- Package rebuilt from `06b662c`:
  `artifacts/packages/darktidevr-0.1.0-alpha.1-06b662c2b662.zip`, 94
  files, SHA-256
  `8C9264D146142BEA5BE25C08F52FDE01502681C6FEB8E67D4E01C75E39804C0A`,
  verifier pass, installed over the game folder (VR mode confirmed by
  the switch's status). This supersedes the bda5d37 archive. The worn
  checks on the popup pointer, medicrate badge and marker step still
  stand; if they pass, this is the archive to upload.

## Late afternoon: release facts and the rename to `darktidevr`

- Release questions answered from the code: the viewer enables only
  XR_KHR_D3D12_enable and suggests Touch and khr/simple bindings, so any
  D3D12-capable OpenXR runtime should work in principle (untested; frame
  pacing was tuned on VDXR) and non-Touch controllers get only select
  and menu; NVIDIA is needed only for DLSS Frame Generation (RTX 40+).
  Guide, Nexus draft and package README now state the tested setup,
  leave streaming resolution and refresh rate to the user, add the
  movement-speed-follows-aim limit and a Planned section (af423dd).
- Mod id renamed from `darktidevr_stereo_probe` (the original stereo
  capture probe it grew out of) to `darktidevr`, on the user's decision.
  git moves for the mod folder, the module folder, the descriptor and
  the three DMF-named files; a token replace over 94 tracked files (685
  occurrences: CMake test paths, io_dofile paths, packager spec, sync and
  launch tools, native bin path, current docs). Dated handovers before
  today, the 11 September code review and the original stereo-probe
  note keep the name they were written under. No code derived the id
  without its prefix, so nothing else needed touching. CMake
  reconfigured, everything rebuilt, 234/234.
- The user's settings migrated on disk with the game closed: the DMF
  key `darktidevr_stereo_probe = {` and the options-menu scroll entry in
  `%APPDATA%\Fatshark\Darktide\user_settings.config` and in the three
  profile copies under `%LOCALAPPDATA%\DarktideVR` (vr, flat,
  pre-first-run-test) renamed to `darktidevr`; `mods\mod_load_order.txt`
  line renamed; installed folder moved. Backups of all five files in
  `%LOCALAPPDATA%\DarktideVR\rename-backup-<stamp>`.
- Package rebuilt from the rename commit `4605293`
  (`darktidevr-0.1.0-alpha.1-460529324883.zip`, verifier pass, no
  old-name entries). Installing it exposed a packaging bug that would
  have hit every user on their first mod update: the switch recognised
  this mod's proxy only as a byte-identical copy of the current
  `bin\d3d12.dll`, so the earlier build of our own proxy read as
  "foreign"; flat left it in place, VR refused to install, and the
  install was left half-switched (patched executable, flat profile, no
  load-order line). The switch now identifies our proxy by the
  `darktidevr` marker its embedded paths carry (wide or ASCII):
  byte-identical is `installed`, marker with other bytes is `outdated`
  (VR replaces it, flat removes it), no marker is `foreign` (never
  touched). New test `mode_switch` drives status, flat and VR against a
  fixture game folder through all four states; full suite 235/235. The
  real install was brought back to VR mode with the fixed switch (proxy
  DE02C833 on both sides, load order listed, vr profile restored; the
  live settings file carries the renamed key with its values). README,
  guide and Nexus draft say what to do after a mod update.
- Package rebuilt from `c626f44`:
  `artifacts/packages/darktidevr-0.1.0-alpha.1-c626f44a82c2.zip`, 94
  files, SHA-256
  `0D9A46C7E2FBE5CDCD39B92CB7D290C79041E83A331822BA19F14199C02B478A`,
  verifier pass. Installed fresh (installed folder removed, archive
  extracted) and driven through the switch: flat (proxy removed, flat
  profile), VR (proxy installed, load order, vr profile), status; all
  exit 0. This is the current release candidate; it supersedes every
  earlier alpha.1 archive.

## Evening: worn pass on the c626f44 package, two Lua crashes

- Worn, all passed (game started 13:5x local, log 03:49 UTC): settings
  and bindings carried over after the rename; laser and cursor on the
  end-of-training popup; "Equip medicrate" reads [X]; the marker step
  passes with the tag button aimed at nothing.
- The session ended in a Lua crash when entering the Psykhanium a second
  time: `script_world.lua:240 bad argument #1 to 'set_data' (userdata
  expected, got nil)` from `activate_viewport`, called by this mod's
  ScriptWorld.render hook with `primary = nil, right = nil`: the world
  rendered during the scene transition before its player viewports
  existed. The hook now lets stock render the world alone when either
  viewport is missing (logged once). The user took it for the known
  flat-screen crash; the trace says it was ours.
- The earlier session (02:43 UTC log) had also ended in a Lua crash, on
  exit: `ui_manager.lua:618 attempt to index field '_view_handler'`
  from the onboarding handler's destroy, whose settings sync closes a
  still-active tutorial view after UIManager has dropped its view
  handler. The trace passes through the prompt scope wrapper, which
  re-raised the stock error. With on_destroy true and no view handler,
  the wrapper now lets the sync fail quietly (logged); everywhere else
  stock errors still propagate. Whether stock alone would hit this at
  exit is not established; the guard costs nothing.
- Full suite 235/235 after both.
- Package rebuilt from `4efe666`:
  `artifacts/packages/darktidevr-0.1.0-alpha.1-4efe666bc7ff.zip`, 94
  files, SHA-256
  `E61B47F6BE61147FF6239DC5E3BBD080B1F18B6A8B07877B7B38EE3E60ADF2FE`,
  verifier pass, installed fresh with the switch round trip (flat, VR,
  status; all exit 0). This is the release candidate; it supersedes the
  c626f44 archive, which differs only by the two crash guards.
- Worn on the 4efe666 package (log 04:14 UTC): hub, Psykhanium, hub,
  Psykhanium again, then a normal exit; no crash marker in the log and
  a clean log end. Limit: neither guard logged in this run
  (`viewport_pair=missing` and `shutdown_sync` are absent), so the run
  shows the sequence no longer crashes, not that the guards were
  exercised; the original failure was timing-dependent. Left on the
  checklist: one Solo mission start to finish, a remote-server mission
  if available, and a flat-mode launch through the batch file.
- Private GitHub mirror created on the user's request:
  https://github.com/Brobert-in-aus/darktide-vr (remote `github`),
  with `main` and the working branch pushed and the working branch set
  as the default so the repository shows current content. The Forgejo
  origin remains the branch upstream; push to both from now on. `main`
  on both remotes is still the bootstrap commit; fast-forwarding it to
  the release commit is a repository-policy call for the user. Tracked
  files were scanned for credentials before the push (none).
- Fresh-install rehearsal on the user's machine, on the user's request,
  before publishing (14:24 local): everything of ours backed up to
  `%LOCALAPPDATA%\DarktideVR-fresh-install-backup-20260912-142429`
  (live settings before and after the flat swap, the whole
  `%LOCALAPPDATA%\DarktideVR` directory with the vr/flat profiles and
  the pristine executable copy, the dev `darktidevr_camera_probe` mod,
  the binaries leftovers, the load order). Then: flat mode through the
  installed switch; the mod's settings table and its options-menu
  scroll entry stripped from the live `user_settings.config` (147
  lines; other mods untouched); mod folder, camera-probe mod, bootstrap
  log and the two disabled winmm proxies removed; profile directory
  deleted; load order left with custom_hud and SoloPlay. Install as a
  user: archive extracted, `Darktide VR Mode.bat` run with choice 1
  through the menu: executable patched with a new pristine copy, proxy
  installed, load order added, vr profile started from the current
  settings; no mod settings present, so the next start is a first run
  (calibration prompt on character select, default bindings). The user
  launches through Steam and the launcher themselves and plays a full
  mission for the page's screenshots and video.

## Evening: full mission from the fresh install (log 04:26 UTC)

The user ran a fresh-install start through the launcher and a complete
mission (hub, matchmaking, mission, end of round). Media pulled from the
Quest into `artifacts/media/2026-09-12/` (three screenshots 14:31, one
2.9 GB recording 14:31 to 14:57); adb needs `MSYS_NO_PATHCONV=1` and a
Windows-style local path from bash. Findings and what was done:

- **Crash on the way back to the hub** (fifth Lua crash type): the
  eye-target hook on `ScriptWorld.create_viewport` asserted that the XR
  render extent could be refreshed while the new player camera was being
  spawned after the mission server exit; the head-pose transport was
  silent that frame. The extent does not change within a session, so the
  callback now returns the last known extent (logged once), and the
  module falls back to stock viewport creation when no valid extent
  exists at all instead of asserting.
- **Device screens blank (auspex scans, generator and decode minigames).**
  All of them are `ScannerDisplayView`, which draws through the shared UI
  Gui into an offscreen render target and links that target to the
  `auspex_scanner_display` mesh material of the unit it was opened for.
  That unit is the first-person device model; the mod hides first-person
  units and shows the third-person ones, whose screen material is never
  linked. The view now also links the target to every third-person unit
  of the equipped slots that carries the display mesh (and unlinks with
  the view), logging which unit the view was given and how many
  third-person screens were found. Not yet worn: `F7` (or
  `/dtvr_scanner_test`) opens the scanner display on the equipped
  device without a mission interface so the screen can be checked in the
  Psykhanium; the game must give a device in `slot_device` there.
- **View permanently lowered after a rescue.** The body camera anchor
  measures the model-eye offsets once per unit and holds them. A unit
  that spawns hanging at a respawn point measures them in that pose.
  Both the stable eye anchor and the camera offset are now measured only
  while the character is in an ordinary upright state (walking,
  sprinting, sliding, jumping, falling, dodging, interacting, minigame,
  lunging, stunned, exploding); until then the first-person fallback
  serves. Worn pending.
- **Keyboard bindings in a menu (the user's third report).** A complete
  scan of the game source (5,049 Lua files from the Aussiemon mirror at
  1.12.5) for every caller of `input_text_for_current_input_device` and
  of the `INGAME_INPUT` macro found 41 call sites. The mod labelled only
  twelve scoped HUD elements for the Ingame service and, for the View
  service, only the input legend and the button-hint helpers; every
  other caller fell through to the stock keyboard text. Callers outside
  that coverage, which is what the user kept seeing: the kick and
  mission-flow voting notifications (`notification_option_a/b`), the
  party invite and matchmaking notifications, the loading view's next
  hint, the title view's continue, the mission board, the tab menus and
  menu panels of every view, the grid legends, the social roster, the
  tutorial overlay, the onboarding templates, the option pass templates,
  the mastery and penance views, and the text utilities other code
  routes through. The hook is now global: Ingame aliases the mod knows
  get their controller badge everywhere; View aliases get the VR
  adapter's route (back is B/Menu, pointed widgets and tabs the pointer
  click, right click the secondary click when it exists) and read
  Unbound where no route exists; unlisted View aliases are logged once
  (`DARKTIDEVR_PROMPTS view_alias=`) so they can be added; hub hotkeys
  keep their keyboard hint by earlier decision. Direct weapon slots
  outside the switch badge read Unbound rather than a keyboard number.
  Votes are therefore honest but unanswerable in VR: known limit.
- **Third-person spectating.** Stock follows the watched player with
  their own first-person camera. The camera handler's
  `_first_person_spectating_mode` is now false outside the hub (option
  `spectate_third_person`, default on), so the stock observer camera
  follows the watched unit in third person with the orientation from the
  local player's own look input. Worn pending; the orientation path may
  need the yaw-only driver like the hub.
- Full suite 235/235. Deployed to the installed mod.
- Package rebuilt from `cd09be6`:
  `artifacts/packages/darktidevr-0.1.0-alpha.1-cd09be663e7e.zip`, SHA-256
  `4523A7F151AD385DA9E58D5D70ACE58E9789C2C873F4AB0A13ED09E0D62AD905`,
  verifier pass, extracted over the game folder and VR mode reconfirmed
  through the switch.
- Fresh-character start (log 05:21 UTC): stuck in the hub on the stock
  "Notice: You are being summoned to the strategium / Obey" popup with
  no laser, and neither mouse nor keyboard answered it even with the
  window focused. The Path of Trust cutscene view had opened first (mode
  2, non-interactive by design) and the cinematic waits for that Obey;
  the mod only selected the interactive panel for a popup when no view
  set a mode, so the popup was drawn without a pointer. Popups now
  outrank loading and cutscene panels (any mode but 5/6 becomes 5 while
  a popup is active). Why stock keyboard and mouse did not reach the
  popup in mode 2 is not established; the interactive route is the
  proven one. Deployed to the installed mod for the restart. The 05:21
  session showed no `DARKTIDEVR_SCANNER` or `DARKTIDEVR_PROMPTS` lines
  yet, as it never got past the hub.
- Package rebuilt from `cad0004`:
  `artifacts/packages/darktidevr-0.1.0-alpha.1-cad000479d0f.zip`, SHA-256
  `0800CE5065C633DB5CF4C994EEEA11BE4A484F257E2842DBF4D3DB315EAAC016`,
  verifier pass, extracted over the game folder with VR mode reconfirmed.
- Next start (log 05:39 UTC): the fresh character went straight to the
  hub; the summons popup and the Path of Trust scene did not recur, the
  game having recorded the previous attempt. A video view (pre-rendered
  clip) played on the flat panel. The prompt hook logged its first
  unrouted menu aliases: `navigate_primary_left/right` (no `_pressed`
  suffix) and `confirm`; added to the pointer-click set.
- F7 played the error sound: DMF found no `toggle_scanner_test`, because
  the function was defined inside the scanner view's require hook and
  so did not exist until that view had loaded once. It is a top-level
  function now, as is the chat command.
- Loading boards and video cutscenes stretched (screenshot 15:46: the
  loading image as a wide band with its caption below). In VR the
  engine's back buffer is the 2112x2304 eye canvas while the desktop
  window is 1280x768; the window mirror squashes the canvas to the
  window's aspect, and mode 2 (loading, video, cutscene boards) took its
  texture from that window capture. Modes 5 and 6 already blit the
  private canvas at the published 16:9 extent and look right. Mode 2 now
  uses the same path: the native canvas capture accepts
  `flat_loading_or_cinematic`, the viewer's shared-menu projection set
  includes it (texture only; menu mode and the pointer stay off), and
  publish_mode gives mode 2 the scaled 16:9 source extent. The window
  capture remains the fallback where no canvas exists (title, character
  select). Full suite 234/234 (the switch test excluded while the game
  ran, then green). Worn pending: a loading board and a video cutscene
  in the headset.
- Package rebuilt from `5cacef0`:
  `artifacts/packages/darktidevr-0.1.0-alpha.1-5cacef0f0ba0.zip`, SHA-256
  `F400C3EA72A8DAE222454489E858487D158862DFCE79A4D764130295F528CE5B`,
  verifier pass, extracted over the game folder with VR mode reconfirmed
  and the installed native module, viewer and main module matching the
  package.
- The user asked how to get the auspex in hand in the Psykhanium: it is
  not in any loadout; a scanning objective zone or a decoding and
  generator interaction equips it into `slot_device` and removes it
  after (mission_objective_zone_scan_extension, scanning_interaction).
  F7 now does the same when the slot is empty: it looks the auspex
  scanner up in the item catalogue (the id is level data; it tries
  `content/items/devices/auspex_scanner`, then any catalogue item named
  auspex that fits `slot_device` and is not the expedition map), equips
  and wields it, opens the display, and on the second press closes the
  display, wields the previous weapon and unequips the device. On the
  user's direction it is active in the Psykhanium only (shooting range
  and training grounds, the local server); in missions, SoloPlay or
  Fatshark's servers, the game's own zones and interactions equip the
  device and F7 just says so. Deployed, worn pending.
- Package rebuilt from `ad7f8ac`:
  `artifacts/packages/darktidevr-0.1.0-alpha.1-ad7f8ac541bc.zip`, SHA-256
  `14268EFE60EF89F3288F52186C650DB527424CF46DF49A6DF180E8E2FBAEF02D`,
  verifier pass, extracted over the game folder with VR mode reconfirmed.
- Worn on that build (log 05:59 UTC, screenshots 15:46 and 16:01): F7
  equipped the auspex in the Psykhanium and its screen showed the
  scanner's idle display (dim green graphic, the "none" minigame's idle
  state; a real scan is the definitive check); `DARKTIDEVR_SCANNER` shows
  the view was given the first-person unit and one third-person screen
  was linked. Loading boards were still squashed. The user also asked
  for the laser to reach only halfway to the panel without the hit dot,
  since the board draws its own cursor.
- Why mode 2 stayed squashed after the canvas path: the packed
  swapchain hands the engine a private backbuffer of one eye's size
  (2112x2304, `EngineEyeBackbuffers::acquire`), every flat board is drawn
  into it, and the canvas blit is a full-quad copy into the 2112x1188
  menu texture, so the texture is squashed 1.94x vertically. Menus look
  right because `flat_interactive_uses_eye_aspect` sizes the flat panel
  at the eye aspect ("eye encoded") for interactive modes with the eyes
  open, which restores the geometry; mode 2 was excluded from that rule
  and so showed the squashed texture on a 16:9 panel. Mode 2 now takes
  the eye-aspect panel too when the eyes are open (core rule; test
  extended). Before the eyes open (title, character select) the window
  capture and 16:9 panel remain. The laser ray is half length with no
  hit dot. Full suite 235/235.
- Package rebuilt from `0450fdf`:
  `artifacts/packages/darktidevr-0.1.0-alpha.1-0450fdf1e838.zip`, SHA-256
  `BFE03B48263B95F0642C8F98B7A6B838BA15EF678E7E5ED4A040139617E13CED`,
  verifier pass, extracted over the game folder with VR mode reconfirmed
  and the installed binaries matching the package. Superseded below.
- Worn on 0450fdf: the half-length laser is right, but "the cursor target
  thing isn't there in all menus". Cause: the only on-panel cursor was the
  reticle that `WindowCapture` painted into its own captured image (black
  edge, cyan ring, white cross), so it existed on window-captured boards
  (title, character select) and nowhere the menu comes from the engine
  canvas or mirror (in-game menus, popups, loading boards). The game
  itself only shows the Windows hardware cursor on PC (its software
  cursor element is Xbox-only), which no capture path includes. The old
  green square had been the target everywhere. Fix, as the user chose:
  the capture paints nothing (`set_pointer_overlay` removed), and the
  viewer shows the same reticle as a 4.5 cm quad at the laser's hit on
  every menu panel (`paint_pointer_target` in `core/reticle_atlas.h`,
  drawn once into the static pointer swapchain beside the laser's
  colour block; sprite test in presentation_policy, capture test asserts
  no cursor is painted). Full suite 235/235.
- Package rebuilt from `2993ea3`:
  `artifacts/packages/darktidevr-0.1.0-alpha.1-2993ea3b83b4.zip`, same
  file list as 0450fdf, SHA-256
  `5F5D9A45B48E50E26DD165C0E640EE77A09A5159A2840EC4B16D48EFD32B0C21`,
  verifier pass, extracted over the game folder with VR mode reconfirmed
  and the installed viewer matching the package. Current release
  candidate; supersedes 0450fdf.
- Worn on 2993ea3: loading screens at their true shape (pass). The
  target shows as a dark green or black square and the laser may be
  black. Cause traced in the code (todo, post-alpha item): the sprite is
  staged in the shared upload buffer's corner and the window-capture
  memcpy overwrites it later in the same frame before the GPU copy
  runs. The user chose to ship it as a known limit and prepare the
  launch; the changelog, user guide and Nexus draft now say the marker
  is a plain dark square in this build.
- Package rebuilt from `8f860f6` (documents only; the three binaries are
  byte-identical to the 2993ea3 package):
  `artifacts/packages/darktidevr-0.1.0-alpha.1-8f860f65aeab.zip`, 91
  files, SHA-256
  `877DC95C0DFAEFFF3DF3ACB69A00E1BAF79B9BDC037BE91F2FD6A58BC4794BE3`,
  verifier pass, extracted over the game folder after the user's session
  ended, VR mode reconfirmed (status: patched, installed, listed,
  present, vr profile), installed viewer and changelog matching the
  package. This is the archive to upload; it supersedes 2993ea3.
- Launch checklist state: section 1 has the worn items still open
  (ADS focus, block pose, prompts, markers, damage indicators, quick
  wield, hub option, focus warning, a full Solo mission, the flat round
  trip, the evening-fix worn pass); section 2 is reviewed except the
  changelog's "(unreleased)" heading, which takes the release date on
  the day; section 3 (public source, `main` fast-forward, tags) and
  section 4 (the Nexus page) are the maintainer's actions.

## Published

- The user published the mod on Nexus Mods (12 September 2026) with the
  8f860f6 archive. Afterwards: changelog heading dated; `main`
  fast-forwarded to the codex branch head on origin and github; tag
  `v0.1.0-alpha.1` on `8f860f6` pushed to both; the GitHub mirror made
  public. Custom HUD (continued) is the named optional dependency (the
  installed 2.1.6 fork by Sungrief, same `custom_hud` mod id).
- Worn after publishing, on the published archive (user's notes): ADS
  focus with a clean release, block pose on melee, controller badges in
  tutorial and HUD prompts, damage-direction indicators in stereo, quick
  wield returning to the last weapon with the "[RS Down]" badge, the
  third-person hub option on and off including the onboarding hub, the
  focus warning appearing and clearing, one Solo mission and one
  remote-server mission start to finish. All pass. Left unchecked in
  section 1: marker convergence at the far edge, the flat/VR round trip
  through the batch file on the installed package, and the evening-fix
  items (real device scan, view height after a rescue, third-person
  spectating, vote and end-of-round prompts, hub return).
- Next release (`0.1.0-alpha.2`): the pointer hit-marker fix (dedicated
  upload buffer for the sprite, todo item) and the user guide's Custom
  HUD wording, plus whatever the first reports bring. Section 5 of the
  launch checklist lists the post-publish steps (first Posts message,
  bug-report instructions, update cadence, game-patch watch).
