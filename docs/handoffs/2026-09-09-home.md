# 9 September: user home, development stopped

The user requested heartbeat stop, a summary and commit/push on returning home.
The 20-minute `keep-darktide-vr-work-moving` heartbeat is paused; the older
duplicate was already paused. No automatic continuation is authorized now.
Quest proximity Enable completed successfully and Status was read. The device
does not expose a durable broadcast-override query; retain the command receipts
locally, without device identifiers in Git.

## Completed work

- Advanced billboard and pickup/HUD diagnostics: isolated capture ownership,
  complete widget bounds/text/primitive measurements, source/material lifetime
  handling and focused candidates. These do not establish a visual fix.
- Kept DLSS blur around HUD items explicitly open, distinct from text softness
  and duplicated UI. Improved RGB/alpha, capture-identity and surrounding-image
  analysis; optional compiled pixel hashing greatly reduces offline analysis
  time while preserving exact checksums.
- Developed separate communication-wheel/tactical-overlay, push-to-talk,
  prompt and binding-audit candidates. Preserved the user's saved controls.
  General gameplay verification was parked when the user changed priorities.
- Reduced native CPU costs in exhausted diagnostics, resource-name matching,
  command snapshots, buffer address/range/pointer lookup and trace-only locking.
  Reduced lock scope/cost in opt-in GPU and billboard diagnostics.
- Reduced Lua temporary allocations in prompts, HUD/crosshair draws, HUD
  following, projection calculations and two controller-aim helpers. Discarded
  a two-hand rotation rewrite whose JIT measurements did not justify it.
- Analysed existing VDXR slowdown traces without new live capture: submission
  waiting closely overlaps runtime OVR_BeginFrame. Corrected interpretation of
  a large first reported CPU statistic. The backend cause and fix remain open.
- Prepared six separate diagnostic/performance plans and verified copied-file
  rollback for the performance packages. No accumulated whole-tree deployment.

## Measured results and limits

The [performance review](../PERFORMANCE-REVIEW-2026-09-09.md) links exact scopes,
commands and receipts. Representative offline results include roughly halved
exhausted-diagnostic hook cost, substantially faster repeated older-buffer
lookups, and less temporary Lua allocation. They are not game FPS results.

The last interleaved Map/Unmap run compared `516f935` with `28a5500`: early-buffer
median 11.2929 to 3.6309 ms, cycling 7.4291 to 5.4128 ms, but newest-buffer
3.1737 to 3.7194 ms per 10,000 pairs. This mixed result is retained, including
control noise and narrowly overlapping newest-buffer ranges. Mapping hooks are
diagnostic-mode paths; the saved normal configuration does not activate them.
Production lighting upload cost and actual lookup distribution remain unmeasured.

Final scoped-aim and equipment candidates are PRs #227/#228, isolated as
`069cd2b` in PR #229. Main's 69 Lua chunks and the focused package's 49 chunks
compile. Actual-helper comparisons preserve returns/errors/restoration and
equipment identity/mutation. The final runner change, PR #230, passes a
20-process interleaved comparison plus a six-process single-library interface
check. Native changes throughout the day built and passed affected checks;
there was no final full-suite rerun, and historical totals are not current claims.

## Preserved installation and candidate state

Accepted Lua `3341afb`, native `23345e5`, viewer `6688841` remain installed.
Both installed native copies retain SHA-256
`FCCDD0DE699F9D50D2BD316792829D4EE5700843D925D194BE4DF1F3EEB02369`.
No game, graphics, saved binding, runtime or shader settings were changed today.
The last Inventory was observation-only; it did not certify Ready.

The shutdown read-only audit reports all six staged plans matching:
`artifacts/unattended/home-focused-trial-status-20260909.json`.
Plans are native performance `8b3697a`, extended native `24687ff`, native UI
`a4ec84c`, marker metrics `8178c5f`, communication `848b78d`, and Lua performance
`008e1b0`. Native alternatives overlap at both DLL destinations. Other overlaps
and rollback receipts are in [staged plans](../FOCUSED-DIAGNOSTIC-TRIALS-2026-09-09.md).
Do not stack them or silently refresh baseline preconditions.

Today's completed work is committed and pushed on task branches with review
PRs; nothing was merged. Focused worktrees remain separate. The pre-existing
user image is untracked and untouched. The unfinished basic-gameplay runner
remains in its explicitly named parked stash, not claimed complete or pushed;
older unrelated stashes/branches remain untouched.

## Next user-directed session

Billboarding first, pickup popup sizing next, with the user's specific worn
observations. DLSS surrounding-HUD blur and sustained VDXR slowdown remain open.
Physical melee stays paused. Skip basic-gameplay revalidation unless the user
requests it. Run Ready before any deployment or game interaction, select one
focused candidate and retain its exact rollback. No further work begins merely
because a previous checkpoint or heartbeat says to continue.
