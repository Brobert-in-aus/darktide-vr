# End-of-day handover — 11 September 2026

The user requested wrap-up. The running mirror A/B/A task is complete;
development and the 20-minute heartbeat are paused. Do not resume live work
until instructed. No game or XR viewer remains running. Accepted native DLLs
were checked at both installation locations; all final trial restoration
receipts pass and Quest proximity automation was restored. No experimental
performance build was promoted. The untracked user JPG remains untouched.

## Work completed today

- **Largest measured improvement:** descriptor metadata demand guards plus
  the native-original three-slot ring raised matched native mission delivery
  from a fresh 76.47 FPS control to approximately 89.34–89.57 FPS, about 17%.
  Earlier high producer rates hid a slower legacy delivery path. See
  [descriptor demand](../DESCRIPTOR-COPY-DEMAND-2026-09-11.md) and
  [frame production](../FRAME-PRODUCTION-2026-09-11.md).
- **Stacked small changes:** bounded logging, allocation-free resource-name
  matching, lighter command-state snapshots, enhanced-barrier filtering and
  direct viewer copying together measured 86.38/86.56 versus nearby
  85.38/85.84 FPS controls, approximately 1%. Individual gains were mostly
  indistinguishable from run variation. One 73.01 FPS run was excluded because
  VD encoding activated while the user was away. Cause remains unconfirmed.
  [Full results](../STACKED-PERFORMANCE-2026-09-11.md).
- **Mirror copies:** implemented a default-off, gameplay-only suppression
  control and opt-in counters. Fixed Steam launch propagation by using
  byte-restored flag files instead of environment inheritance. Same-binary
  on/off/on returned 89.09/89.52/89.06 FPS, approximately 0.5% locally.
  Counters show zero instrumented gameplay copies/blits when disabled.
  [Evidence and limitations](../DESKTOP-MIRROR-PERFORMANCE-CONTRACT.md).
- **CPU diagnosis:** post-ring paired sampling verified idle-worker overlap
  during 25 of 40 observed render-dispatch waits. This points toward work
  availability/tail balance for further investigation, not proof that all
  workers are idle. More workers did not help. Engine preparation/update-link
  sampling did not reveal a dominant cost worth blindly skipping.
  [Paired residency](../ENGINE-PAIRED-RESIDENCY.md),
  [preparation](../ENGINE-PREPARATION-OBSERVER.md).
- **Shadow investigation:** bounded cascade observations and a shadow-atlas
  capture contract identify what must be proved before safe reuse. Matching
  pointers/settings do not establish identical eye work or safe GPU reuse.
  No unsafe shadow skip was introduced.
  [Cascade observer](../CASCADE-STAGE-OBSERVER.md),
  [atlas contract](../SHADOW-ATLAS-CAPTURE-CONTRACT.md).
- **Hub and FG controls:** fixed the offline hub-readiness predicate and
  measured about 92.58 native / 117.35 distinct FG FPS in the hub simulator.
  This did not reproduce the physical 40-native/80-FG report. Later 144 Hz
  tests remained around 120 distinct images plus repeats; title-bar rates
  are not distinct game-frame rates.
  [Hub results](../HUB-PERFORMANCE-2026-09-11.md).
- **Packaging:** added hash-bound per-component build/source records and
  extracted-package verification. Records are claims tied to payload hashes,
  not signatures or proof of reproducibility. Clean-install and early-alpha
  acceptance gates remain open. [Packaging](../RUNTIME-PACKAGE.md).
- **Dependency audits:** documented why the legacy eye mailbox cannot yet
  be skipped safely, and why desktop capture, game mirror, simulator preview
  and VD encoding need separate controls.
  [Legacy dependencies](../NATIVE-RING-LEGACY-DEPENDENCIES.md).

## Validation and retained state

Windows x64 focused builds and relevant tests passed: native capture/barrier
checks, bounded logging/name/snapshot checks, viewer GPU copy checks, mirror
mode/GPU test, package fixtures and PowerShell 5/7 hub-readiness cases. Lua
launch gates remained enabled. Final mirror test passed 1/1; all three valid
mirror runs exited cleanly, restored files and reported zero pose mismatches.
These checks do not establish worn HUD, hand or gameplay acceptance.

- Root task branch: `codex/simulator-desktop-capture-control-2026-09-11`.
  Native mirror control `7d2bda5`, metrics `770ec42`, Steam flag fix `96fe789`.
- Focused mirror branch: `codex/focused-gameplay-mirror-control-2026-09-11`,
  source `7a62226`, based on stacked native `2f2bed3`. Worktree:
  `D:/Projects/games-xr/Darktide VR descriptor demand`.
  DLL: `build/focused-gameplay-mirror-control/src/producer/Release/darktidevr_native_capture.dll`.
  SHA-256 `2250B200F395C85FC508505329B4BC58F974AD53F9E483EA437AEC6DCA2EA012`.
- Accepted installed native SHA-256:
  `FCCDD0DE699F9D50D2BD316792829D4EE5700843D925D194BE4DF1F3EEB02369`.
- Mirror runs used unchanged viewer `a8061e8`, SHA-256
  `216E3F760D1CC9BE5489376EF5DDB0A473F49990F144F0227F8CF87178E429F5`.
- Evidence is ignored under `artifacts/unattended`; detailed documents record
  per-experiment revisions and hashes. Do not substitute accumulated root
  build outputs for the focused identities.

## Later session, 11 September

The user asked for development to continue. Physical Ready failed again
(`hmd-unavailable` through VDXR with the Quest awake), so the work stayed on
the authorized simulator path. Branch
`codex/simulator-window-capture-policy-2026-09-11`, from `8a804bd`.

- [Simulator viewer capture control](../SIMULATOR-WINDOW-CAPTURE-2026-09-11.md)
  closes item 4 below for the simulator: the viewer accepts deferred window
  acquisition and an explicit `always|on_demand` capture policy, the runner
  exposes `-DesktopWindowCapture`, and the analyzer reports capture counters.
  Same-binary always/on-demand/off delivered 89.33/91.65/91.57 FPS (one run
  each; about 2.5% for the legacy always-on loop). Focused viewer
  `E8154112...`; the accepted installed viewer and native files are unchanged.
- The first capture trial exposed a real defect in the on-demand candidate:
  an enabled capture worker ignored the stop request and the viewer hung at
  shutdown. Fixed in `capture_worker.h` with an enabled-shutdown test; rebuild
  from this branch, not `9856e00`, for the pending physical comparison.
- Item 2 lead: `invoke-unattended-preflight.ps1` in Ready mode broadcasts
  `com.oculus.vrpowermanager.prox_close`, which holds the Quest awake as if
  worn for the whole trial. When the headset's Virtual Desktop client is
  connected, that is a plausible trigger for the desktop encoding that
  appeared during the excluded stacked run. Unverified; all four runs today
  observed no Streamer engine activity above threshold.
- Final state: no game or viewer running, accepted native `FCCDD0DE...` at
  both locations, simulator runtime `3C3F41C3...` restored, every run's
  restoration receipt passed, proximity automation restored after each run.
- A [whole-project review](../CODE-REVIEW-2026-09-11.md) followed, and its
  findings were worked through in eight commits on the same branch
  (`d0a8633`..`ad69c7f`): the marker hook vtable off-by-one, viewer
  flat-swapchain guards, Lua per-frame file reads and stuck marker flag,
  byte-exact launcher flag restores, PowerShell 5.1 defects, seven orphaned
  test suites registered, test residue cleanup, formatting and end-of-line
  configuration, a docs index, and a repository garbage collection (11.55 GiB
  of loose objects to a 6 MB pack). Full offline suite: 234/234. Items that
  need a worn check or a focused engine trial are listed in the review under
  "Not done, deliberately". None of the built binaries were deployed.

## Next priorities, set by the user on 11 September

The user has moved the next development focus to two visual defects. Both
need the user wearing the headset for the decisive observations, so prepare
the offline work first and ask for a specific worn comparison, per AGENTS.md.

### 1. DLSS frame-generation blur around HUD objects

Symptom: blur in the world pixels surrounding HUD items and at HUD edges,
distinct from softness inside HUD text and from duplicated/displaced HUD.
Current state, from [DLSS-UI-DETAIL.md](../DLSS-UI-DETAIL.md#blur-around-hud-items):
no cause or fix is established; opaque-text contrast measurements neither
measure nor exclude it; source inspection of the panel draw found no supported
fix; the `ui_render_target_straight_blur` material name is not evidence of a
blur pass. Evidence in hand: the owned-UI/NGX bitmap match is FG-boundary
evidence only; SR color, motion-vector, jitter and history ownership are not
attributed; the SR probe now records jitter/mv-scale/reset/exposure, the
64-evaluation SR capture matches the engine jitter table, camera history is
consistent per viewport, and all sampled SR native command lists match NGX.

Steps:

1. Establish the worn baseline with independent controls. The stock master
   DLSS toggle couples SR and FG; set `dlss` and `dlss_g` individually and
   verify the applied state (`dlss=5`, `dlss_g=1` is the saved configuration).
   Compare HUD visible/hidden with FG off/on at fixed SR, then SR off/on at
   fixed FG, same scene, pose, per-eye extent and streaming settings. Ask the
   user to report three regions separately: opaque HUD interior, translucent
   boundary, and nearby world pixels; and whether blur is present while still,
   during head movement, or persists after motion stops.
2. If FG on/off isolates the symptom, trace where the world-space HUD panel
   enters the FG inputs: the panel is a compositor-side world quad drawn from
   the owned capture target, so check whether its pixels are present in the
   hudless colour, the UI-tagged colour, or only in the final backbuffer that
   FG interpolates, and whether the interpolated frame receives the panel from
   history rather than from the current frame. The packed-colour path is
   documented in [FG-COLOR-PACK-BARRIERS.md](../FG-COLOR-PACK-BARRIERS.md) and
   the tag lifetime in `streamline_continuous_submission.cpp`.
3. If SR isolates it, capture SR inputs at the reconstruction boundary with
   the optional SR probe and the owned-UI exporter (focused `a4ec84c`, PR
   #133, still unvalidated live) before proposing masks, history resets or
   motion-vector changes. Do not subtract HUD-visible/hidden captures.
4. Candidate fixes to evaluate only after attribution: submit the HUD as a
   separate OpenXR quad layer outside the FG/SR image (the viewer already
   composes quad layers), or tag the panel as UI for Streamline so FG treats
   it as an overlay. Each needs a worn A/B on the same scene. Record settings,
   extent and streaming state with every observation.

**Worn result, 11 September evening (accepted installation, hub):** the user
confirms frame generation causes the blur around HUD objects; toggling FG off
in the in-game options removes it with SR unchanged. Arms A3/A4 are therefore
answered: this is an FG-boundary defect, not SR history. Hub delivery on the
accepted build was about 90 FPS with FG on and about 60 with FG off through
Virtual Desktop; the ~120/~89 figures from today's notes came from the
opt-in descriptor-demand + native-ring candidate in the simulator with idle
desktop encoding, and are not installed. Next step for the blur: trace where
the world-space panel enters the FG inputs (hudless colour versus UI-tagged
colour versus final backbuffer) and evaluate the quad-layer or UI-tag fix.

**Two constraints from the user, 11 September evening:**

- Every FG-on comparison today ran under a 120 frame-rate cap (the FG arms
  used cap 120 at 120 Hz, and the 144 Hz control was also capped, which is why
  it only added repeats). "No FG gain" from the descriptor-demand candidates
  is therefore a false negative: a physical FG rate of about 90 sits below
  the cap and can show a gain. Re-test FG candidates on the headset, or
  uncapped in the simulator, before dismissing them. Reversible wrapper:
  `artifacts/unattended/home-fg-trial-20260911/launch.ps1` (EE317801, FG on).
- Frame generation must stay controllable from the standard in-game options
  menu for any mod user. Anything selected at launch by FG mode is a
  non-starter: the native ring publisher is currently skipped when the
  persistent FG launch is requested (`native_capture.cpp`, the
  `streamline_persistent_requested` gate near the flag parsing), so ring and
  FG are exclusive per launch. The ring must be constructed regardless and
  the viewer must switch between generated surfaces and ring originals as
  the in-game toggle changes, with the packed presentation and the
  legacy mailbox dependencies honoured. This is the design task before the
  ring can be promoted.

The FG-off ring trial (`home-ring-trial-20260911/launch.ps1`, D4AB131A) ran
on the headset this evening, twice. The first launch omitted
`-StreamlineEyeTargetProbe`: the ring captures only named eye finals, so it
never attached and delivery fell to about 46 to 48 pairs per second (the user
saw about 50), the descriptor-demand-alone case. With the isolated eye
targets enabled the ring attached (`openxr.original_stereo=attached`, ring
log active at 2496x2688 typeless) and the viewer delivered 68.6 to 70.3
distinct pairs per second in the hub against about 60 on the accepted build;
the user saw 66 to 70 and called it a minor improvement. Two pose mismatches
at the attach transition, none after. This matches the simulator's ~17%
rather than exceeding it: the physical ceiling is dominated by encoding,
VDXR submission and the hub scene, which the ring does not touch. Do not
promote the ring without the in-game FG control design above, and treat the
eye-target dependency as part of its contract.

**FG-on candidate, worn result:** the FG descriptor-demand candidate
(EE317801, `home-fg-trial-20260911/launch.ps1`) delivered about 85 distinct
pairs per second in the hub (42.5 original + 42.5 generated); the user saw
about 87, against about 90 on the accepted build earlier (viewer intervals
up to 95). Toggling FG off in-game on the candidate gave 62 to 65 fresh pairs
against about 60 accepted. No gain either way; the cap false negative applied
to the simulator only. The descriptor-demand change stays unpromoted on its
own, and the only physical gain tonight is the FG-off ring at 66 to 70.

Evening performance summary, all hub, Quality SR, 2496x2688 per eye:

| Build | FG on | FG off |
| --- | ---: | ---: |
| Accepted `FCCDD0DE` | about 90 (viewer up to 95) | about 60 |
| Ring `D4AB131A` + eye targets | not applicable | 66 to 70 |
| FG descriptor demand `EE317801` | about 87 (viewer 85) | 62 to 65 |

**Physical versus simulator gap, attribution plan (user priority, 11 September
evening):** the combined stack (stacked native `0E4718A3`, ring, combined
viewer `E342C7EA`, direct copy, precise pair wait) gives about 60 native /
90 FG physically at Quality against 90 / 120 in the simulator; DLSS
Performance gives about 70 native (background, FG idle) / 104 FG foreground.
Read-only sampling during the live Performance session, all processes:
Darktide 3D 66%, Streamer video encode about 22% per encoder, Streamer 3D
7.4%, viewer 3D 2%, DWM 0; board 84%. CPU: total 49% of 16 logical, game
about 5 cores, Streamer 0.07 core, viewer 0.06 core; the game's busiest
thread is pinned at a full core. So the GPU 3D engine is a quarter idle and
the streaming processes take almost no CPU, yet the game produces 44 to 52
originals per second: the frame is being paced or stretched, not starved of
throughput. In the simulator the presentation thread is 92 to 98% CPU-bound
at 11.8 ms per frame (`darktidevr-present-cpu-*.log`); physically the same
thread is also pinned but delivers roughly half the frames, so its CPU cost
per frame has roughly doubled. Leading explanation: a busy wait on GPU
completion inside the presentation path (Streamline FG, Reflex sleep or a
hook) that turns contended GPU latency (encoder, Streamer passes, VDXR
compositor) into thread time. Reflex cannot be switched off with FG, so the
native FG-off arm is where a Reflex on/off comparison can confirm
latency-binding.

Prepared: `artifacts/unattended/home-physical-profile-20260911/launch.ps1`
runs the same stack at Quality FG with the presentation-thread CPU profile
(240 samples) and the native GPU per-eye profile, copies both logs into the
session directory and restores everything; `tools/stereo/summarize-present-cpu-profile.py`
summarises them. Compare against the simulator references: presentation
thread 11.79 ms wall / 10.81 ms CPU (share 0.92, about 85 FPS native), GPU
per eye 6.7 / 11.8 ms, interval sum 18.5 ms. Outcomes: CPU share near 1 with
doubled wall means spinning or extra work on that thread (then sample its
stacks with `capture-thread-residency.ps1`, adapted for a physical launch);
CPU share well below 1 means genuine waiting (pacing or GPU latency); GPU
spans doubled means the GPU frame itself is stretched by contention.
Candidate remedies by outcome: lower the viewer's queue contention and
copies, keep DLSS Performance, reduce encoder load (bitrate, codec), and
only then engine-side changes.

**Physical profile result (11 September evening, `home-physical-profile-20260911`):**
same combined stack, Quality FG, hub, foreground. The viewer delivered
47.6 to 48.3 originals plus the same generated per second (about 95 to 97
distinct), with an 8.3 ms loop, 0.9 ms fence wait and no pair wait, so the
viewer is not the limiter. The game's own GPU per-eye spans (native
`DARKTIDEVR_GPU_PERF`, 23 reports after warm-up) average **10.1 ms left,
15.3 ms right, 25.5 ms per stereo frame**, against **6.7 / 11.8 / 18.5 ms**
for the same workload in the simulator: the game's command lists take about
38% longer wall time on the headset. With the game's 3D engine share at only
66%, the stretch comes from other GPU clients interleaving with its frame
(the Streamer's per-display-frame 3D passes at 7%, two encoders at about 22%
each on HEVC 10-bit, VDXR's compositor, the viewer's 2%), plus the
preemption and memory traffic they bring. This is the physical-versus-
simulator gap; it is not viewer waits, not CPU competition and not the
ring/legacy handoff. The presentation-thread CPU profile is absent from this
run because that profiler only exists in the focused `83E4D445` ring-cpu
build, not the stacked native.

Levers, in order of expected return: reduce the game's GPU span (DLSS
Performance already gives about +15% physically; keep it as the default
candidate), reduce concurrent GPU clients (VD codec/bitrate/sharpening, the
mod's own copies), and test hardware GPU scheduling on/off (registry
`HwSchMode` was unset). A live bitrate/codec A/B while watching the
`DARKTIDEVR_GPU_PERF` spans in the console log is the cheapest next
discriminator. Two operational defects fixed on the way: the previous
session's viewer `0c047c0` hung at exit on the capture-worker shutdown bug
(it predates `5b058cc`), stalling both wrappers; it is rebuilt with the fix as `baf0807` on
`codex/physical-viewer-worker-fix-2026-09-11`, viewer SHA-256
`437F6D971E50D9FD1C766FC075E373E7FDF51457BBDC4E0A01B04F33A6DD25C0`
(worktree `D:/Projects/games-xr/Darktide VR physical viewer fix`,
`build/viewer`). Simulator proof: closing the captured window ends the fixed
viewer in 0.29 s with `lifecycle=stopped`/`result=pass`; the unfixed
`E342C7EA` viewer never exits. Use the fixed viewer for every physical
launch from now on. Physical launches do not print the console log path, so
the profile wrapper now takes the newest console log written after launch.

**Encoder controls, live in the same session (`home-physical-profile-20260911`
run 2, fixed viewer `437F6D97`, foreground hub segments only):**

| Virtual Desktop setting | Game GPU span per stereo frame | Originals/s | Distinct delivery |
| --- | ---: | ---: | ---: |
| HEVC 10-bit, 200 Mbps | about 28.6 ms (26 to 33) | 46 to 48 | 91 to 96 |
| HEVC 10-bit, 50 Mbps | about 27.0 ms (25 to 28) | 43 to 46 | 88 to 92 |
| H.264+ | about 25.6 ms (24.5 to 26.8) | 44 to 49 | 86 to 97 |

Bitrate is a few percent at most; H.264+ shortens the spans by about 10%
without a delivered-frame change (the user saw 97 at the top of the range).
FG evaluation GPU time is 1.84 ms physically against 1.76 ms in the
simulator, so FG itself is not stretched. Background intervals (Virtual
Desktop menu open) must be excluded: FG idles there and the right-eye span
halves on its own.

Reference caveat: the 18.5 ms simulator span reference comes from the
mission-start workload (both 2112x2304 and 2496x2688 runs give 18.5 to
19.7 ms, so resolution alone does not move it). No simulator GPU profile of
the **hub** exists, and the physical measurements are all hub. The remaining
gap (25.6 versus 18.5 ms with the lightest codec) may therefore be scene and
FOV rather than streaming contention. Next control, needing the game closed:
a simulator hub run with `-GpuProfile` at 2496x2688, Quality, FG on. If hub
spans are about 25 ms in the simulator too, the physical stack is at parity
and the gap was a scene mismatch in earlier comparisons; if they are about
18 ms, contention from VDXR compositing and preemption is real and hardware
GPU scheduling and compositor load are the next levers.

**Simulator hub control (`synthetic-hub-gpu-profile-a-20260911`, same stacked
native, fixed viewer, direct copy, Quality, FG requested, 2496x2688, hub):**
the game's GPU per-eye spans are **7.2 ms left, 10.1 ms right, 17.3 ms per
stereo frame** (28 reports after warm-up, 109 s analysed, zero pose
mismatches) against **25.6 ms** on the headset with the lightest codec and
28.6 ms with HEVC 10-bit. Same scene, same resolution, same binaries, so the
physical stretch of about 50% in the game's own GPU frame is real and is
not scene, resolution, viewer waits, CPU competition, bitrate or codec. What
remains is the GPU work and preemption that only exist on the headset path:
VDXR's per-vsync compositing and the Streamer's per-frame passes
interleaving with the game's command lists. Next levers: hardware GPU
scheduling on/off (registry `HwSchMode` is unset today; needs a reboot per
arm), then minimising what the viewer submits per vsync (layer count,
submission cadence) and keeping DLSS Performance as the headset default.

Defect surfaced by this control: with this stack in the simulator, FG
evaluated but never completed or published from launch (`evaluations=4476
complete=2 paired=1 published=0`, `generated_submitted_frames=0`), so the
runner rejected the FG arm and the engine paid for evaluations at 44 FPS
with native-only delivery. This is the same signature as the DLSS
quality-change recovery failure already on the todo, now reproducible at
launch offline. Physically the same stack published FG normally. Investigate
before any FG-related stack is promoted; the eye-span comparison above is
unaffected because evaluations were running in both.

**Virtual Desktop resolution one step down (run 3, VDXR recommends 2112x2304,
H.264+, same stack, foreground hub):** game GPU spans **7.7 to 9.8 ms left,
12.4 to 14.0 ms right, about 21.9 ms per stereo frame**; delivery **54.5
originals plus 54.5 generated, 109 distinct** (user counter about 110).
Against 2496x2688 on the same evening (25.6 ms, 44 to 49 originals, 86 to
97 distinct) that is a 15 to 23% shorter GPU frame for 38% fewer pixels,
and delivery within about 8% of the simulator's 117 to 120. The simulator's
spans hardly move with resolution (17.3 to 18.5 ms at either size), so the
physical stretch scales with the amount of per-frame GPU work interleaved
with the streaming clients, which is what contention predicts. HAGS cannot
be disabled (required by DLSS frame generation), so that lever is closed.
Practical settings for the headset today: VD resolution one step below the
top, H.264+ if its quality is acceptable, DLSS Performance; each is a
settings recommendation, not a mod change.

**Closing state after the evening (11 September, about 18:40):** no game or
viewer running; accepted native `FCCDD0DE...` at both locations, installed
viewer `36D60D68...`, simulator runtime `3C3F41C3...`, no ring/profile flags,
every trial wrapper's restoration receipt passed, proximity automation
restored. Nothing experimental is installed. Headset settings the user
tested and kept: VD resolution one step below the top (2112x2304 per eye),
H.264+, DLSS Performance; the saved game configuration is back at Quality
with FG on, so set Performance in-game before comparing.

**DLSS quality-change FG defect, fixed offline (11 September, evening):**
the user asked for the defect where changing the DLSS quality in-game with
frame generation on breaks the mod's FG path to be fixed next. It reproduced
offline with a test-only Lua flag action that applies the quality change the
way the options menu does (FG-tested baseline native, simulator hub): the ring
paused on `binding_rejection` at the switch and never resumed while DLSS-G
evaluations continued. Cause: applying render settings makes the engine
register new Streamline per-eye viewport handles and re-create depth/motion
at the new internal resolution; the capture gate and ring kept the startup
handles and extents, so every later frame was rejected on the viewport
identity check. The fix follows the engine (viewport migration in the gate
and ring, ring texture reallocation once paused and idle, old DLSS-G options
slot released, bounded `gate_reject` diagnostic). Verified in
`synthetic-dlss-quality-recovery-{e,f}-20260911`: both switch directions
recover within one present (`viewports`, `reallocated`, `resumed`) and
publication continues; WARP test `continuous_recovery` covers it. Native
`F5799F85` (branch `codex/fg-quality-switch-recovery-2026-09-11`, commit
`158a50f`) is the offline-verified binary; the port on this branch builds and
passes the WARP test but its lineage still fails FG offline, so it is not
offline-verified. Worn verification passed on the second attempt (20:05,
`F5799F85`, window kept foreground; the first attempt was inconclusive because
the game window lost OS foreground and DLSS-G stopped evaluating). The same
session surfaced two mission observations recorded on the todo: the FG ring
rejects every present in a remote-server mission after migrating to its
viewports, and tracked hands/input are absent there by the documented
remote-mission gate. Details:
[FG-QUALITY-SWITCH-RECOVERY-2026-09-11.md](../FG-QUALITY-SWITCH-RECOVERY-2026-09-11.md)
and the physical wrapper `artifacts/unattended/home-fg-quality-switch-20260911/launch.ps1`.
The reproduction wrappers now take `-SettleSeconds`/`-SoakSeconds` (30 s
each by the user's request) instead of the original 60 s waits.

**Performance bundle deployed (11 September, about 21:00):** at the user's
request the day's performance work is now the installed baseline. This
branch already carried re-applied versions of every focused optimisation
and the FG completion-API chain, so the bundle is this branch head built
into the launcher's tree and deployed by the ordinary sync (transaction
`deployment-f6350854edec4ce299d69aa1002cd721`; native `CAF9E0DF...`,
bootstrap `E7ED3929...`, Lua `D3E733CD...`). The launcher now sets the
viewer's precise pair wait and direct native originals by default
(`-LegacyViewerTiming` opts out) and enables the native original ring with
isolated eye targets for native stereo launches (`-NoNativeOriginalRing`
opts out). Offline controls: 89.5 native, 114 distinct FG with the accepted
viewer, 110 steady with the bundle viewer, FG complete in a SoloPlay
mission; details, limits and the rollback command in
[PERFORMANCE-BUNDLE-2026-09-11.md](../PERFORMANCE-BUNDLE-2026-09-11.md).
Worn acceptance of the bundle is the next session's first check. The
`FG completion failure offline` todo item was a property of the focused
stacked build, not of this branch: the branch native evaluates, completes
and publishes FG offline in both hub and mission.

**Remote-mission FG rejection loop, fixed (about 21:30):** the ring now logs
both eyes' Present bindings on a rejection (`phase=binding_detail`,
bounded). The offline hub-to-range reproduction crashed twice on a stock
`player_husk_locomotion_extension` Lua error (`parent_unit_id`) while leaving
the online hub, and SoloPlay has no live-ring transition, so the evidence
came from a worn run (`home-mission-fg-diag-20260911/`, bundle native, a
dedicated-server mission): every rejected binding after the mission load
carried DLSS-G `options mode=0`, i.e. the engine had generation off during
the mission intro; the ring thrashed only while that lasted, then FG
published normally in the mission (the user saw a solid 120). The capture
gate now holds the ring paused (`generation_off`) while the viewport's
options report mode 0, verified offline with the in-game FG toggle (one
pause, one resume, no failures) and deployed as the second sync
(`deployment-2d3e6bfc2f174649bc7b17ce0ec7747e`, native `5B7D5B9B...`, Lua
`50B1F10F...`). The installed state is therefore that second sync, and
the test flag action accepts `fg_off`/`fg_on` in addition to quality names.
Worn confirmation at 21:38 (`home-mission-fg-diag-20260911/`, attempt 2):
FG held through a dedicated-server mission; one pause and one resume around
the mission load, the `generation_off` hold through the intro, no failures,
63 to 67 engine FPS with 62 published pairs per second in the mission.

### 2. Cylindrical billboarding for atlas particle displays

Current state, from [ATLAS-BILLBOARD-CANDIDATE.md](../ATLAS-BILLBOARD-CANDIDATE.md)
and [ATLAS-BILLBOARD-HOME-TRIAL.md](../ATLAS-BILLBOARD-HOME-TRIAL.md): the
accepted installation already substitutes the raw-particle vertex shader
`vs-42e436fb1ef1b392.dxil` through the PSO-time replacement path (presence flag
`darktidevr_billboard_shader_substitution.flag`). The three atlas-display
families (`c403...`, `e18a...`, `fe640...`) have reconstructed-stock and
cylindrical variants that compile, pass the runtime interface gate with the
installed DXC 1.6 reflection library, pass 692 basis cases and 108 output
cases per shader on WARP and the RTX 4090, but they are staged only: not
installed, not packaged, never observed worn. The passive readback shows
`e18a...` and `fe640...` affect background/floor haze in character select;
the smoke family the user sees is still not attributed. The native
horizon-lock/selector path is a retired diagnostic, not the production route.

Steps:

1. Attribute the visible smoke/haze draws in the scene the user cares about
   with a bounded observation (draw census plus readback), not cached shader
   presence. Prefer a mission or hub scene the user can reproduce.
2. Run the prepared home trial in [ATLAS-BILLBOARD-HOME-TRIAL.md](../ATLAS-BILLBOARD-HOME-TRIAL.md):
   Ready preflight, `apply-trial.ps1 -Profile stock` as the control, worn
   comparison with natural head movement, restore, then `-Profile cylindrical`
   on the same scene. Rebuild the candidates from the reviewed source with
   `build-atlas-billboard-candidates.py` into a fresh directory if the staged
   artifacts are older than the current native interface gate. The trial
   changes only the two atlas shader files and is transactional.
3. Ask the user for a specific observation: per-eye smoke orientation under
   yaw, pitch and roll, authored spin still present, no new distortion near
   the vertical pole, and behaviour after recentering. Accept only on the
   worn report; restore on adverse or inconclusive results.
4. On acceptance, add the accepted family files to the runtime package list
   and record hashes and rollback. Keep `c403...` excluded until it is
   attributed. Selective smoke suppression stays a separate deferred feature.

Performance work continues below only as time allows behind these two items.

## Next session, after authorization

1. Keep 120 distinct FPS the core goal at 2496x2688 per eye, Quality DLSS;
   144 Hz is secondary. Continue using matched mission, foreground policy,
   versions and original/generated/distinct/repeat accounting.
2. Investigate controlled active VD streaming overhead and the spontaneous
   encoding transition. Final mirror runs observed no Streamer activity, but
   incomplete counter coverage prevents claiming zero encoding. Physical
   Ready was unavailable during the mirror batch; no headset result is implied.
3. Mirror-copy suppression is a small, single-candidate-run signal. Retain
   default-on mirror. FG packed presentation, resizing, hidden/absent-window
   behavior and menu recovery remain untested. Window/HUD pointer dependencies
   are real; hiding a window is not proof of eliminating render work.
4. Viewer on-demand desktop capture remains a separate pending test. The
   simulator currently does not capture the desktop; adding capture requires
   correct game-window startup ordering. No capture-enabled launcher was
   implemented in this task. Rebuild focused `9856e00` before comparing it.
5. Continue investigating render work availability and safe engine savings;
   honor the shadow and legacy-mailbox dependency contracts. Avoid unsafe
   copy/wait skips based on matching pointers or average FPS alone.
6. DLSS blur around HUD, pickup asymmetry and alpha release gates remain open.
   Physical melee remains paused; the user requested no basic gameplay checks.

Run Ready before any new live deployment/session, preserve exact restoration
and one branch/worktree per change, and obtain worn observations for visual
acceptance. Do not restart VD merely because streaming is suspended.
