# 8 September workday continuation

The user restarted development and the 20-minute heartbeat, explicitly in this
session, until told to stop. The prior task has stopped; this task owns the
checkout on `codex/dlss-readback-validation-2026-09-08`, stacked on
`codex/gpu-trace-integrity-2026-09-08` and the two-hand branch
`codex/two-hand-grip-2026-09-08`. Yesterday's stop instructions are
historical. Preserve the user's untracked image and the accepted `ab9e9db` gun
pitch, hand alignment and draw/reload behavior.

## Offline baseline

Windows x64, headset tests OFF. Full CTest run at `52fdd88`: 125/126 pass in
22.57 seconds. `hud_options` assumed fixed top-level menu positions and failed
after the accepted gun-pitch setting was added. The test now resolves settings
by ID, rejects duplicate IDs and retains the HUD/turning/binding group checks.
No runtime change. The affected HUD, gun aim, Lua compiler and invariant checks
then passed 4/4 in 0.83 seconds. This is a full run plus a focused repair rerun,
not a new single-run 126/126 claim.

Commands (CTest is installed in Visual Studio's CMake/bin directory):

```powershell
ctest --test-dir build/windows-vs2022 -C Release --output-on-failure
ctest --test-dir build/windows-vs2022 -C Release --output-on-failure -R '^(hud_options|gun_aim|lua_source_compile|lua_source_invariants)$'
git diff --check
```

## Quest setup

The user prioritized Quest setup before further implementation. Proximity
Disable then Status succeeded. Default Ready passed: one authorized Quest,
Streamer and VDXR available, 600/600 submitted frames, no unrendered frames.
All 43 Lua chunks compiled. Evidence is ignored under
`artifacts/unattended/preflight-20260907T194407Z.json`.

Started the ordinary launcher with `-EnterPsykhanium -EnableHudPanel
-DlssGeneratedStereo` while Darktide was closed. Initialization is being checked
in `artifacts/unattended/workday-session-20260908.log`. Proximity remains applied
for active development; restore with Enable and Status when development ends.
The readiness result alone is not gameplay stereo or worn acceptance.

Setup completed: the title screen needed a foreground Space press. Automatic
character selection then continued. Fresh range result=pass at 19:48:13 UTC,
synchronized sequential stereo at 19:48:13 and both rigid hands ready at
19:48:14. Harness shared_ready=704 with zero interval fallback and zero pose
mismatches. No mod WARNING/ERROR in the checked fresh console. Game and launcher
remain running for development; no new worn acceptance is claimed.

## Next development

Continue contextual two-handing/ADS from `docs/VR-TWO-HANDING-ADS.md`. Input
ownership, numeric pose solving and the production coordinator are implemented
offline. Ten focused checks passed in 1.11 seconds; the subsequent seven affected
checks passed in 1.16 seconds, including all 45 Lua chunks. The candidate defaults
disabled with no guessed weapon sockets. Session-only tracked socket capture
commands now pass 4 focused checks in 0.97 seconds, but are not deployed or worn
tested. Support-hand visual placement is now implemented; the complete offline
suite passes 128/128 in 21.88 seconds (45 Lua chunks). Virtual-stock math is
tested but lacks a production shoulder anchor; toggle-ADS coordination remains.
PR #3 contains this branch against the aggregate branch in PR #2. See the implementation
checkpoint in `VR-TWO-HANDING-ADS.md`. Preserve independent bindings, neutral guards, staff and
accepted controller-based gun aim. Remaining weapon-family worn checks require
the user's observations and should not block independent offline work.

The fixed-input interruption audit found that a null service between render
samples could leave an active calibration countdown alive. Fixed cancellation
now retires a started measurement while preserving its initial chat-close wait.
Validation: `ctest --preset windows-vs2022-debug -R 'two_hand|gameplay_ui_ownership'
--output-on-failure` passed 3/3 in 0.08 seconds;
`tools/stereo/test-darktide-lua-source.ps1` compiled all 45 chunks. This source
change remains undeployed with the rest of the two-hand candidate.

The subsequent mapper audit reproduced a swallowed real aim-button release
when support cancellation happened in the same sample. The mapper now tracks
which physical controls actually contributed, preserving a healthy alias's
release while still suppressing cancelled-only edges. The equivalent axis-loss
case is covered, including a blocked alias that must not manufacture a release.
Full Windows x64 Release CTest: **128/128 pass in 24.07 seconds**, headset tests
OFF; command `ctest --test-dir build/windows-vs2022 -C Release --output-on-failure`.
All 45 Lua chunks compile. Latest evidence can be copied from
`build/windows-vs2022/Testing/Temporary/LastTest.log` before another run.

The two-hand branch ends at `d579634`; the full-suite evidence is preserved in
`artifacts/unattended/two-hand-input-audit-128-20260908.log`. Independent offline
performance-tool work continues on the GPU trace branch. PR #4 adds capture
integrity and render-eye checks to the analyzer, stacked on PR #3. Its focused
regression and two saved real trace analyses pass; see
`docs/PERFORMANCE-HEALTH-ANALYSIS.md`. Live settings and the accepted running game
remain unchanged. Keep working in this session until explicitly told to stop.

GPU analysis checkpoint: `451b8e1`, PR #4. The next offline task on the DLSS
readback branch rejects bitmap/log dimension mismatches in both UI measurement
CLIs. Two affected CTests pass in 1.22 seconds; the original saved readback
retains its previous measured values. See `docs/DLSS-UI-DETAIL.md`. These are
diagnostic integrity changes, not rendering changes or resolution of motion blur.
