# 8 September workday continuation

The user restarted development and the 20-minute heartbeat, explicitly in this
session, until told to stop. The prior task has stopped; this task owns the
checkout on `codex/shader-runtime-dependency-2026-09-08`. Reviews are stacked in
the checkpoint order below. Yesterday's stop instructions are
historical. Preserve the user's untracked image and the accepted `ab9e9db` gun
pitch, hand alignment and draw/reload behavior.

At 09:57 the user ordered billboarding fixes immediately after the pre-launch
TODO list is clear. Finish that work first; do not insert unrelated polish.
Worn checks cannot be marked complete while the user is away. Physical melee
remains paused under the earlier server-side melee priority.

The user also reports unequal pickup-popup sizing between eyes because the
popup shrinks near the screen edge. Add the fix and a pass over all similarly
behaving prompts/markers to the active TODO list. This supersedes the historical
classification of slight edge asymmetry as optional polish; do not mark it
accepted from tracking/rendering counters. Broader worn edge/center checks are
required once the correction is ready.

The [pickup sizing audit](../STEREO-MARKER-SIZING.md) now enumerates related
marker families and center/edge acceptance cases. No sizing root cause or
visual correction is claimed. The [billboard investigation](../BILLBOARD-INVESTIGATION-2026-09-08.md)
records the original shader evidence and an offline candidate correcting
concurrent identity-log appends. Production shader targeting and the current
live session are unchanged.

Validation for the append correction: Windows x64 Release native capture and
the new diagnostic test build pass. Full CTest passes **142/142 in 31.23 s**,
including the pinned Lua gate. Evidence:
`artifacts/unattended/billboard-diagnostics-142-20260908.log`. Inventory at
10:35 Brisbane records one authorized awake Quest, proximity false, display
suspend blocker held, one Darktide and zero crash reporters. This observation
is not a new Ready certification. The running session advances shared_ready
past 270,234 with zero reported reuse, fallback or pair-pose mismatches at that
sample; the frame-rate sample is not a controlled benchmark.

Billboard diagnostic append correction and the related marker sizing TODO are
committed as `866b5aa`, pushed and reviewed in PR #29 on top of runtime-package
PR #28. A second offline correction now preserves pixel-only probes through
cached pipeline-stream loads. Actual isolated D3D12 tests reproduce the old
failure and pass after the fix, including stock fallback on rejected creation.
Release build and **144/144 CTests pass in 31.15 s**. Evidence and limitations
are in the billboard investigation above. No live files were changed.

The new isolated shader test exposed a separate clean-install dependency gap:
native shader validation explicitly loads `dxcompiler.dll` beside the capture
module, but the current sync and runtime package do not provision it. The live
installation already has that library (and `dxil.dll`) from earlier setup;
this does not establish that a clean destination has it. Resolve the package
dependency before calling the pre-launch installation path complete.

Cached pipeline probe correction is committed as `d853f67`, pushed and reviewed
in PR #30 atop PR #29. The clean-install dependency candidate now pins the
official DXC `v1.8.2505.1` x64 reflection DLL and all three archive notices.
Archive SHA256 comes from the upstream release asset metadata; file hashes are
checked at preparation, package validation and sync. It is installed through
the existing recoverable transaction. No compiler executable or DXIL validator
DLL is needed in the runtime payload: both isolated D3D12 probe tests pass with
only this pinned reflection DLL beside the native module. The existing worn
deployment retains its earlier reflection DLL; no live files were updated.

Dependency validation: isolated preparation from the hash-verified upstream ZIP
and repeat preparation both pass. The deployment test verifies DLL/notices,
rollback and rejection of altered runtime bytes. Full CTest passes **144/144
in 32.40 s** (`artifacts/unattended/dxc-runtime-144-20260908.log`). The rebuilt
95-file package was extracted separately, passed its pinned DLL and 48-chunk
Lua gates, and its actual extracted native/DXC files passed the D3D12 cached
probe application test. Evidence: `dxc-runtime-package-20260908.log` and
`dxc-runtime-package-extracted-20260908.log` under `artifacts/unattended`.
This supersedes the earlier 89-file candidate, which lacked the required
reflection dependency. No fresh-game installation or worn acceptance is claimed.

Dependency implementation is committed as `124efc2`, pushed and reviewed in
PR #31 atop PR #30. Final 95-file archive:
`artifacts/packages/darktidevr-candidate-124efc22778f-bd4828a4.zip`, SHA256
`1CD7E2334BADD949BEB62E350C935CEF2F1F4DD627E17CC1EDE22368046F5277`.
Receipt: `artifacts/unattended/dxc-runtime-final-receipt-20260908.json`.
The source state remains dirty only because of the preserved, excluded user
image; do not remove it to alter that label.

## Latest live deployment, 08:27 Brisbane

This supersedes earlier "undeployed" notes for the focused preview/visual fix
only. Deployed four Lua files from focused PR #11 at `8efdf66`, verified against
accepted baseline `ab9e9db`; the other stacked changes are still offline. Native
capture, D3D12 bootstrap and harness SHA256 identities were preserved. Backup
and rollback manifest: `artifacts/unattended/melee-preview-live-backup-20260908/`.

Stopped the previous XR owner; its launcher cleaned up its owned game. Default
Ready passed 600/600 with no unrendered frames; evidence:
`artifacts/unattended/melee-preview-ready-20260908.json`. The focused source gate
compiled 45 chunks, and installed gate compiled 46 because an existing unused
`darktidevr_gun_alignment.lua` was preserved. Relaunched with `-SkipDeploymentSync
-EnterPsykhanium -EnableHudPanel -DlssGeneratedStereo`; game was closed when armed.

New launcher session **67427**, Darktide PID **123208**, start **08:25:24 Brisbane**.
The launch uses the default 28,800-second lifetime, approximately **16:25 Brisbane**.
Do not assume it lasts until home tonight: monitor this deadline and renew the
live session through the normal closed-game/Ready workflow if the user has not
told us to stop. The active 20-minute heartbeat retains this same-session duty.
Keep this session running. Fresh range pass 22:27:04 UTC, online rules enabled
and both rigid hands ready at 22:27:05. Shared_ready=1068 with zero interval
fallback/mismatches; this is delivery evidence, not a controlled benchmark.
No matching mod errors found. Preview defaults off and requires the user's worn
check; melee animation alignment and actual server damage are not accepted yet.

Session log: `artifacts/unattended/melee-preview-session-20260908.log`.
Console: `console-2026-09-07-22.25.25-9a2c8520-8131-402a-970c-623beaa01bfa.log`
under the ordinary Fatshark console_logs directory. Last observation before
restart: Quest awake, external AC power, battery 93%, one Streamer, no crash
reporter. Proximity override remains applied until development ends.

## Earlier offline checkpoints

The standalone development package uses an explicit file allowlist and hashes;
it contains no game executable, user settings, device identifiers or session logs.
Release native build and production shader build pass. The 89-file archive was
extracted separately and passes Windows PowerShell validation, its bundled
48-chunk Lua gate and production shader identities. It correctly reports no Git
checkout even beneath the repository's artifact directory. This is not live
installation or worn acceptance. Full CTest passes 141/141 in 29.74 seconds.
Evidence: `artifacts/unattended/runtime-package-141-20260908.log`,
`runtime-package-extracted-20260908.log` and `runtime-package-receipt-20260908.json`.
The initial archive/extraction records a dirty source state while packaging code
was being prepared; regenerate after committing for the checkpoint revision.
An automatic review rejected the combined temporary extraction/cleanup command
with only "blocked by policy"; a safer extraction-only command passed, retaining
the evidence under `artifacts/x/`. No current game files or XR session changed.
Portable launch paths are committed at `d6584f4`, reviewed in PR #27.

Runtime package checkpoint `5482653` is reviewed in PR #28. Final candidate:
`artifacts/packages/darktidevr-candidate-548265387f18-4639c5b2.zip`, SHA256
`B89AB50FCE47A6C373D0CB4F9060D960BEE24C05ABC954BF3C09CC7F8F893536`.
All 26 packaged PowerShell scripts parse under Windows PowerShell. The manifest
truthfully records the checkout dirty because the user's untracked image remains;
the image is excluded from the package. The final receipt is
`artifacts/unattended/runtime-package-final-receipt-20260908.json`.
Inventory at 10:12 confirms one awake Quest, one game, zero crash reporters;
separate battery observation: external AC true, level 92%, temperature 45 C,
status 3 (do not describe this as active charging).

Asked whether "pre-launch" means launch/install checks or all remaining features
such as handedness. No answer arrived before continuing independent validation.
After allowing over a minute, explicitly assumed launch/install checks for now
and began the requested billboarding source investigation. Feature, mission and
worn checks stay open; this is not a claim that the whole release TODO is complete.

The default XR harness path now resolves from the runner script's directory,
so launch does not depend on the caller's working folder. Shortcut creation can
retain the explicit prebuilt production shader option for packaged deployment.
PowerShell parsing and four focused launcher checks pass; prior full baseline
is 140/140. No live shortcut, game or headset state was changed.
Portable readiness metadata is committed at `26d2336`, reviewed in PR #26.

Readiness source identity is now optional metadata: absent Git/checkout or failed
queries report unavailable revision/dirty state without weakening XR readiness.
Extracted packages cannot inherit a parent repository's identity. Metadata queries
preserve the caller's native exit status. Full CTest passes 140/140 in 28.91 seconds,
including 48 Lua chunks; `artifacts/unattended/portable-readiness-140-20260908.log`.
Actual Inventory at 09:53 confirms the source checkout is identified, one awake
Quest, one game and zero crash reporters; shared_ready=175977, zero fallback or
pose mismatch. Evidence: `artifacts/unattended/workday-inventory-20260908-portable-readiness.json`.
Prebuilt production shader support is committed at `438792a`, reviewed in PR #25.
All new candidates remain undeployed.

The optional prebuilt production shader path validates build profile plus
source/output hashes before sync. Ordinary development keeps its default DXC
build. Both launcher entry points forward the option. Actual isolated DXC builds
pass production acceptance and diagnostic-scale rejection; fake-install checks
also reject stale source, changed bytes and missing metadata without writes.
Recovery is committed at `36445a6`, reviewed in PR #24. All remain undeployed.
Full offline CTest passes 139/139 in 29.58 seconds, including 48 Lua chunks.
Evidence: `artifacts/unattended/prebuilt-production-shader-139-20260908.log`;
actual shader outputs/logs are in the adjacent production/diagnostic-shader artifacts.

Saved deployment manifests now have a closed-game recovery command. Original
hashes/root/scope are checked before writing; changed installed files require
explicit selection and are backed up. Interrupted mixed state, failed recovery
rollback, repeat recovery and unrelated folder contents have isolated fixtures.
Source-tree clean installation is committed at `775abf7`, reviewed in PR #23.
All setup/recovery work remains offline; the focused live preview is unchanged.
Full offline CTest passes 139/139 in 28.81 seconds, including the 48-chunk Lua
gate. Evidence: `artifacts/unattended/deployment-recovery-139-20260908.log`.

Source-tree sync now supports explicit `-InitializeInstall` against an existing
Mod Loader/Framework setup. Missing VR directories and the appended mod-list
entry participate in verified rollback; existing list bytes/order and ordinary
sync semantics are retained. Full offline CTest passes 138/138 in 26.21 seconds,
including 48 Lua chunks. Evidence: `artifacts/unattended/clean-install-138-20260908.log`.
Deployment rollback is committed at `df2e66f`, reviewed in PR #22. Neither setup
candidate was deployed. At 09:40 Brisbane, Inventory confirms one awake Quest,
one game and zero crash reporters. Shared_ready=142319, zero fallback/mismatches;
this is delivery observation, not performance or worn acceptance. Inventory
evidence: `artifacts/unattended/workday-inventory-20260908-clean-install.json`.

Development sync now stages and verifies every file/flag before installed writes,
with verified reverse rollback on caught failure. Temporary filesystem and real
sync fixtures cover a locked final write, unchanged installation after rollback,
successful update and Lua-gate rejection. The normal CTest launch needed an
explicit Utility module import in each PowerShell test entry point. No live
deployment was performed. See `docs/DEPLOYMENT-TRANSACTIONS.md` for recovery limits.
Support calibration ownership is committed at `9b5b0ad`, reviewed in PR #21.
Full offline CTest passes 137/137 in 25.08 seconds, including the 48-chunk Lua
gate. Evidence: `artifacts/unattended/deployment-rollback-137-20260908.log`.

Measured two-hand profiles now retain their physical support side. Regression
failed before the fix; six focused tests pass including the 48-chunk Lua gate.
The opposite hand cannot inherit the measurement and role changes cancel an
active countdown. Gun alignment is committed at `5b2fcba`, reviewed in PR #20.
All remain offline candidates; preview/accepted native live session is unchanged.

Gun alignment now supports either known dominant grip with destination-glove
basis conversion and attachment rollback when the opposite glove cannot place.
Default right-hand behavior is retained. Full offline CTest 135/135 passes in
22.91 seconds; 48 Lua chunks compile. Evidence:
`artifacts/unattended/handedness-gun-135-20260908.log`. Weapon sound owner fix
is committed at `66a10ee`, reviewed in PR #19. Neither candidate is deployed.

Local VR initial weapon sound registration now selects the matching visible 3P
slot when its ownership/attachment tree is valid. Stock VFX already follow the
3P equipment selection; no global effect/attack pose override was added.
Full CTest 135/135 passes in 22.76 seconds, followed by a focused retiring-tree
guard check. Optional actual-stock registrar check passes. Attachment foundation
is committed at `8483bbb`, reviewed in PR #18. Live preview remains unchanged.

Handedness attachment foundation now maps tracked rigid equipment by role while
retaining each weapon hand's authored anatomical basis. Per-hand placement flags
prevent the other successful glove from admitting stale equipment coordinates.
Default right-hand behavior is retained; left-handed settings remain unexposed.
See `docs/HANDEDNESS-AUDIT.md`. Integrated preview is reviewed in PR #17.
Full offline CTest passes 134/134 in 22.74 seconds; 47 Lua chunks compile.
Evidence: `artifacts/unattended/handedness-attachment-final-134-20260908.log`.

Integrated the focused display diagnostics and unknown-hand-role guard at
`29e965a`; preview module/display and display tests now match the focused branch.
Full offline CTest 134/134 passes in 23.36 seconds; LuaJIT gate compiles 47 chunks.
Evidence: `artifacts/unattended/melee-preview-integration-134-20260908.log`.
No live deployment changes. Exported-spline check is reviewed in PR #16.

Real exported-spline preview contract passes seven force-sword damage-window
frames at three reference orientations. Full stock spline evaluator and actual
matrix data agree with independent raw-matrix base/center/tip calculations.
Value types and action parameters are fixtures; worn rendering remains pending.
Force-sword parser contract is committed at `ddcc7bb`, reviewed in PR #15.

Force-sword parser extension passes all three actual weapon input overrides:
light/heavy release, sustained quell/release, primary target release and
alternate-only release preservation. Both full parsers agree through the stock
in-memory send/receive fixture. No target finding, peril or live damage claim.
Validation: pinned LuaJIT command in `docs/STOCK-MELEE-INPUT-CONTRACT.md`.

Latest optional source check executes the stock final melee attack dispatcher:
positive client-side calculated damage does not invoke the health sink; only
the allowed server branch does. Block-state ownership and protected-target
skips pass. Calculation/health math are fixtures. See
`tests/tooling/test-melee-authority-stock-contract.lua` and the stock input
contract document. This is stock button-melee work; physical melee stays paused.
Visual correction is reviewed in PR #13. Focused PR #11 is now `8efdf66`
(runtime correction `aa4da89`), pushed and clean, with native live build unchanged.

Latest correction: [stock-input melee visual aim](../STOCK-MELEE-VISUAL-AIM.md)
reproduced and fixed at `a85d91f`. Stock-input mode disabled the controller-pose
query still used by animated hands, leaving them head-facing. The hands/equipment
and melee-only constraint now use owned simulated aim. Full CTest 134/134 passed
in 24.79 seconds; 47 chunks compile. Gameplay/network/damage are unchanged.
The focused candidate includes the same correction at `aa4da89`; its 45 chunks,
simulated-visual test, existing melee-aim test and expanded display test pass.
Focused preview diagnostics/drawing checks were added at `053671e`.

The new [stock melee input contract](../STOCK-MELEE-INPUT-CONTRACT.md) executes
the full cached parser/formatter/hierarchy with real VR binding output. Default,
fast, mid and slow light/heavy/auto-completion cases pass; default block/push,
follow-up, cancel and immediate versus held-block release cases pass. This is
an optional source test outside CTest, with engine/action-consumption fixtures.
No physical melee work, deployment or authoritative damage claim is involved.
PR #12 contains these contracts. The parser cases now compare local and receiving
full parsers through actual stock buffering/send/receive methods with an in-memory
RPC sink, four-frame client ring and duplicate packets. All cases pass; a deliberately
false received primary hold is detected. Engine serialization and damage remain
outside the fixture.

Focused headset-test candidate: `7eb98ee`, PR #11, based exactly on accepted
live Lua `ab9e9db`. Separate checkout `D:/Projects/games-xr/Darktide VR melee preview`,
branch `codex/accepted-baseline-melee-preview-2026-09-08`. Only preview Lua/tests
and its document were applied. Its 45 chunks compile; preview, display lifecycle
and existing melee aim tests pass. No deployment occurred. Preserve native
capture/harness `23345e5` for this focused test. The stacked preview remains
PR #10 at `672b1e4`; reference contract is committed/pushed at `d04d7f6`.

Latest read-only live sample remains healthy: shared_ready 269197, zero interval
fallback/pose mismatches, original/generated interval pairs both 23.0272/s.
This is an observational delivery sample, not a controlled performance result
or worn acceptance. Darktide PID 113588 retains its 05:44:55 session start.

Preview checkpoint `672b1e4` is pushed as PR #10 against PR #9. Full offline
133/133 passed at `6ecc133`; the subsequent explicit sphere-sweep rejection and
snapshot wording passed the focused preview test. A new optional stock-reference
contract passed with the pinned LuaJIT against the cached source root:

```powershell
& build/dependencies/luajit/src/luajit.exe tests/tooling/test-melee-reference-stock-contract.lua _downloads/Darktide-Source-Code
```

It verifies actual stock damage-window reference dispatch with supplied poses,
not server damage or live contact behavior. Physical melee remains paused.

Current priority: [first stock swing preview](../STOCK-MELEE-PREVIEW.md), following
the user's explicit pause on physical melee until server-side melee is locked
in. The parked stock damage-contract work was committed and pushed at `7882302`.
The preview is an opt-in local guide using actual stock sweep frames; stock-input
mode reads simulated aim, while the existing local mode reads controller aim.
It retains stock origin/reach and hides during running weapon actions. No live
deployment or worn acceptance has occurred for this candidate.

Latest review checkpoint: UI RGBA export/checksum and concurrent log-reader
fixes are committed at `79a5519`, pushed as PR #9 against the RGB checksum
branch. Full offline CTest passed 131/131; those native changes are undeployed.
This branch adds only an optional cached-stock melee damage contract.
It executes the stock hit and damage-forwarding methods with engine fixtures,
confirming replay-side mutations, separate target index/count arguments and
mass/armour/breed abort selection. It does not call the running game or enable
physical damage. The user subsequently paused physical melee until server-side
stock melee is locked in, and requested a first-swing preview based on the hand.
Park the damage-falloff question and physical-damage implementation.

Validation for the new contract:

```powershell
& build/dependencies/luajit/src/luajit.exe tests/tooling/test-melee-damage-stock-contract.lua _downloads/Darktide-Source-Code
```

Result: pass against cached stock snapshot
`0f0cb45991e9305ef4a7b925370792d7d6035f95`. This optional test is outside CTest;
the earlier 131/131 result is not a new full-suite run for this branch.

Windows x64, headset tests OFF. Full CTest run at `52fdd88`: 125/126 pass in
22.57 seconds. `hud_options` assumed fixed top-level menu positions and failed
after the accepted gun-pitch setting was added. The test now resolves settings
by ID, rejects duplicate IDs and retains the HUD/turning/binding group checks.
No runtime change. The affected HUD, gun aim, Lua compiler and invariant checks
then passed 4/4 in 0.83 seconds. This is a full run plus a focused repair rerun,
not a new single-run 126/126 claim.

Commands (CTest is installed in Visual Studio's CMake/bin directory):

```powershell
ctest --test-dir build/windows-vs2022 -C Release --output-on-failure
ctest --test-dir build/windows-vs2022 -C Release --output-on-failure -R '^(hud_options|gun_aim|lua_source_compile|lua_source_invariants)$'
git diff --check
```

## Quest setup

The user prioritized Quest setup before further implementation. Proximity
Disable then Status succeeded. Default Ready passed: one authorized Quest,
Streamer and VDXR available, 600/600 submitted frames, no unrendered frames.
All 43 Lua chunks compiled. Evidence is ignored under
`artifacts/unattended/preflight-20260907T194407Z.json`.

Started the ordinary launcher with `-EnterPsykhanium -EnableHudPanel
-DlssGeneratedStereo` while Darktide was closed. Initialization is being checked
in `artifacts/unattended/workday-session-20260908.log`. Proximity remains applied
for active development; restore with Enable and Status when development ends.
The readiness result alone is not gameplay stereo or worn acceptance.

Setup completed: the title screen needed a foreground Space press. Automatic
character selection then continued. Fresh range result=pass at 19:48:13 UTC,
synchronized sequential stereo at 19:48:13 and both rigid hands ready at
19:48:14. Harness shared_ready=704 with zero interval fallback and zero pose
mismatches. No mod WARNING/ERROR in the checked fresh console. Game and launcher
remain running for development; no new worn acceptance is claimed.

## Next development

Continue contextual two-handing/ADS from `docs/VR-TWO-HANDING-ADS.md`. Input
ownership, numeric pose solving and the production coordinator are implemented
offline. Ten focused checks passed in 1.11 seconds; the subsequent seven affected
checks passed in 1.16 seconds, including all 45 Lua chunks. The candidate defaults
disabled with no guessed weapon sockets. Session-only tracked socket capture
commands now pass 4 focused checks in 0.97 seconds, but are not deployed or worn
tested. Support-hand visual placement is now implemented; the complete offline
suite passes 128/128 in 21.88 seconds (45 Lua chunks). Virtual-stock math is
tested but lacks a production shoulder anchor; toggle-ADS coordination remains.
PR #3 contains this branch against the aggregate branch in PR #2. See the implementation
checkpoint in `VR-TWO-HANDING-ADS.md`. Preserve independent bindings, neutral guards, staff and
accepted controller-based gun aim. Remaining weapon-family worn checks require
the user's observations and should not block independent offline work.

The fixed-input interruption audit found that a null service between render
samples could leave an active calibration countdown alive. Fixed cancellation
now retires a started measurement while preserving its initial chat-close wait.
Validation: `ctest --preset windows-vs2022-debug -R 'two_hand|gameplay_ui_ownership'
--output-on-failure` passed 3/3 in 0.08 seconds;
`tools/stereo/test-darktide-lua-source.ps1` compiled all 45 chunks. This source
change remains undeployed with the rest of the two-hand candidate.

The subsequent mapper audit reproduced a swallowed real aim-button release
when support cancellation happened in the same sample. The mapper now tracks
which physical controls actually contributed, preserving a healthy alias's
release while still suppressing cancelled-only edges. The equivalent axis-loss
case is covered, including a blocked alias that must not manufacture a release.
Full Windows x64 Release CTest: **128/128 pass in 24.07 seconds**, headset tests
OFF; command `ctest --test-dir build/windows-vs2022 -C Release --output-on-failure`.
All 45 Lua chunks compile. Latest evidence can be copied from
`build/windows-vs2022/Testing/Temporary/LastTest.log` before another run.

The two-hand branch ends at `d579634`; the full-suite evidence is preserved in
`artifacts/unattended/two-hand-input-audit-128-20260908.log`. Independent offline
performance-tool work continues on the GPU trace branch. PR #4 adds capture
integrity and render-eye checks to the analyzer, stacked on PR #3. Its focused
regression and two saved real trace analyses pass; see
`docs/PERFORMANCE-HEALTH-ANALYSIS.md`. Live settings and the accepted running game
remain unchanged. Keep working in this session until explicitly told to stop.

GPU analysis checkpoint: `451b8e1`, PR #4. The next offline task on the DLSS
readback branch rejects bitmap/log dimension mismatches in both UI measurement
CLIs. Two affected CTests pass in 1.22 seconds; the original saved readback
retains its previous measured values. See `docs/DLSS-UI-DETAIL.md`. These are
diagnostic integrity changes, not rendering changes or resolution of motion blur.

DLSS readback checkpoint: `ae5aae1`, PR #5. The optional virtual-stock anchor is
now connected on its own branch, using explicit measured profile values only.
It latches body heading at contact while following translation and artificial
scene turns, and falls back to plain two-hand aim without owned body data.
Configuration edits cancel the grip gesture. No stock profile is supplied,
enabled or deployed; crouch/torso/roomscale behavior remains worn-unverified.
Full integrated Windows x64 Release suite: **128/128 in 26.36 seconds**, headset
tests OFF, including all 45 Lua chunks and both diagnostic-tool changes.
Evidence: `artifacts/unattended/virtual-stock-integrated-128-20260908.log`.
Virtual-stock checkpoint: `1a481f1`, PR #6, based on the DLSS readback branch.

The next branch adds the optional mission-device/hold-interaction stock consumer
contract. It passes with the actual controller mapper and cached stock input
methods; see `docs/MISSION-INTERACTION-AUDIT.md` for command, coverage and fixture
limits. No runtime code changed for this step; it does not establish real
revive/rescue, mission completion or worn acceptance. Prior full CTest remains
128/128; this source-dependent check is separate.
Mission contract checkpoint: `13e9a8b`, PR #7.

The next diagnostic branch verifies generated RGB pixels against the existing
native exported eye checksums, extending the earlier extent check. Both focused
DLSS measurement CTests pass in 0.84 seconds, and the original saved readback
matches both hashes without changing its measured contrast. See
`docs/DLSS-UI-DETAIL.md` for checksum coverage limits. Native/live state is unchanged.
Generated RGB verification checkpoint: `ee33a6a`, PR #8.

The next branch adds native one-shot UI RGBA checksums and corresponding Python
verification, with explicitly limited support for historical logs. Three new
offline checks cover real WARP export, forced export failure and native/Python
round trip; configured CTest count is now 131. A concurrent-log-reader regression
exposed exclusive native log opens, fixed with explicit sharing; both native
success/failure cases then pass 20 repeated runs each. Native/test Release builds
pass. This native candidate is not deployed; preserve the accepted live DLL.
Full final Release CTest: **131/131 in 23.23 seconds**, headset tests OFF;
`artifacts/unattended/ui-rgba-final-131-20260908.log`. This supersedes the earlier
130/131 run that exposed the diagnostic log-sharing failure.
