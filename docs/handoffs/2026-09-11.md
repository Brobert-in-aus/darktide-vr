# Handover for 11 September 2026

**Session resumed:** the user restarted continuous development and the existing
20-minute heartbeat on 11 September. Follow [today's frame-production work](../FRAME-PRODUCTION-2026-09-11.md)
for the latest results. Idle desktop encoding recovered ~116 distinct FG FPS and ~77 native FPS;
HUD on/off remained equivalent. Active desktop encoding coincided with ~97.4 FG
FPS. The streaming-on reversal is unconfirmed, so preserve that limitation. Earlier paused/stop statements below describe the prior
evening, not the current authorization. Continue until the user stops the session.

Additional morning work:

- [Descriptor-copy demand](../DESCRIPTOR-COPY-DEMAND-2026-09-11.md) removes
  unnecessary clean-launch shader descriptor bookkeeping. The focused candidate
  raises game Present rate from about 75 to 93, but delivered native stereo falls
  from about 75 to 53 FPS in A/B/A controls. Do not promote it alone. Investigate
  the single-slot producer/consumer handoff; a 72 FPS cap delivered 71.75 FPS.
  FG-tested baseline/candidate controls both reach about 119-120 distinct FPS
  at 120 Hz and about 120 at 144 Hz, with the extra submissions being repeats.
- [Native original ring](../NATIVE-ORIGINAL-RING.md) adds an opt-in three-slot
  native publisher using the viewer's existing original-frame protocol. Typed
  and typeless GPU pixel/ownership tests pass. With typeless format support,
  repeated native mission trials deliver 89.57 and 89.34 FPS against a fresh
  accepted 76.47 FPS control: about 17% improvement from the combined candidate.
  Both exit cleanly, restore files and report zero pose mismatches. Physical
  performance and worn visual acceptance remain pending; keep it opt-in.
- [Simulator exit status](../SIMULATOR-EXIT-STATUS-2026-09-11.md) retains the
  viewer process handle so Windows PowerShell 5.1 cannot lose its exit code after
  readiness polling. Standalone zero/nonzero exit checks pass.
- [On-demand desktop capture](../ON-DEMAND-WINDOW-CAPTURE.md) removes unnecessary
  window captures during stereo. Candidate builds/tests pass; physical comparison
  awaits resumed VD streaming. It is not the accepted viewer default.
- [SR native command identity](../DLSS-SR-NATIVE-COMMAND-IDENTITY.md) matches all
  64 sampled Streamline native command lists to NGX. The earlier pointer mismatch
  was the proxy layer; no history/jitter/visual fix is claimed.
- [GPU activity recording](../BENCHMARK-GPU-ENGINE-ACTIVITY.md) is available via
  `-ObserveGpuEngineActivity` and has now completed two mission diagnostics with
  clean job cleanup. A third identity diagnostic also completed successfully. Counter gaps remain unknown.
- [Dispatch command routes](../ENGINE-DISPATCH-BUNDLE-COMMANDS.md) identify the
  graphics-queue compute preparation path for all 121 sampled category-2 commands.
  A follow-up identity capture identifies GPU-particle emit/sim batch labels;
  736/736 descriptors were valid at 92 waits. The next investigation is the
  particle update flag and per-view ownership, before considering shared work.
- [Particle ownership tracing](../ENGINE-PARTICLE-STEREO-OWNERSHIP.md) locates
  update flags, regular/shadow paths, ping-pong buffers and parameter lookup.
  Camera-dependent work prevents a blanket second-eye skip; duplication and
  cost still need measured evidence before an engine behaviour change.
- [Particle submission observer](../PARTICLE-SUBMISSION-OBSERVER.md) completed
  1,024 readable records across seven threads. No same-object/update emit/sim
  repetition was observed; pending-eye tags could not identify the worker views.
  Virtual Desktop encoding was active again, so the run is diagnostic evidence,
  not a comparable clean FPS measurement. Installed files and flags restored.
- [Compute dispatch timing](../COMPUTE-DISPATCH-OBSERVER.md) recorded 4,096 calls:
  resource binding occupied about 92.4% of observed aggregate command-recording
  wall time across eight threads. A further 4,096-call stage capture attributes
  about 58% of binding time to descriptor binding at `7e0fe0`; investigate our
  always-installed descriptor-copy metadata hooks next. This is
  not a frame-time share or projected speedup. The idle-encoding run restored
  files and returned about 74.73 native FPS, with zero pose mismatches.
- [Residency reader correction](../RESIDENCY-READER-CORRECTION-2026-09-11.md)
  fixes corrupted function-owner labels after chunk parsing; all three affected
  local captures were reprocessed. Raw timings, wait gates and FPS are unchanged.

Both engine diagnostic sessions exited cleanly, restored saved files and reported
zero pose mismatches. Physical rendering remains unavailable; the request to
resume the Quest's PC stream is pending. Continuous offline/simulator work remains
authorised. Normal proximity handling is restored between sessions.

Latest investigation: [HUD/menu controls](../HUD-MENU-PERFORMANCE-2026-09-10.md).
Both-off validation returned 91.45 distinct FPS, not the historical 112.68;
HUD-only returned 91.36. Do not attribute the historical loss to HUD or debug
validation. The simulator title counts submissions including repeated images:
114.24 submissions/sec in the both-off run contained 91.45 distinct frames and
22.79 repeats. Continue investigating the unreproduced historical baseline using
explicit settings and publication rates. Final both-off was 89.07 versus 88.08 menu-only and 88.22 both-on; drift prevents
a confident menu-cost claim. All five runs closed/restored with zero mismatches.
Full matrix results are in the linked note.

Latest repeat: [clean-launch audit](../CLEAN-LAUNCH-DIAGNOSTICS-2026-09-10.md).
Matched foreground controls deliver 89.11 FPS with validation versus 88.43 without;
the debug-layer bottleneck claim is withdrawn. Cleaned optional diagnostics yield
88.55 FPS with HUD/FG intact. Normal launches now suppress stale optional traces
reversibly. Investigate HUD/menu work and on-demand desktop capture next.

Latest: [launch parity corrections](../LAUNCH-PARITY-2026-09-10.md) enable the
simulator HUD/menu input, expose both-eye preview and make physical graphics
validation opt-in. The user confirmed the preview/HUD. The debug-layer FPS dip
is provisional because user interaction may have affected it. All sessions closed.

**Evening follow-up:** the user subsequently authorized resolution baselines and
a physical mission comparison. Six simulator controls completed; resolution alone
did not reproduce the full physical deficit. The physical session is now closed
with temporary files restored. Read [physical closeout](../PHYSICAL-SIMULATOR-GAP-2026-09-10.md)
and [resolution results](../RESOLUTION-BOTTLENECK-2026-09-10.md) before repeating work.
Match HUD, camera position/orientation and FOV as well as resolution next.
The live-session caution below describes the earlier state, not an active session.

Resume only when the user starts the next session. No automatic optimisation
run is scheduled. The mistakenly created higher-resolution automation was
deleted on clarification; the original heartbeat remains paused.

## First task

Run the second optimisation batch in the [11 September todo](../phase1/todo-2026-09-11.md)
at **2496x2688 per eye**, matching the current Virtual Desktop/VDXR recommendation.
The former simulator default was 2112x2304; the new dimensions mean approximately
38% more output pixels. Do not compare the previous ~120 FPS mission-start result
directly with the current ~80 FPS physical hub result as though resolution matched.

Commit `4ca2657` removes the silent fixed-size default. The runner accepts a recent
successful VDXR Ready receipt, or both explicit dimensions for a labelled control.
Resolver tests pass and the actual 10 September receipt resolves the correct size.
Matched higher-resolution controls have since completed; see the newer results
linked at the top. Do not repeat them on the assumption that none exist.

## Evidence and comparison controls

The user's hub observation was confirmed at about 40 original + 40 generated FPS.
Frequent alt-tabbing explains intervals with no new generated frames; foreground
1:1 intervals remain valid. Current timing showed source-frame waiting rather
than the earlier long `xrEndFrame` stall. Hub content, larger resolution and runtime
differences have not been apportioned. The physical launch also used standard
polling while synthetic runs enabled precise polling; match this variable.

Read [hub investigation](../HUB-PERFORMANCE-2026-09-10.md),
[day summary](../DEVELOPMENT-HANDOFF-2026-09-10.md), and
[alpha requirements](../EARLY-ALPHA-READINESS.md). Keep Quality DLSS and FG On/Off
as explicit controls, prioritising efficient stereo, reconstruction and engine
rendering toward 120 FPS. 144 Hz is a stretch goal. Skip basic gameplay verification.

## Live-session caution

At the time this handover was written, the user had launched a physical headset
test. Do not assume shutdown or restoration has completed. Check processes and
installed hashes before the next trial. The temporary launch wrapper at ignored
`artifacts/unattended/home-headset-test-20260910/launch.ps1` backs up and restores
the two native DLLs and viewer in its finally block, then restores proximity
automation. Verify its outcome rather than overwriting a live installation.

That test selected native FB244186 diagnostic cleanup and viewer 216E3F76, retaining
accepted Lua. These are test selections, not blanket acceptance of all accumulated
changes. Preserve the accepted mixed installation; use a separate task branch
and reversible trials. Follow AGENTS.md for readiness, Lua compilation and validation.
