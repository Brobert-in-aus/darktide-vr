# 8 September 2026 — night handoff

The user reports **"All checks passed"** and requests winding up, updating docs,
preparing the full todo list for tomorrow, and committing/pushing. Tonight's
tested changes are accepted. Development is stopped; resume with the
[9 September todo list](../phase1/todo-2026-09-09.md). Billboarding is the next
implementation task, followed by the active pickup-popup sizing correction.

## What completed today

Daytime work prepared SoloPlay 2.6.2 with stock online-style local combat rules,
stock light-swing preview, deployment/rollback and Lua validation hardening,
billboard material/readback research, shared marker-plane/capture-lifetime
foundations, and VDXR slowdown/stage-timing investigation. The latter visual
fixes remain incomplete; see the [workday record](2026-09-08-workday.md).
The requested last three Quest recordings were copied before the preview fix;
originals were retained. Transfer hashes remain in ignored local artifacts.

Evening testing and corrections completed:

- Mission crosshair restored; melee guide changed to immediate drawing so old
  frames no longer accumulate into a blue blur. Unified attack observer removes
  the hook warning.
- Wrist twist now chooses the nearest 45-degree stock first-swing angle while
  forward aim remains stable; current action/combo owns the locked angle.
  The user separately confirmed "Melee aim fixed."
- Staff charge and hit/weakspot feedback moved beside the hand-aim reticle,
  preserving stock relative layout and sharing geometry between eyes.
- Action-first binding dropdowns and persistent full-height options layout;
  stronger adjustable ranged stabilization and a bounded Light VR aim adapter.
  Saved Light is controller_aim_assist=new_slim; this adapts stock target/aim
  correction rather than forcing gamepad UI or moving the headset.
- Right-stick horizontal +/-45-degree turning sectors no longer overlap
  up/down. Snap turning rearms only at neutral. Existing bindings remain.
- Crosshair and attached feedback default to 70% of their former size, with a
  live Crosshair Scale VR option from 25–150% in 5% steps. It scales associated
  sizes/offsets together; the melee sweep and menu pointer are unaffected.
- Non-wrapping binding labels and width-fitted ability badges.

The final all-checks report closes tonight's requested worn checks, including
the final turn/scale/label update. It is not blanket certification of all weapon
families, full mission completion, remote official servers, billboard fixes or
pickup edge sizing. The user entered a solo mission, but no end-to-end mission
acceptance record exists. Physical melee remains paused pending stock/server
melee lock-in. DLSS generated stereo is enabled for gameplay; matched in-mission
performance and remaining blur/long-run slowdown work are still open.

## Accepted source/deployment map

| Purpose | Source head | Review / branch |
| --- | --- | --- |
| Main integrated source, including undeployed candidates | 287c651 | PR #71; codex/right-stick-turn-sectors-2026-09-08 |
| Installed focused Lua | 3341afb | PR #72; codex/focused-right-stick-turn-sectors-2026-09-08 |
| Installed scale viewer | 6688841 | PR #73; codex/reticle-size-viewer-2026-09-08 |
| Offline billboard diagnostics | 5248005 | codex/focused-billboard-readback-2026-09-08; not installed |

The main checkout now has a documentation-only night-handoff branch based on
287c651. The focused Lua worktree is `D:/Projects/games-xr/Darktide VR melee preview`;
the viewer worktree is `D:/Projects/games-xr/Darktide VR reticle size`.
These feature heads were pushed before wind-down. Keep the focused Lua mapper/
hand path separate from main's undeployed two-hand work; port small reviewed
changes rather than copying complete main modules.

The viewer is based on accepted ba91dcc with only reticle scaling/live settings
transport and the scoped runner path added. Installed viewer SHA256:
`36D60D68E3FA70CDAF136920DAE6D044268B7A4A60851C7D2FEBF3788FA41815`.
It is staged at the main checkout's normal
`build/windows-vs2022/tests/xr_harness/Release/darktidevr-xr-harness.exe`.
Accepted capture/bootstrap DLLs, production shaders and SoloPlay were preserved.
Do not rebuild/deploy main native sources as part of an ordinary next launch.

Local transactional receipts under ignored `artifacts/unattended/`:

- `turn-size-lua-deployment-20260908.json` (first six Lua files).
- `crosshair-scale-lua-deployment-20260908.json` (three scale-option files).
- `crosshair-scale-viewer-deployment-20260908.json` (viewer only).
- `crosshair-scale-ready-20260908.json` (Ready passed, 600 rendered frames).
- `crosshair-scale-startup-restore-receipt-20260908.json` (preview startup restored).

Rollback uses the receipt manifests: reverse the two Lua transactions in reverse
order and restore the viewer transaction separately. The earlier hardcoded-size
viewer candidate was superseded and never installed. Keep
`mods/darktidevr_stereo_probe/darktidevr_crosshair_scale.flag`: it is persistent
settings transport, not a one-shot. It last contained 70. Light is already saved;
do not force its startup setting again after the user changes it.

## Windows x64 validation and final runtime evidence

Recorded before the final launch:

```powershell
tools/stereo/test-darktide-lua-source.ps1
ctest --test-dir build/xr-frame-stage-timing -C Release -R '^(turning|controller_bindings|controller_prompts|menu_prompts|scanner_stick_reference|crosshair_feedback)$' --output-on-failure
cmake --build 'D:/Projects/games-xr/Darktide VR reticle size/build/reticle-size' --config Release --target darktidevr-xr-harness --parallel 4
```

LuaJIT gates passed: 56 main chunks, 49 focused chunks, 50 installed chunks.
Six relevant CTests passed; the isolated viewer Release build passed. The
turning regression samples 720 directions against actual shortcut mapping,
checks exact diagonals and center-only snap rearm. Earlier feedback/assist/menu
fixtures passed as recorded in their feature docs. These are recorded results,
not a claim that the full repository suite was rerun during documentation cleanup.

Last session log: `artifacts/unattended/turn-scale-session-20260908.log`.
Preview initialized at 10:08:36 UTC; Psykhanium passed at 10:09:27 UTC.
shared_ready advanced to 715 in the inspected sample; one interval delivered
44.25 fresh plus 44.25 generated pairs/s, zero fallback/pose mismatches, and no
relevant mod warnings/fallback errors in the inspected startup window.
Final delivery summary reports **167292 fresh shared pairs / 180880 submitted
frames**, and OpenXR session destruction. These totals are not a frame-rate
measurement and do not diagnose the prior sustained slowdown.

The launcher initially recorded game.result=running on XR exit, then terminated
the orphaned flat game and retired its Psykhanium request. It restored the prior
Streamline/controller-aim/HUD flags. Do not relabel this as a clean game exit=0.
The log's automated visual_acceptance=pending label is not a worn assessment;
the subsequent user report supplies tonight's acceptance.

## Night shutdown and tomorrow's launch

Darktide and the XR viewer are absent at wind-down. Both existing Darktide
heartbeats (`keep-darktide-vr-work-moving` and the older duplicate) are PAUSED.
No overnight work is scheduled. Quest normal proximity automation was restored:

```powershell
tools/quest/set-proximity-override.ps1 -Action Enable
tools/quest/set-proximity-override.ps1 -Action Status
```

Both commands completed. The status sample reports Asleep and proximity false;
Quest provides no durable override query. Raw receipts are kept locally as
`night-close-proximity-{enable,status}-20260908.log`, never committed.

On an authorized next live session, perform Ready preflight and installed Lua
validation first. With the game closed and accepted files preserved, the launch
reference is:

```powershell
$prior = $env:DTVR_XR_PRECISE_PAIR_WAIT
try {
    $env:DTVR_XR_PRECISE_PAIR_WAIT = '1'
    & tools/stereo/start-darktide-vr.ps1 -SkipDeploymentSync -EnterPsykhanium -EnableHudPanel -DlssGeneratedStereo
} finally {
    $env:DTVR_XR_PRECISE_PAIR_WAIT = $prior
}
```

SkipDeploymentSync preserves this intentionally mixed accepted deployment.
Do not copy a fresh main build over it. Enable melee preview through its current
binding (last configured F6) or a backed-up one-shot startup flag; restore a
one-shot after fresh preview=on. The scale option works live without restart.
Read actual saved bindings before giving keyboard instructions: Enter is not
the user's chat key, and F4 is not a direct SoloPlay menu shortcut.

Recordings remain under `artifacts/quest-recordings/2026-09-08-evening/` with
`transfer-manifest.json`; no recordings, generated binaries, device identifiers
or the user's untracked image belong in this documentation commit.

## Documentation closeout validation

This wind-down changes documentation only. All 141 local Markdown file targets
in the 17 changed/new documents resolve, and git diff --check passes. The
installed viewer hash was rechecked against the accepted value above. No code,
settings or game deployment was changed during documentation cleanup. The
separate Quest proximity restoration is recorded above.
