# Darktide VR: handover for 8 September 2026

## Stop state and restart rule

The user requested winding up for the night after the final gun fixes. Development
is stopped. Do not resume unattended development until instructed. Both
work-continuation automations were checked and are PAUSED. End-of-session cleanup
is complete as recorded below. No merge or official online mission test.

Work branch: `codex/gun-pitch-hand-animation-2026-09-07`.
Aggregate review branch: `codex/development-continuation-2026-09-07`.
Draft PR: http://192.168.8.181:3000/robert/warhammer-40k-darktide-vr/pulls/2
Final implementation commit: `ab9e9db` (later commits are handover documentation).
Native capture/harness remain the matching build stamped `23345e5`.
Preserve the user's untracked image; it is not generated work to clean up.

## What changed today

- Psykhanium uses stock input-frame aim and combat origins to approximate the
  constraints of online rules. Server input/origin capability assessments are
  documented; remote missions remain gated. Firing-only aim switching remains
  on hold. Cosmetic muzzle effects do not permit gun-only blind fire around cover.
- Staff visual particles begin at the hand/staff and converge onto the real
  projectile over about one metre, preserving physics/damage.
- Roomscale head/hands translate independently of the collider. A 10 cm deadzone
  chase uses stock movement acceleration/braking and manual-input priority.
  Fixed the wrong input-frame lookup that prevented displacement repayment and
  caused endless sideways drift. Vertical movement remains visual; crouch later.
- Ranged reticles include appropriate stock recoil/sway/assist. Native target
  transport carries the exact target plus tracking/recenter identity.
- Copied visible hands/body had query actors blocking lasgun shots at the face.
  Disabled only those visual-root actors, including sparse/nil actor slots.
  Live evidence shows 24 disables in 44 slots per root and forward scenery hits.
  Stock gameplay-body collision and damage processing remain intact.
- Rejected a hand-model-derived firing basis. Gun direction now follows the
  physical controller aim pose and placement follows its grip position.
- Final candidate adds **Gun pitch angle (degrees)** in VR mod settings:
  default **-10**, range -45..45, negative lowers the barrel. Changes apply live
  to gun aim/reticle/visual orientation; staff and support aim are unaffected.
- Final candidate aligns the right visible hand to the corrected gun using the
  authored wrist-to-grip relationship. Hand geometry does not feed aim back.
  Draw, reload and unwield now retain tracked gun ownership instead of briefly
  handing its angle to the animation. Stock timing, ammo and moving parts remain.
- Added contextual two-hand support, virtual stock and possible stock ADS
  activation to the todo list. See `docs/VR-TWO-HANDING-ADS.md` for primary-source
  research and input ownership boundaries. This is planned, not implemented.

Earlier controller hints/lifecycle, performance and online-source investigations
are preserved in `2026-09-07-development.md` and their focused audit documents.

## Worn acceptance versus remaining checks

Accepted by the user: sideways step/stop settles without drift; staff normal/
charged shots and reticle while leaning are good; controller-based gun left/right
alignment is good. The user then requested the additional downward pitch.

Final follow-up: the user reports **"All checks passed"** to the final pitch,
visible hand placement, switch-away/back and reload question. Those checks are
accepted on `ab9e9db`; do not reopen them as unresolved alignment bugs tomorrow.
Settings adjustment, separate enemy-damage confirmation, wider roomscale
transitions and remaining owned ranged families are still separate checks.

## Validation

Windows x64; native unchanged for final Lua-only changes. The final selected
CTest run passes **9/9 in 0.91 seconds**, including all **43 Lua chunks** and
source invariants. New checks cover pitch sign/live setting/staff isolation,
120 translated/rotated/scaled parent cases, draw/reload pose ownership, the
actual aim reader and the actual right-hand transform. Run:

```powershell
ctest --test-dir build/windows-vs2022 -C Release --output-on-failure -R '^(gun_aim|lua_source_compile|lua_source_invariants|online_reticle|ranged_aim|ranged_evidence|roomscale|rigid_hand_readiness|weapon_hand_roles)$'
```

Full integrated baseline remains 124/124 (22.92 s), predating later additions;
126 currently configured does not mean 126 fully rerun. Optional stock source
contracts cover 530 input elements across 62 templates, 135 scheduler scenarios
and 12 pellet-scheduler cases, with documented engine/damage substitutes. These
are not counts of worn-accepted weapons.

## Resume sequence

1. Inspect this handover, Git status and the latest user instruction. Do not
   enable the paused heartbeat automatically.
2. Follow AGENTS.md: proximity Disable/Status and default Ready preflight before
   deployment/live work. Wireless ADB may drop while VD stays connected. Use
   discovery/reconnect if needed; duplicate transports to the same Quest can
   make the exactly-one-device check fail. Verify identity before removing a
   duplicate. Do not restart VD merely for suspension.
3. Arm Psykhanium while the game is closed, retaining HUD/generated stereo:
   `tools/stereo/start-darktide-vr.ps1 -EnterPsykhanium -EnableHudPanel -DlssGeneratedStereo`.
   Verify fresh Lua stereo initialization and nonzero harness `shared_ready`.
4. Preserve accepted gun pitch, visible hand and draw/reload behavior; verify
   actual enemy hits and remaining ranged families. Staff is the accepted
   regression control. Use the pitch setting for ergonomic
   adjustment instead of deriving aim from the animated hand skeleton.
5. Resume ranged-family acceptance, then contextual two-handing/ADS as prioritized
   by the user. Physical crouch, online missions, SoloPlay, melee, handedness and
   blur/performance work remain open in `docs/REMAINING-DEVELOPMENT.md`.

## Final session and cleanup

Final Ready and sync passed for `ab9e9db`. Last launch evidence is under ignored
`artifacts/unattended/gun-pitch-*20260907.*`. Console beginning 10:21:33 UTC
reports range pass at 10:22:55, fresh stereo/rigid hands and both visual roots'
24/44 collision cleanup. Harness sample: `shared_ready=750`, zero interval
fallback, one cumulative pose mismatch. The lasgun controller alignment log at
10:23:13 reports 0.0290 degrees quaternion residual and zero grip-position error.
No mod WARNING/ERROR in the checked fresh log. These establish initialization,
not final worn acceptance. The final pitch/hand/draw/reload question was sent;
the user's subsequent **"All checks passed"** accepts pitch, hand placement and
draw/reload continuity. This supersedes the initially pending shutdown record.

The owned final game was asked to close; its launcher exited successfully,
restored prior runtime flags and retired its Psykhanium request. No Darktide
process remains. Proximity `-Action Enable`, then `-Action Status`, both succeeded.
Quest does not expose a durable query of the override, so this records successful
restoration commands rather than an independently readable override flag. Both
continuation automations remain PAUSED. Virtual Desktop itself was not restarted
or closed. No further overnight work is scheduled by this task.
